/**
 * @fileoverview 配置 Log Note 的 Next.js 构建行为。
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  outputFileTracingRoot: process.cwd()
};

export default nextConfig;
