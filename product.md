---
status: mvp-implemented
created_at: 2026-08-11
---

# Log Note

## 产品原点

Log Note 想把我们现在这种日日记录生活的方式转化为一款具体产品。

它首先解决的不是“做更多分析”，而是让人能够低成本地持续记录一天，并让记录在之后真正有用。

## 当前已经确认的记录方式

1. 日记按照原有分区组织，例如日常、健康、学习和交易，不把所有内容混成一条统一分类流。
2. 每个分区内部按照时间点或时间段记录“做了什么”，必要时补充结果、状态或下一步。
3. 日均步数等跨全天汇总的数据可以固定记录；饮食、作息、学习和交易等内容使用放在对应分区里的参考模板，不要求逐项填写。
4. 模板需要同时提供分类、字段提示和具体示例，让人打开后就知道怎样记录。
5. 记录不是最终成果。记录需要在后续转化为评价和调整，再通过新的记录验证调整是否有效。

## 一句话方向

纯净记录 + AI 拟合 + 健康监控。

当前先把“记录”做对，再继续讨论 AI 如何理解记录、如何形成反馈，以及健康监控应该做到什么程度。

## 初版范围

初版已经确定采用移动端优先的 Next.js PWA：

1. 不需要登录，全部数据先保存在当前设备的浏览器中。
2. 首页按日期显示记录时间线，点击固定的“记录”按钮即可新增。
3. 每条记录自动带入时间，并支持分类、自由标签、编辑、删除和搜索。
4. 分类由用户维护；分类名使用 `父级 · 子级` 时，导出会生成对应的 Markdown 层级。
5. 模板包含名称、默认分类、默认标签、输入提示和文本骨架，暂不建设复杂表单设计器。
6. 支持导出单日或全部 Markdown，同时使用 JSON 做完整备份与恢复。
7. 初版不加入 AI 能力。

## 初版设计原则

- 视觉方向：像一张安静、留有时间痕迹的纸，以克制的蓝色作为唯一操作强调。
- 内容结构：日期是方向，时间线是工作面；首页不展示统计、分类筛选或空状态说明，固定加号是唯一主动作。
- 交互方向：记录抽屉从底部快速出现，默认只显示输入区；模板使用下拉选择，分类、标签和时间收进“更多”；模板拥有顶部独立管理入口。

## 后续议题

- 这款产品首先为谁使用。
- AI 应该在什么时候介入，输出什么。
- 健康监控与普通生活记录之间的边界。
- 一条记录如何逐步转化为周评价和实际调整。
