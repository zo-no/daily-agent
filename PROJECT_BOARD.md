# O1-KR4｜Log Note 影响力成果项目任务看板

> 本文件是项目任务、优先级和验收状态的唯一正式来源。`docs/思维风暴/` 只保存探索想法，未进入本看板的内容不代表开发承诺。

- 最后更新：2026-09-04
- 项目归属：O1-KR4｜通过 Log Note 形成可验证的个人影响力成果
- 项目意图：通过把模糊产品目标拆解、实现并形成可展示证据，提升“能把模糊目标拆成可执行系统”的个人影响力
- 对齐状态：已写入 `/Users/kual/Desktop/memory/OKR.canvas`；Log Note 项目是 O1-KR4 的行动载体，O3 保留个人生活记录与评价实践
- 当前阶段：MVP 收口、成果证据与发布基线建设
- 当前判断：产品已达到“可演示 MVP”，尚未达到“可放心长期使用/正式发布”
- 当前分支：`main`
- Git 状态：2026-08-31 正式产品变更 `70adfab` 已推送至 `origin/main` 与美团仓库 `~xiaokeqiang/log-note-hackthon` 的 `main`；各任务行中的“未提交、未推送”保留为该项验收当时的历史状态

## 会话与 OKR 绑定审计（2026-08-12）

项目内 Codex 会话按稳定编号命名；编号不随 OKR 归属变化而变动。

| 编号 | 会话任务 | OKR 绑定 | 绑定依据 | 当前状态 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| T00 | 项目总控 | O1·KR4 | 统一调度可靠演示、案例、展示与影响证据，直接服务 KR4 交付 | 进行中 | 维护任务边界、验收与异常续跑 |
| T01 | 可靠演示基线、质量门禁与本地数据安全收口 | O1·KR4 | 核心路径可靠性是 KR4“可靠演示基线”的前置条件 | 已完成至 LN-012 | 等待总控按 Ready 队列分派下一项可靠性任务 |
| T02 | 智能分组 AI 原型 | O1·KR4 | 早期 DeepSeek + SQLite 探索，已被更窄的 LN-069 既有标签匹配方案替代 | 已替代；隔离 worktree 不合并 | 保留研究证据，不把原型的 SQLite、广义分析或额外数据模型并入主线 |
| T03 | 代码质量与提交收口 | O1·KR4 | 测试、构建与可回滚提交保障可靠演示基线 | 历史任务，已完成先前收口 | 仅在主线变更准备提交时按需唤醒 |
| T04 | 产品远景与反馈回路 | 非 OKR | 远期 Agent 自动化、匹配与产品思考不直接满足当前 KR4 验收 | 归档参考 | 不启动实现；后续另行立项 |

治理结论：项目登记与 OKR 真源当前均为 `O1·KR4`，历史 `O3·KR1–2` 显示冲突已解除；项目内任务继续按真实工作使用 `[O1·KR4]` 或符合规范时使用 `[=]`。

## 状态说明

| 状态 | 含义 |
| --- | --- |
| 进行中 | 已有明确负责人和写范围正在推进；主工作区同时最多 1 个写任务 |
| 下一步（Ready） | 依赖、范围、验收、验证和决策状态均明确，可立即拉取 |
| 验证中 | 执行者已返回，等待总控独立核对证据；不等于完成 |
| 等待 | 等待用户决策、审批、凭证、外部状态或观察期，并记录原因 |
| 待规划 | 方向成立，但需要先完成依赖或产品决策 |
| 暂缓 | 当前阶段明确不投入 |
| 已完成 | 已满足验收标准并留下验证证据 |

优先级：`P0` 阻塞可靠使用或当前阶段收口；`P1` 下一阶段核心能力；`P2` 长期探索。

## 项目基线

- Next.js 15 + React 19，账号归属、离线可用、移动端优先的 PWA。
- 已有记录新增/编辑/删除、日期时间线、月历密度浏览、搜索、领域/分类/模板管理、周期模板、中英双语、Markdown/JSON 导出恢复、本地图片附件、便携附件备份和离线壳。
- 未登录时所有业务路由统一停留在账号门禁；登录后文字与结构写入 `log-note:data:user:<auth.uid()>:v1`，图片 Blob 在 IndexedDB 内按同一账号命名空间隔离。文字记录、日计划、结构和设置本地先写、自动 debounce 保存到 Supabase；revision 不一致立即停写并要求选择版本，图片仍只在本地和便携备份中。
- 2026-08-31 当前门禁覆盖设计规范 11/11、206/206 单测、31/31 移动浏览器、书页仪式感/Diary Agent/首页右轨/Plan Agent/分类章节/领域复盘与 7 天确认式总结聚焦回归、PWA v15 production build/installability/已认证离线缓存/persistence/update、生产构建和 `git diff --check`；账号隔离、CAS 安全纠正、Google Calendar、日记导入、按已有领域/分类智能整理、AI 单日时间梳理、Diary/Plan 行内 Agent、可替换书脊形象契约、参考日记 UI、本地领域趋势与静态构建资源缓存优先均纳入覆盖。真实邮箱/Google 登录后的首次云端创建、跨设备 revision 冲突、双账号 RLS，以及领域复盘 14 天真实使用效用仍待真实会话验收。
- 2026-08-31 右轨同轴与固定横线断开实现提交 `26eedfa` 已通过普通 fast-forward 推送到 `origin/main` 与 `sankuai/master`，推送后分别以 `git ls-remote` 核对为同一哈希；全程未 force push、未 rebase、未改写历史。仓库证据仅保留两张 390px 主图，其余宽度由 31/31 浏览器回归覆盖。
- 正式产品源码、自动化、规格、README 与展示图片已进入 `70adfab`；本机技能、含会话信息的临时部署脚本及 `output/playwright/**` 运行产物未进入提交，工作树仅保留这些本地证据变化。
- 2026-08-31 美团 `master` 隔离合并候选 `8795da7` 保留既有 Cargo/CentOS 7 Node 20 与 3100 端口修复，Node 20 下单测 206/206、production build 与 `git diff --check` 通过。DevTools `test` 构建模板当前未配置内部发行所需的 AIBase 浏览器变量，仓库 `.env.production` 仍是已作废的外部 Supabase + `standard` 登录；因此本轮仅推进代码到 `master`，不触发新 Cargo 构建/部署。下一硬闸门是授权用户在 AIBase 创建/选择获批 Workspace，并在构建模板中注入 URL、publishable/anon 值和 `NEXT_PUBLIC_LOG_NOTE_AUTH_MODE=meituan-sso`，同时保持 DeepSeek 与 Google 变量为空。
- 2026-08-21 正式引入 Spec Kit `0.16.5`：Codex Skills 位于 `.agents/skills/`，项目配置位于 `.specify/`；项目 Constitution 与四份模板覆盖已接入 Log Note 的核心循环、账号隔离、离线、原始记录、备份兼容、准入和 `npm run check` 门禁。Spec 包只细化既有 `LN-###` 板项，不替换 `AGENTS.md`、`product.md` 或本看板。

## 进行中

| ID | 优先级 | 任务 | 负责人 | 验收标准 | 备注/依赖 |
| --- | --- | --- | --- | --- | --- |
| LN-078 | P1 | 普通记录编辑器内的 Hero 一击内容优化 | P01-T00 主工作区 | 仅普通自由文本编辑器在纸面书写区内展示一个不少于 `44px` 的现有 Hero 入口；点击非空且不超限的正文只发起一次有界内容优化，不出现聊天面板、回复输入或历史。候选稿继续占用原书写区，并以一行紧凑操作提供“看原文 / 使用优化稿 / 取消”；只有“使用优化稿”改变当前草稿，仍需原有 `Done` 才保存。空白或超限正文、取消、关闭、账号/目标/正文变化、离线、超时、限流或非法响应均零写入；请求只包含当前正文、语言、版本化随机会话目标与 fingerprint，不发送账号身份、记录 ID、标签、分类、附件、图片、完整文档。结构化与周期编辑器、Diary/Plan Agent、schema、同步、导出和备份不变；320/390/426/1280px 无遮挡或溢出，所有操作不少于 `44px`，聚焦回归、`npm run design:check` 与 `npm run check` 通过。 | 实现已 Returned，等待真实 Provider 与产品负责人验收。用户在真实移动编辑器截图上明确要求“Agent（Hero 图）也会进来看着，点击 Hero 图交互”，并否定独立按钮和偏重的小对话；实现复用现有 Hero，在同一书写区完成一击请求、原文/优化稿切换、显式使用或取消，外层 traveler、结构化/周期编辑器和既有保存链不变。严格六字段、同源、Bearer 账号校验、限流、体积、20/25 秒超时、一次模型调用、零重试、严格输出、request/target/fingerprint 回显与 stale invalidation 已由聚焦回归覆盖；隐私页已说明正文传输与“使用优化稿后仍需完成保存”。聚焦模型/Provider/路由/runtime/结构/政策 `28/28`、最终独立浏览器 `1/1`、完整 `npm run check` 连续通过设计规范 `11/11`、Node `249/249`、浏览器 `35/35`、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check`；320/390/426/1280px 临时截图已人工检查，无聊天层、遮挡或横向溢出，Hero/操作均满足命中区，键盘顺序为“正文 → Hero → 更多”。空白/超限零请求，pending 去重，取消/失败/Escape/正文变化/迟到响应零写入，候选与“使用优化稿”在 `Done` 前均不持久化；结构化与周期编辑器不显示入口。回滚只需移除 composer mount/样式/Provider/路由/模型/Mastra capability/文案，无 schema、迁移、持久提案、备份转换或数据清理。当前会话无可用 sigo skill 或 executable，已按同等完整性/准确性/格式约束/遗漏/可执行性清单复核并修正；构建保留既有 Mastra dynamic-dependency warning。真实非敏感 Provider 结果、至少 3 次时延、产品负责人 390px 主观视觉复核与 14 天 use/cancel/rewrite 观察仍为 Accepted 前开放证据。未提交、未推送、未部署。 |
| LN-076 Rework 12 | P1 | 将日记/计划拨杆下移，并明确今日日记导出 | P01-T00 主工作区 | 上方工具区在日记模式仅保留“搜索 → 设置 → 时间/分类”，计划模式仅保留“搜索 → 设置”；唯一“日记/计划”拨杆移动到右下快速操作区并在两种模式都可达。日记模式下，“导出今日日记 / Export today”与蓝色新增记录印章横向相邻、真实目标均不少于 `44px`，导出继续只导出当前所选日期；计划模式隐藏日记导出/新增记录，只保留下方工作区拨杆与原有新增计划。320/390/426/768/1280px 无溢出、遮挡或重复控件，键盘顺序与视觉顺序一致，回调、记录/计划数据、账号、离线、同步、备份和导出内容不变。 | 实现已 Returned，等待产品负责人刷新本地 PWA 视觉复核。旧实现回归准确失败于上方仍存在工作区拨杆；实现后上方只保留日记的搜索/设置/时间分类与计划的搜索/设置，下方唯一日记/计划拨杆在两种模式均可达，导出左、蓝色记录右且显示精确中英文同日标签。完整门禁首次发现 320px Agent 审阅态的临时通知条压住新导出目标，通知条上移到下方操作区之上后，Diary Agent 专项与整轮命中检查均通过。同状态对照：`output/ln-076-rework12/reference-implementation-comparison-390.png`；聚焦下方操作区：`output/playwright/ln-076-lower-workspace-export-record-390.png`。最终隔离验证通过设计规范 11/11、单测 233/233、浏览器 34/34、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check`。Sigo 复核确认范围、文案、验证与未改边界完整；未提交、未推送、未部署。 |
| LN-036 Phase 2 | P0 | 必须登录、账号隔离缓存与自动版本化云端保存 | P01-T00 主工作区 | 未登录无法进入首页/设置/结构/整理；邮箱密码与 Google 产生真实 `auth.uid()`；每个账号使用独立文字缓存和图片命名空间；新设备登录后优先加载云端，已有认证设备离线可继续；所有修改本地先写并自动 debounce 上传；云端每账号一份完整文字 JSON，省略图片；CAS revision 冲突停写并提供显式选择；旧匿名数据不删除、不静默上传；备份/恢复和 PWA 不退化 | 实现已 Returned，等待真实会话独立验收。独立审计的 3 个 P0 已修：NULL expected revision 不再绕过 CAS；云读取失败先重读、不直接写；reconcile 使用最新本机状态且云端文字合并保留本机图片引用。慢保存续写、operation ID 重试、账号切换异步 generation、图片 owner 捕获和旧图片认领回滚也已收紧；冲突页并列展示双方记录/计划/版本/更新时间，两个替换选择同权。2026-08-17 使用 Supabase CLI `2.114.0` 正式 link 到 `log-note` 生产项目，按顺序执行并登记 `20260816090000_log_note_documents.sql` 与 `20260816170000_require_expected_revision.sql`；migration list 本地/远端两项一致，后续 dry-run 显示 remote up to date，匿名读取两表及调用保存 RPC 均为 401。安全依赖升级后最新完整 `npm run check` 通过：设计规范 11/11、122/122 单测、22/22 浏览器、PWA production build/installability/已认证离线缓存/persistence/update、生产构建与 `git diff --check` 全绿，`npm audit` 为 0。仍需人工验证真实邮箱与 Google 登录、首次 revision 1、第二设备读回、stale revision 停写和双用户 RLS。未部署。 |
| LN-037 | P1 | 保留美团内部部署，并建立 GitHub → 个人腾讯云的独立公开发布链路 | P01-T00 主工作区 | 内部发行必须从已推送、可复现的明确 commit 构建，以美团 SSO 作为唯一可见登录入口，并使用获批的美团托管 Supabase/AIBase 账号与文字同步边界；真实会话必须证明稳定 UUID owner、auth.users、authenticated、auth.uid() RLS、CAS RPC、服务端 token 校验、双账号双向隔离、双设备 stale revision、已认证离线、备份、报告、HTTPS、脱敏日志、健康检查和回滚。任何密钥/员工标识不得进入仓库或证据，远程 AI/Google 入口保持关闭，真实个人笔记进入前另获数据边界验收。 | 用户要求主要参考 Hackathon 实践《用 CatPaw 部署 Web 应用》（学城 2761294276）；官方 CatPaw/AIBase/Auth 文档仅校验关键能力。首期采用 CatPaw CloudNative Node 20 单服务、端口 3100，规格包见 specs/006-internal-pilot/。旧的“外部 Supabase + 邮箱测试账号”方案已作废；新方案先由授权用户在公司网络打开 AIBase、创建/选择 Workspace 并配置 meituan_sso。若真实 SSO 不能生成兼容的稳定 UUID owner 或现有迁移/RLS/RPC 不兼容，立即停止并回到 owner 数据模型设计，禁止将 MIS/email 强转 UUID 或使用共享账号。CatPaw build 变量、HTTPS、日志及回滚能力也必须实测；腾讯云 note.kual-shown.online 仍仅作为后续公开发布路径。2026-08-29 本地实现已 Returned：默认发行仍保留邮箱/Google，内部发行由 allowlist 构建变量切为单一 meituan_sso，MIS/姓名仅作展示、UUID 继续作为 owner；内部模式在运行时拒绝非 UUID owner，退出失败也不能绕过门禁；内部 Settings 隐藏 Google Calendar，远程 AI/Google 变量首发留空。`npm test` 170/170、内部/默认认证定向 E2E 各 1/1、内部 320/390/426px、内部生产构建、部署契约及最终 `npm run check`（25/25 浏览器 + PWA + 设计规范 + diff）均通过；独立只读审查最终无 blocker、无新增 P2。2026-08-30 可演示集成基线 `5da8471` 已推送至 `origin/main` 与美团仓库 `~xiaokeqiang/log-note-hackthon` 的 `main`。2026-08-31 路由候选 `ecc30a3` 已推送至美团仓库 `master`，临时 clean clone 确认 `HEAD` 与 `origin/master` 一致并成功完成依赖安装和 production build；通过 AppKey 的 test HulkPlus/Cargo 模板部署，平台自动注册端口 `3100` 后观察到正常 OCTO HTTP 节点。Oceanus 主域名 `clockwork-log-note.adp.test.sankuai.com` 已创建，Cargo 实际生成并回填 `cargo-rebwp-sl-clockwork-log-note.adp.test.sankuai.com`；实际 HTTPS 根页加载登录界面，`/monitor/alive` 与 `/api/healthz` 均返回 HTTP 200 和固定 `{"status":"ok","service":"log-note"}`。T041/T042 完成仅证明构建、进程与路由可用；当前部署仍显示标准邮箱/Google 登录，AIBase Workspace、`meituan_sso` 唯一入口、精确回调、双账号/双设备/RLS/CAS、报告、离线和回滚真实验收仍未完成，因此 LN-037 保持进行中且真实个人笔记继续受阻。 | 2026-08-31 用户确认 CatPaw 文件保留但不参与个人腾讯云发布；本地已把 `@mtfe/hlb` 与内网 registry 隔离到 `ops/catpaw`，GitHub 根依赖完成公开 registry 干净安装，并新增完整质量门禁后才执行的 `master` → `production` standalone 构建、固定 host key SSH、checksum、原子切换、健康检查和自动回滚契约。部署专项与既有内部契约 12/12 通过，standalone 真实打包并在 Node 22 上启动后返回固定健康响应；Node 22 公开 registry `npm ci`、`npm audit`（0 漏洞）及最终 `npm run check` 均通过：设计规范 11/11、单测 216/216、移动浏览器 32/32、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 全绿；GitHub environment/SSH 身份、服务器一次性安装、真实 master 部署与失败回滚演练仍待外部配置，当前未 push、未改线上服务、未删除 release。 |
| LN-067 About Rework 1 | P1 | 将公开 About 从政策配套页升级为成熟宣传页 | P01-T00 主工作区 | `/about` 首屏明确 Log Note 品牌、核心承诺与打开应用动作，并以一个主导产品画面展示真实的快速记录、浏览、计划与 Google 只读日程语义；下方按“核心闭环 → 本地优先/账号隔离/原文不改写/可移植 → 可选 Calendar → 最终 CTA”展开。中英文信息等价，320/390/1280px 无横向溢出，正文不小于 16px、操作目标不小于 44px，键盘焦点与 reduced motion 明确；页面继续免登录、静态、无追踪、无账号或 Calendar provider，不增加首页控件、记录步骤、数据字段、网络请求或新依赖。 | 实现已 Returned，等待产品负责人视觉复核。结构化政策单测 6/6、About 聚焦浏览器 1/1、设计规范 11/11、生产构建与 PWA 回归、全局 `git diff --check` 均通过；320/390/1280px 首屏、无横向溢出、44px 动作、固定示意、标题顺序、可见焦点和 reduced motion 已自动验证并人工核对 390/1280px 截图。全量 `npm run check` 中单测 226/226 且 About 场景通过，整库浏览器为 30/34；其余 4 条失败位于同一脏工作树的首页侧栏、日期选择器、旧计划动作坞与 Diary Agent 点击层，不在本项写集内，因此 T027 与本项保持进行中，不标 Accepted。范围沿用 `specs/010-oauth-public-policies/` 与 LN-067；未提交、未推送、未部署。 |
| LN-076 Rework 11 | P1 | 在非当天日期提供一击返回今天 | P01-T00 主工作区 | 仅当所选日期不是设备本地今天时，在日期标题旁显示一个本地化次级“今天 / Today”动作；当天隐藏。真实目标不少于 `44px`，一次点击回到今天、收起已展开月历并把焦点交还日期标题，同时保留 Diary/Plan 与 Time/Category 模式；320/390/426/768/1280px 无溢出或碰撞，账号 payload 不变，不增加右轨项、记录/计划写入、存储字段或网络请求。 | 实现已 Returned。旧实现回归准确失败于 `[data-home-return-today]` 不存在；实现后新增场景与既有日期头场景各 1/1 通过，覆盖中英文、真实键盘焦点、月历收起、Diary/Plan、Time/Category、账号 payload 逐字节不变，以及 320/390/426/768/1280px 的 `44px` 命中区与无溢出/碰撞。390px 中文截图已人工核对。完整质量门禁各阶段全绿：设计规范 11/11、单测 223/223、浏览器 34/34、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check`；Spec Kit Rework 11 无 Critical/High 覆盖缺口。未提交、未推送、未部署。 |
| LN-076 Rework 10 | P1 | 统一 PWA 窄屏搜索与月历的书写面右边界 | P01-T00 主工作区 | 320/360/389/390px 的内嵌搜索与设置右缘停在可见装订刷线左侧 `8±1px`，不再继承普通记录的 `82px` Agent 预留；搜索关闭入口完整可见且命中区不少于 `44px`。`<390px` 月历只取“正常书写边界”和七个 `44px` 日期列所需最小纸面宽度二者较大值，360px 月格越过装订轴不超过 `8px`，320px 只保留不可避免的最小月格重叠；右轨仍可点击且无横向溢出。搜索/设置/月历状态、焦点、回调、记录/Agent 间距、数据、离线、同步、备份与宽屏布局不变。 | 实现已 Returned。新增回归先在旧 CSS 上准确失败：320px 搜索到刷线空带为 `42px`，扩大书写面后又准确捕获关闭按钮从 surface 右缘外溢 `44px` 且不可点击；最终 320/360/389/390 四档几何 1/1 通过，既有日期选择器、搜索编辑删除、页内设置/右轨各 1/1 通过。视觉证据：`output/playwright/ln-076-pwa-width-{search,calendar}-{360,390}.png`。完整 `npm run check` 退出 0：设计规范 11/11、单测 220/220、浏览器 33/33、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 全绿。Spec Kit Rework 10 无 Critical/High 覆盖缺口；未提交、未推送、未部署。 |
| LN-076 Rework 6 | P1 | 将右轨两个单文字模式按钮收敛为双模式可见拨杆 | P01-T00 主工作区 | “时间 / 分类”与“日记 / 计划”继续是两个独立的一击切换，各自在同一按钮内同时显示两个模式，并用滑块位置、抬起纸面和墨色共同表达当前态；移动端真实目标不少于 `44px`，键盘焦点与 reduced motion 明确，英文/中文均不截断；Diary 顺序为“搜索 / 设置 / 工作区 / 记录视图”，Plan 仅隐藏记录视图，不恢复底部重复控件；月历、Agent、记录/计划数据、账号、离线、同步、备份与快速记录步骤不变。 | 实现已 Returned、等待产品负责人查看换位后的最终视觉：两个切换继续各用一个真实按钮，内部同时显示双标签和一块抬起纸面滑块；移动端为竖向两档，桌面为横向两档。产品负责人本轮标注要求互换两枚拨杆后，日记页现为“搜索 / 设置 / 日记-计划 / 时间-分类”，DOM、视觉与键盘顺序一致；Plan 仍只省略记录视图。聚焦 LN-076 浏览器场景 1/1，390px 实际截图已核对，目录继续在完整工具栈后保留 `24px`，320px 月历继续避开完整工具栈。最终 `npm run check` 退出 0：设计规范 11/11、单测 206/206、浏览器 31/31、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 全绿。未改回调、状态、尺寸、间距、记录/计划数据、账号、离线、同步、备份或快速记录步骤；未提交、未推送、未部署。 |
| LN-076 Rework 7 | P1 | 收敛普通记录编辑器的写作区与更多信息层级 | P01-T00 主工作区 | 未展开时正文仍是占主导的书页写作面；展开“更多”后正文继续可直接编辑但不保留巨大空白，日期/时间、分类/标签形成紧凑信息账页，附件成为独立次级区，既有记录的删除动作置于有明确边界的危险页脚；“更多”暴露展开状态与受控区域，关闭/完成/更多/附件/删除均不少于 `44px` 且键盘焦点明确；320/390/426/768/1280px 无横向溢出，reduced motion、原文精确保留、附件/删除确认、账号、离线、同步、备份与一击保存行为不变。 | 本轮实现已 Returned、等待共享门禁与产品负责人视觉复核：展开态移动书写区收敛到 `184–236px`，元数据、附件与删除形成三级账页；正文键盘焦点改为页边细蓝墨线；`320–350px` 日期/时间纵排，新增回归先后准确拦截缺失 `aria-expanded`、详情控件越界 `26.8px` 与硬矩形焦点。五档展开态、中文 390px、精确保存、编辑删除、Markdown 列表/格式、附件和 44px 目标定向回归各自通过，证据在 `output/playwright/ln-076-composer-rework7/`；独立视觉/代码审查均无 blocker。`design:check` 11/11、`npm test` 180/180、独立 PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 通过。完整 `npm run check` 中本轮编辑器及相关回归全绿，整库浏览器为 23/29；剩余 6 条均来自同一工作树正在进行的 LN-076 Rework 8、右轨目录和 LN-010 Domain Insights（Agent 旧定位/素材断言、目录焦点、日期头 Rework 8 DOM、Domain Insights 默认领域），因此 T062 与本项保持“进行中”，不标 Accepted。未改字段值、回调、附件 Blob、删除确认、记录数据、账号、离线、同步或备份；未提交、未推送、未部署。 |
| LN-076 Rework 8 | P1 | 将 Diary Agent 提升为右侧书脊常驻爬行角色 | P01-T00 主工作区 | Agent 从内容流移到应用外壳级视口固定层；Diary 的空白日与有记录日均可见，移动端 `320–700px` 在右侧安全轨道巡游，桌面端静止探头，滚动顶部/中部/底部均留在视口且不遮挡工具、目录、导出、新增、正文或输入；有记录日 idle 态在 Agent 下方显示“拍一拍，分析日记”的本地化弱提示并随角色安全移动，提示盒不宽于 `52px` 且不覆盖记录文字，空白日、月历和非 idle 态隐藏；`idle/scanning/reviewing/complete` 使用不同节奏，月历、聚焦、按住、页面后台与 reduced motion 按规则暂停；空白日点击只显示 4.5 秒页边批注且不写入；Plan、搜索、设置与编辑器隐藏；角色素材不含书脊线，离线可用，并保留未来替换外观的注册边界。 | 实现已 Returned，等待产品负责人查看最终动效与 14 天偏好观察，不虚报产品 Accepted。角色四态直接从旧版石墨形象归一化，移除素材自带竖线；移动端采用带抓握停顿的 `28s / 20s / 32s / 30s` 单程慢速巡游，桌面与月历/焦点/按住/后台/reduced motion 静止。有记录日 idle 态使用 `52px` 紧凑两行弱墨提示，并向书脊外侧收拢 `2px`；文案经 sigo 收缩为“拍一拍，分析日记”与 “Tap to review”，保留动作和分析语义，空白日/月历/分析态不显示，既有按钮可访问名称与可见文案一致。Agent 为应用壳固定层，空白日批注 4.5 秒且零请求/零写入，Plan/搜索/设置/编辑器隐藏；目录文字碰撞经独立验收发现并修正为操作轴与书脊轴分离，未破坏 Domain Insights 或目录锚点。用户补充指出固定记录手绘横线延伸到 Agent 下方，移动端规则现改为左对齐并在行右缘前 `24px` 结束；共享门禁又捕获 426px 输入与 Agent 目标的 3px 隐形相交，因此仅把移动输入右缘再收 `4px`，输入高度、值、焦点、保存、行布局、数据、回调与桌面布局不变。390px 横线证据位于 `output/playwright/rule-gap-final/ln-061-fixed-scope-tags-390.png`，home hierarchy、date-led header 与 viewport-spine Agent 聚焦回归分别 `1/1`、`1/1`、`1/1`；同一自动化覆盖 320/426/600/671/700/768/1280px。外观注册表保留 legacy alias 并加入静态/动效/尺寸/模式，8 个新本地素材由 Service Worker v15 离线缓存。既有证据：`output/playwright/ln-076-agent-rework8/comparison-390.png`、`output/playwright/ln-076-agent-rework8-directory-proof/`、`output/playwright/ln-076-agent-rework8-final2/`、`output/playwright/ln-074-agent-idle-390.png` 与 `design-qa.md` Rework 8（`final result: passed`）。新增几何回归覆盖 `320/390/426/600/700/768/1280px`，提示盒宽度不超过 `52.5px`、与记录文字重叠不超过 `1px²`，聚焦 viewport-spine 与完整 Agent 交互各 1/1 通过。本次最终 `npm run check` 退出 0：设计规范 11/11、单测 206/206、浏览器 31/31、PWA production build/installability/authenticated offline cache/persistence/controlled update、生产构建与 `git diff --check` 全绿。Spec Kit analyze 无 Critical/High 覆盖缺口，独立只读验收无 blocker。2026-09-01 产品负责人通过 390px 中文截图指出周期值文字到手绘横线的间距偏大；移动端仅将内联标签与输入列下移 `6px`，单行标签到横线顶部由 `21.1px` 收至 `15.1px`，保留既有行高、`44px` 输入目标、横线长度和保存行为。新增几何断言先准确失败，修正后 home hierarchy 与中文 date-led/Agent 多宽度聚焦回归各 `1/1`；最终隔离 `npm run check` 退出 0：设计规范 `11/11`、单测 `220/220`、浏览器 `33/33`、PWA production build/installability/authenticated offline cache/persistence/controlled update、生产构建与 `git diff --check` 全绿。2026-09-01 产品负责人继续指出普通单行记录正文与手绘横线之间仍有大块空带；移动端普通时间/分类记录最小行高由 `72px` 收至 `56px`，390px 实测正文到横线间距由 `28.1px` 收至 `13px`，长记录自动增高至 `134.7px` 且不截断，整行按钮仍可命中并保持零横向溢出。新增几何断言先在旧样式准确失败，修正后聚焦回归 `1/1`，同屏对照见 `output/playwright/ln-076-row-gap-comparison.png`；最终 `npm run check` 退出 0：设计规范 `11/11`、单测 `223/223`、浏览器 `34/34`、PWA production build/installability/authenticated offline cache/persistence/controlled update、生产构建与 `git diff --check` 全绿。未改记录、计划、账号、同步、备份、审查写入规则或快速记录步骤。 |
| LN-074 Rework 16 | P1 | 在领域复盘中增加二次确认的最近 7 天 AI 周总结 | P01-T00 主工作区 | `/insights` 折线图后提供一个次级文字动作；第一次点击只展示当前领域、最近 7 个本地自然日、普通/周期总数、截断提示、远程正文发送与会话期结果边界，只有再次确认才调用独立 `/api/organize/domain-review`。请求仅含窗口、领域名、语言及最多 80 条当前账号当前领域的 `id/date/time/content/sourceType`，每条最多 4000 字；不含账号、标签、附件、图片、字段对象、模板、分类树、其他领域或整份文档。响应只允许不超过 3 句总览和最多 3 个无来源索引主题；无记录不请求，样本少仍可总结并标记“样本有限”，停止/离页/切领域/切账号取消并清空。离线、无配置、超时、非法响应仅显示不可用；投资领域保留非建议边界，任何买卖、标的、价格、仓位、配置、收益或预测输出使整份结果失效。结果只存在页面会话，不写记录、缓存、Supabase 或备份。 | 实现已 Returned，规格包为 `specs/008-domain-weekly-summary/`。当前领域最近 7 个本地自然日选择器同时纳入普通/周期记录，跳过非法日期/时间，按最新 80 条及每条 4000 字截断；浏览器 provider 与独立 API 只发送白名单字段，复用同源、Bearer、Supabase、256 KiB、限流、20 秒服务端硬超时、25 秒客户端超时、`private/no-store` 与 Abort。UI 完成“披露→开始/取消→停止→会话期结果/不可用”闭环，切领域、账号、语言或离页均取消并清空；模型伪造/重复 ID、超长/重复主题、诊断/因果/评分/建议与投资动作或预测整份拒绝。聚焦模型/路由/分析 27/27、AI Playwright 1/1、本地折线 Playwright 1/1；最终 `npm run check` 退出 0：设计规范 11/11、单测 202/202、移动浏览器 31/31、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 全绿。证据位于 `output/playwright/ln-074-weekly-summary-final/`，`design-qa.md` 为 `passed`；独立安全、UI/无障碍与规格审查无 blocker。真实模型中文质量、隐私信任、主观延迟不超过 8 秒及 14 天内至少一次复用仍待人工观察，因此不标 Accepted。未提交、未推送、未部署。 |
| LN-074 Rework 17 | P1 | 让 Plan Agent 在空计划与仅 Google 日程时也常驻 | P01-T00 主工作区 | Plan 日期无论是否有可编辑本地计划都显示 Agent；无本地计划时只展示非交互角色与一行 `12px` 弱提示“编写计划后和我聊聊吧”，不得出现唤醒按钮、发起 Agent 请求或改写计划；仅 Google 日程保持只读并采用同一被动态。有本地计划时既有“检查这一天的计划”、扫描、对话与显式更新流程不变。320px 下提示保持一行，角色与文字不得遮挡新增按钮、右轨或计划块。 | 实现已 Returned，规格包沿用 `specs/004-agent-plan-review/`。新增被动展示分支与中英文文案，唤醒按钮只在存在可编辑本地计划时渲染；空计划与仅 Google 日程实测零 `/api/organize/agent` 请求、零计划写入，320px 无横向溢出、提示不换行且与新增按钮/角色无重叠。测试先以旧实现 0/1 准确失败，修正布局后聚焦 Plan Agent E2E 1/1；证据位于 `output/playwright/ln-074-plan-agent-persistent/`。最终 `npm run check` 退出 0：设计规范 11/11、单测 202/202、移动浏览器 31/31、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 全绿；Spec Kit analyze 无 Critical/High 覆盖缺口。等待产品负责人查看 320/390px 被动态视觉，不标 Accepted。未提交、未推送、未部署。 |
| LN-074 Rework 18 | P1 | 建立分类与反问收敛的 Diary Agent 分析流程 | P01-T00 主工作区 | 每条普通记录的初始分析只能进入“明确已有分类 / 为区分 2–3 个候选分类而追问 / 为补足一个有检索价值的事实而追问 / 无需处理”之一；回答后只能得到继续追问、补充正文建议、一个已有分类建议或保持原文，且最多接受两次用户回答。候选分类必须来自当前账号既有且非当前分类；非法、冲突、同时提出正文与分类或第二次仍不确定的结果安全收敛为保持原文。回答和展示均不写入；补充、新记录与应用分类继续显式确认，分类沿用撤销。会话字段不持久化，不引入通用 Agent 框架、工具调用、学习偏好或新 schema；离线降级遵守同一流程。模型/路由/页面回归与 `npm run check` 必须通过。 | 实现已 Returned，规格包沿用 `specs/003-agent-diary-review/`。初始分析、远端 schema、本地降级与回答归一化已统一为有界状态机：分类追问只携带当前账号 2–3 个既有候选分类，详情追问只收一个可检索事实；回答严格收敛为 `ask / append / category / none` 之一，冲突或非法输出归零，第二次仍不明确时结束且不写入。页面只把终态提案接回既有显式“补充正文 / 新建记录 / 应用分类 / 撤销分类”动作；切日期、工具、Plan、账号、离页或重新开始会取消会话，不新增持久化字段、通用工作流框架或工具调用。测试先以旧实现 6 个失败锁定缺口，随后模型/路由聚焦 19/19、Diary Agent 聚焦浏览器 1/1、设计规范 11/11 与 390px 分类追问/收敛/无变化视觉复核通过；Spec Kit analyze 为 0 Critical、0 High。最终 `npm run check` 退出 0：单测 220/220、移动浏览器 33/33、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 全绿。真实 DeepSeek 中文追问质量、分类区分度与实际使用效用仍待产品负责人在真实记录上人工观察，因此不标 Accepted。未提交、未推送、未部署。 |
| LN-074 Rework 19 | P1 | 直接用 Mastra 承载 Diary Agent 分类与反问工作流 | P01-T00 主工作区 | 保持 Rework 18 的请求、响应、页面、显式确认、本地降级和零持久化行为不变；服务端直接嵌入一个无工具、无记忆的 Mastra Agent 与一个“生成 → 项目归一化”Workflow，每次 analyze/reply 最多一次模型请求。Log Note 继续拥有鉴权、输入裁剪、分类 allowlist、两次回答上限、互斥终态、错误映射和写入权限；Plan Agent 不迁移。不建设或部署自研 Agent Runtime、Mastra 独立服务、工具注册表、持久化工作流、Dify/LN-077 或新 schema。Node.js `>=22.13.0`、聚焦运行时/路由测试、production build 与 `npm run check` 必须通过。 | Node 22 实现已 Returned，但存在合入/内部部署前置决策，规格包沿用 `specs/003-agent-diary-review/`。最终依赖精确锁定 `@mastra/core@1.63.2`；曾验证的 Node 20 兼容旧版 `0.24.9` 已完全移除，因为它是 pre-1 且安装图比 1.63.2 多约 246 个包，不作为长期基线。`src/mastra/` 只注册无工具、无 Agent 记忆、未配置应用持久化、无日志的 Diary Agent 和瞬态“结构化生成 → 项目归一化”Workflow，快照明确不持久化。现有路由仍独占鉴权、输入裁剪、提示词、分类白名单、两次回答上限、互斥终态、错误码与写权限；只把 Diary analyze/reply 委托给 Mastra，Plan 保持原 AI SDK 直连。Node 22 下运行时 4/4、路由 10/10、完整单测 233/233、设计规范 11/11、production build、PWA production/installability/authenticated-offline/persistence/update 与 `git diff --check` 通过；构建保留一条来自 Mastra 内部动态加载的 webpack 警告但产物成功。完整 `npm run check` 已实际执行，移动 E2E 为 32/34：公共页预览静态旋转矩阵和 320px Diary Agent 控件命中层两项断言失败，均属于本轮未触及的既有脏 UI/CSS 工作树，因此未越界修复，T118 保持开放。兼容审计又确认公开腾讯链路已是 Node 22，但 Plus/Cargo/CatPaw 仍硬锁 Node 20；精确版本在本机 Node 20.20.2 的 14 条聚焦测试与 production build 虽通过，Mastra 上游仍声明 `>=22.13.0`，不能据此授权内部部署。T120 保持开放，需产品负责人选择升级并独立验收内部运行时，或让本改动继续隔离于内部发行。npm 官方审计另报 Mastra 的 AI SDK v5 兼容别名 1 个 Low `GHSA-866g-f22w-33x8`；当前实际模型 provider 使用 provider-utils 4，且鉴权、限流、20 秒超时、输出 token 与单次调用约束仍在，但 audit 不是全绿。没有可直接替换的 3.x 修复版，本轮不强行覆盖框架内部主版本，T121 保持开放，部署前须升级上游图或显式接受该 Low 风险；本项不标 Accepted。回滚为删除 `src/mastra/` 与 `@mastra/core`/Node engine 变更，并把 Diary 恢复到原直接 `generateText` 分支；真实 DeepSeek 中文分类/追问质量与延迟仍待人工观察。未提交、未推送、未部署。 |

| LN-074 Rework 20 | P1 | 将全部现有远程 AI 能力统一迁移到 Mastra | P01-T00 主工作区 | Diary/Plan Agent、单日时间梳理、既有分类归档与领域 7 天总结继续保留现有页面、HTTP 路径、请求/响应 schema、错误码、显式确认、会话期结果和本地降级；五类服务端模型执行全部通过内嵌的无工具、无 Agent 记忆、未配置应用级或持久化存储、无持久快照 Mastra Agent/Workflow，每次请求最多一次模型调用且不重试。Log Note 继续独占鉴权、输入裁剪、业务 schema/allowlist、归一化、安全拒绝、确认、写入与撤销。删除项目自有的直接 AI SDK `generateText` 与手写 DeepSeek fetch 执行路径，以及不再需要的直接依赖；不得新增独立 Runtime、工具、记忆、持久化、页面能力、schema 或 LN-077 范围。Node 22 聚焦回归、全部单测、production build、PWA、依赖审计、`npm run check` 与 `git diff --check` 必须完成。 | 实现已完成但状态为 Waiting，规格包为 `specs/012-mastra-ai-consolidation/`。五个固定 capability ID 现由一套通用 Mastra Agent/Workflow 执行；每次运行一次结构化模型调用、零自动重试、无工具、无 Agent 记忆、未配置应用级/持久化存储、无 Workflow snapshot，Mastra 进程内默认 store 不承载业务状态。四个原 HTTP 路径、浏览器 Provider、本地/离线降级、公开错误码、业务 normalizer、显式确认、写入和撤销未改；通用 Provider adapter 在解析前限制 DeepSeek 响应为 512 KiB，并继续使用 20 秒 Abort。已删除 Diary 专用 Mastra 文件、项目直接 `ai` 依赖/导入、`generateText`/`Output.object` 与手写 `/chat/completions` 执行；`ai@6.0.257` 仅作为 `@mastra/core → chat` 的传递依赖保留。Node 22 聚焦回归 39/39、全部单测 238/238、设计规范 11/11、PWA production/installability/authenticated-offline/persistence/update、production build 与 `git diff --check` 通过；完整 `npm run check` 在移动 E2E 为 33/34 后退出 1，唯一失败是本轮未触及的公共 OAuth 页面 reduced-motion 断言：预览纸张仍为约 -1° 的静态旋转矩阵，而测试要求 `transform: none`，单项复现同样为 0/1。构建仍有一条来自 Mastra 包内动态依赖的 webpack warning，但产物成功。npm 官方审计返回 2 个 Low，均指向同一 `GHSA-866g-f22w-33x8` 与 `@ai-sdk/provider-utils-v5`，本轮未强制覆盖框架内部版本。回滚只需恢复四个路由的原执行 adapter 与直接 `ai` 依赖，无数据迁移或清理。内部 Plus/Cargo/CatPaw 仍为 Node 20，真实 DeepSeek 中文质量/延迟与 2 个 Low 依赖风险仍是合入/部署前开放证据；另需用户决定是否授权独立修复范围外公共页 reduced-motion 门禁，T028 保持开放，因此不标 Returned/Accepted。未提交、未推送、未部署。 |

## 验证中

| ID | 优先级 | 任务 | 已有证据 | 待独立核对 |
| --- | --- | --- | --- | --- |
| LN-079 | P1 | 基于 Mastra 的当前领域今日总结 | 实现已 Returned。`/insights` 现在在 30 天折线与独立 7 天总结之间显示当前领域设备本地今日总数及普通/周期拆分；首次激活只披露范围，独立确认才向 `/api/organize/domain-daily-summary` 发起一次请求。浏览器只发送 `domainName/date/locale/entries[{id,date,time,content,sourceType}]`，稳定保留最新 80 条且每条最多 4,000 Unicode 字符；账号、计划、标签、附件、字段、模板/分类结构、其他领域/日期和完整文档均不发送。结果只在页面会话中渲染 1–3 句事实总览和最多 3 个主题，不显示来源 ID、摘录、聊天或写入动作；Stop、失败后重试、切领域和离页均阻断迟到结果，空记录零请求，投资类不安全结果整份拒绝。收尾审计修复了 E2E 过滤名不一致、来源变化清理晚于布局提交，以及 Mastra 包装后业务校验错误被误映射为通用不可用的问题，并补齐任务/提醒类输出拒绝。聚焦模型/Provider/路由/结构/政策/Studio `22/22`、过滤浏览器 `1/1`；完整 `npm run check` 退出 0：设计规范 `11/11`、Node `268/268`、浏览器 `36/36`、PWA production build/installability/authenticated offline cache/persistence/controlled update 与 `git diff --check` 全绿。浏览器自动化覆盖 320/390/426/768/1280px、44px 目标、焦点、触控、80 条上限/省略、重复确认去重、Stop/晚到响应、invalid/unsafe/rate-limit/unconfigured/timeout/offline、领域切换、投资边界、离页、空状态、周总结隔离及 localStorage 字节不变；合成截图为 `output/playwright/ln-079-domain-daily-summary-{390,1280}.png`，已检查为开放纸面层级且无横向溢出。按负责人要求新增 localhost-only Studio 调试入口：Agents 列表显示 `domain-daily-summary Agent`，Workflows 列表和图形页显示复用生产 schema、prompt、normalizer 的两步 `domain-daily-summary-workflow`；仅接收合成输入，不读取账号、Supabase、本地存储，不启用 tools、memory、storage、snapshot 或写入。仅新增开发期 `mastra@1.27.2` CLI、`.nvmrc` 与忽略的 `.mastra/` 输出，不改变产品 Runtime、存储、备份或 Service Worker；范围外 Plan Agent、首页和 Google Calendar 工作树保持原状。构建仍有既有 Mastra dynamic-dependency warning，但成功产出。实现提交 `a9d34f3` 已推送至 `sankuai/feature/req-20260903-01-domain-daily-summary-studio`；未部署。 | 使用真实、非敏感 Provider 完成至少 3 次成功请求，记录确认到可见结果时延且中位数不超过 8 秒；产品负责人在真实 390px/桌面页面确认额外今日区不拥挤、结果可与来源记录核对；14 天内至少主动使用 2 次并完成采用/移除判断。另需在目标部署环境核对 Provider 配置、数据处理、日志脱敏与真实账号边界。上述证据完成前保持“验证中”，不标记 Accepted。 |
| LN-010 Phase 1 | P1 | 建立本地领域趋势与轻量复盘页 | 本次二次返工已 Returned，等待产品负责人重新视觉确认：旧实现只对齐活动领域内部文字与复盘图标，普通领域仍回到书脊轴，导致目录随“有无复盘按钮”左右跳动。新增响应式回归先在 320px 准确失败：顶部工具中心 `x=292px`、活动领域中心 `x=298px`、普通领域真实目标中心 `x=264px`、普通文字中心 `x=280px`。最终移动端所有领域真实目标、可见文字、顶部工具、两枚拨杆和复盘入口统一到距右边 `28px` 的操作轴；390px 为 `x=362px`。领域小节点独立保留距右边 `56px` 的书脊轴；390px 为 `x=334px`。所有目录目标均为 `44×44px`；Agent 目标左移后右缘 `x=331px`，目录目标左缘 `x=340px`，保留 `9px` 真实目标净空和焦点后的至少 `4px` 可见间隔，`80×80px` 角色图与书脊抓握点不变。390px 证据位于 `output/playwright/rail-axis-final/ln-075-unified-rail-390.png`；首页右轨、Domain Insights 与 viewport-spine Agent 聚焦回归分别 `1/1`、`2/2`、`1/1`，同一自动化覆盖 320/426/600/671/700/768/1280px。最终 `npm run check` 退出 0：设计规范 `11/11`、单测 `206/206`、移动浏览器 `31/31`、PWA production build/installability/authenticated offline cache/persistence/controlled update、生产构建和 `git diff --check` 全绿。其余 `/insights`、数据、离线、账号、备份和快速记录行为不变。 | 产品负责人在当前 390px 页面独立确认所有目录文字在有无复盘按钮时都与顶部工具同轴、Agent 与右列分离且间距自然；再核对紧凑标签、折线选日详情、空状态与投资边界。随后真实使用 14 天内至少主动打开两次。人工确认和观察期完成前保持“验证中”，不标记 Accepted；Phase 2 继续受 LN-007/008/009 闸门约束。 |
| LN-074 Rework 15 | P1 | 修复 Agent 应用同分类后无反馈且队列卡死 | 根因是 `applyAgentCategory` 对“建议分类已等于记录当前分类”直接静默 `return`，不写入也不进入下一条。现在 Agent 结果进入 UI 前会再与最新记录/分类对账，丢弃未知记录、重复项、非法分类和已是当前分类的无效建议；若展示后才因状态变化成为同分类，点击会提示“已经属于该分类”、不产生写入并自动继续下一条。新增失败回归后 `npm test` 171/171、Agent 模型/路由定向 15/15、设计规范 11/11、PWA production build/installability/authenticated offline/persistence/update 与 `git diff --check` 通过。隔离合成数据浏览器旅程实际通过“应用分类 → 写入 → 下一条 → 撤销”断言；临时跳过的既有人物遮挡断言已完整恢复。完整 E2E 当前 25/27，剩余两项是当前未完成 UI 工作树的独立回归：右轨目录与工具亲密度，以及 reviewing 人物在 320/390px 与批注/右轨控件重叠。未提交、未推送、未部署。 | 产品负责人在原日期重新唤醒 Agent，确认不再出现“当前分类→应用分类无反馈”，并在两项既有 UI 回归收口后重跑完整 27/27 门禁；通过前 T100 与 LN-074 保持“验证中”，不标记 Accepted。 |
| LN-076 Rework 5 | P1 | 重排首页日期、右轨导航与常驻 Agent 的顶部关系 | 当前日期已恢复为左侧最大单一月历触发器，星期随其同行，右轨独立月历入口和底部日记/计划重复入口已删除；移动 Diary 右上工具按“搜索 / 时间-分类 / 日记-计划 / 设置”排列，Plan 为“搜索 / 日记-计划 / 设置”，两个切换均为单按钮、单击完成且命中区不少于 `44px`。Agent 零高度挂载在完整日期上下文之后，月历开合时随文档自然下移；空闲/完成内部竖线与唯一书脊轴光学重合，扫描/审查不移动真实按钮，四态常驻与交互目标保持稳定。普通记录到固定记录仅保留上一行所属的一道横线。320/390/426/768/1280px、月历展开/收起、Escape/焦点返回、日期切换、快速记录、Diary/Plan/Time/Category 切换与 Agent 四态均已自动化覆盖。`design-qa.md` Rework 5 最终 `passed`；同屏证据 `output/ln-076-date-rail-agent/comparison-390.png`，实现截图 `output/playwright/ln-076-date-rail-agent-{collapsed,expanded}-viewport-390.png`。最终 `npm run check` 退出 0：设计规范 11/11、单测 170/170、移动浏览器 27/27、PWA production build/installability/authenticated offline cache/persistence/controlled update、生产构建和 `git diff --check` 全绿。未改 Agent 行为、记录/计划/视图数据、月历内核、账号、离线、同步、备份或导出；未提交、未推送、未部署。 | 产品负责人刷新 390px 当前页，独立确认展开月历不再与 Agent 重叠、空闲/扫描/审查/完成均只有一根书脊轴、普通记录与固定记录交界只有一道横线，以及右轨四项排列和两种切换手感。通过前保持“验证中”，不标记 Accepted。 |
| LN-076 Rework 4 | P1 | 收敛分类视图的章节标题与重复横线 | 分类视图现在以独立 `h2 / h3` 组成紧凑的“领域 / 首分类 进度”编辑式章节线，后续分类继续显示明确次级标题；相邻领域删除下一领域顶部横线，只保留上一领域末行的手绘分隔和区块留白。旧 DOM 在新增回归中准确失败于缺少章节线；修正后新增章节回归与既有层级回归各 1/1，通过 320/390/426/768/1280px 无溢出、44px 输入、安全换行、后续分类和单分隔断言。证据：`output/ln-076-category-chapters/ln-076-category-chapter-{320,390,426,768,1280}.png`，用户源图与 390px 实现同屏对照为 `ln-076-category-chapter-comparison-390.png`，`design-qa.md` 最终 `passed`。完整 `npm run check` 退出 0：设计规范 11/11、单测 170/170、浏览器 26/26、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改名称、层级数据、填写回调、Agent、时间视图、账号、同步、备份或导出；未提交、未推送、未部署。 | 产品负责人刷新当前分类视图，确认“健康 / 身体指标”现在像一个紧凑章节、“睡眠”与“学习”之间只剩一道有归属的横线，并核对长分类名真实数据下的换行手感。通过前保持“验证中”，不标记 Accepted。 |
| LN-075 Rework 11 | P1 | 提升设置下载区文件动作的图标与说明辨识度 | 产品负责人基于 390px 截图指出下载区“都是这种 icon 看不懂，说明也没辨识度”。本轮按 `design-review` / `frontend-skill` 的 App UI 规则审查后，将问题定义为“所有动作都产出文件，因此通用下载箭头没有信息增量”。修正为六个文件角色：`MD` 今日可读 Markdown、`ALL` 全部记录 Markdown、`FULL` 可迁移完整备份、`JSON` 文字回滚备份、`MAP` 当前结构、`NEW` 起步空结构；左侧换成不同线性图标，右侧显示文件角色短标记，避免再次依赖一串同质灰色下载箭头。辅助说明同步改为先说用途再说边界：阅读、迁移、回滚、复用设置和新账号起步各自承担不同含义。`DESIGN.md` 新增 Settings 下载动作识别规则；E2E 新增断言六个下载动作必须暴露不同 `data-file-kind`。证据：`output/ln-075-settings-file-roles/ln-065-download-structure-zh-390.png`；定向 Settings E2E 1/1 通过；最终 `npm run check` 退出 0：设计规范 11/11、单测 158/158、移动浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改导出内容、恢复逻辑、账号、同步、备份格式或快速记录步骤；未提交、未推送、未部署。 | 产品负责人刷新首页内 Settings → 下载，确认 MD/ALL/FULL/JSON/MAP/NEW 与文案是否能在不打开文件的情况下区分“读记录、迁移、回滚、复用结构、起步模板”。通过前保持“验证中”，不标记 Accepted。 |
| LN-075 Rework 10 | P1 | 按 Sigo 信息密度原则压缩全设置页文案 | 产品负责人指出设置页说明重复、信息增量低，本轮使用 SkillHub 拉取并阅读 `sigo`，按“收缩检验、信息密度、标题不做标签复读、只保留会改变决策的安全边界”重写 Settings 全部六个面板：`常规 / 账号 / 下载 / 恢复 / 图片 / 记录结构` 只作为短导航标识，详情标题改为 `语言与入口 / 登录与同步 / 保存文件 / 导入备份 / 图片占用 / 编辑记录结构`，移动详情标题同步避免与索引重复；账号、Google Calendar、导出、备份、恢复、图片、日记 Markdown 导入和记录结构说明均压缩为状态或边界，不再解释显而易见的控件。`DESIGN.md` 补充 Settings 文案规则；E2E 新增断言：设置面板 marker 与标题不得相同，并覆盖图片和记录结构详情标题。证据目录：`output/ln-064-sigo-all-settings/` 与最新 `output/playwright/ln-064-settings-mobile-index-390.png`、`ln-064-settings-general-copy-zh-390.png`、`ln-065-download-structure-zh-390.png`、`ln-064-settings-backup-390.png`。最终 `npm run check` 在非沙箱环境退出 0：设计规范 11/11、单测 158/158、移动浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改记录、账号、同步、备份格式、恢复行为或快速记录步骤；未提交、未推送、未部署。 | 产品负责人直接刷新 `/settings` 或首页内 Settings，逐项查看六个设置面板，确认短标题、状态行和安全提示是否足够清楚且不再重复；若真实使用中发现某条压缩后影响风险判断，再只补该处边界说明。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 14 | P1 | 基于截图系统化收敛 Agent 批注 UI 层级 | 按用户提供的 390px 截图和 `design-review` / `frontend-skill` 方法重新审查 Diary Agent：将问题归纳为正文、Agent 批注、右侧装订轨和固定记录四套系统抢层级，而非单点样式。新增设计说明 `docs/2026-08-28-LN-074-Agent批注UI系统优化方案.md`；因 `citadel` 读取 `https://km.sankuai.com/collabpage/2782913331` 当前 SSO/CIBA 返回 `fetch failed`，本文不冒充引用学城原文，只记录认证受限。移动 Diary Agent 的元信息改成同一行 metadata：左侧 `Agent`、右侧进度并为关闭按钮让位；问题独占阅读行并保留关闭避让；分类标签继续只显示一次但降低蓝色权重；回复宽度、紧贴动作、44px 目标、右轨位置、Plan Agent、数据、账号、离线、同步、备份和快速记录均不变。证据：`output/ln-074-ui-opt/ln-074-agent-category-390.png`、`ln-074-agent-review-zh-390.png`、`ln-074-agent-question-390.png`、`ln-074-agent-category-320.png`、`results.json`。定向 Diary Agent 1/1；最终 `npm run check` 退出 0：设计规范 11/11、单测 158/158、移动浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未提交、未推送、未部署。 | 产品负责人直接查看中文 390px 截图，确认 metadata 行、关闭入口、分类标签降噪和动作区域是否比截图更像一条附着在记录下方的页边批注；学城参考文档仍需在认证恢复后补读或同步。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 13 | P1 | 去除 Agent 分类提问与分类标签之间的重复信息 | 产品负责人指出分类问题文案已经包含“交易 / 市场”，下方结果标签又重复显示同一路径，导致信息层级混乱。本轮将分类问题统一为不带具体路径的中英文通用提问，具体 `领域 / 分类` 只保留为一次可扫描标签；本地降级、远端输出归一化、路由提示、单测和 Diary Agent 390/320px 回归均已更新。证据：`output/ln-074-agent-rework13/ln-074-agent-category-390.png`、`ln-074-agent-review-zh-390.png`；定向 Diary Agent 与 PWA 验证通过，单测 158/158、设计规范 11/11。未改行为、数据、账号、离线、同步、备份、右侧 icon 轨或 Plan Agent；未提交、未推送、未部署。 | 产品负责人确认中文/英文分类状态中问题与标签各承担单一职责，标签只出现一次且仍足够明确；真实模型措辞、延迟和 14 天启用/信任继续作为人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 12 | P1 | 恢复移动 Agent 回复可写宽度并增强关闭入口可发现性 | 接受产品负责人对 Rework 11“可交互不方便”的直接否定，不再用同排密度指标牺牲真实输入：原 CSS 为两个分类动作绝对预留 `136–140px`，390px 回复仅剩约 120px，中文占位被迫断成三行。现在 320–420px Diary Agent 的回复 textarea 恢复批注可用宽度，390px 自动化要求不少于 220px、320px 不少于 160px；一到两个未解决动作改为紧贴输入下方的单行右对齐文字目标，间距 `0–4px`，不重叠、不形成全宽按钮带，每项仍不少于 44px。右上关闭继续保留 44px 命中区，内部增加约 28px 的安静圆形图形面、提高对比度，并为问题文字预留 40px，避免文字与关闭重叠。正式证据：`output/ln-074-agent-rework12/ln-074-agent-question-390.png`、`ln-074-agent-category-390.png`、`ln-074-agent-category-320.png`、`ln-074-agent-question-resolved-390.png`、`ln-074-agent-review-zh-390.png`。Diary Agent 新几何 1/1、Plan Agent 1/1、首页右轨 1/1、日期选择器 1/1 聚焦回归通过；最终隔离 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改文字层级、右侧 Search/Calendar/Settings/Export icon 轨、Plan Agent、行为、数据、账号、离线、同步、备份或快速记录；未提交、未推送、未部署。 | 产品负责人直接查看 320/390px 中文分类状态，确认完整回复宽度、动作紧贴程度和关闭入口是否比 Rework 11 更易输入与发现；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 11 | P1 | 按社区布局原则消除 Agent 页剩余空带并建立紧凑阅读网格 | 接受产品负责人对 Rework 10 的再次标注，不再把“44px 可点击”误解为“必须显示一整条 44px 空行”。SkillHub 检索到 `aidesign-spec-context` 的页面骨架 / 模块排版 / 组件细节三层方法（未安装未认证社区 Skill）；同时参考 Nielsen Norman Group 邻近原则与 W3C WCAG 2.5.8 target-size/spacing 说明。390px Diary Agent 激活态现在把标题/摘要锚到既有安全头部下缘，摘要到“记录”标题为 16px；时间/正文阅读槽由 66px 级收为 `42px + 10px = 52px`；活动来源行保持 52px，其他普通单行记录与固定记录行统一为 56px；分类的“应用分类 / 保持原文”与回复输入共享同一 44px 行；固定记录工具头只占 28px 可见布局槽，但“调整”真实目标仍为 44px 且不覆盖首字段。聚焦回归曾发现全局 56px 固定行会让空日横滑起点落入输入控件，已将全部新密度规则严格限制到 Diary Agent 激活态。正式截图：`output/ln-074-agent-rework11/ln-074-agent-question-390.png`、`ln-074-agent-category-390.png`、`ln-074-agent-question-resolved-390.png`、`ln-074-agent-review-zh-390.png`。Diary Agent、Plan Agent、首页右轨、日期选择器聚焦回归均通过；最终隔离 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改字体层级、右侧 Search/Calendar/Settings/Export icon、Plan Agent、行为、数据、账号、离线、同步、备份或快速记录；未提交、未推送、未部署。 | 产品负责人直接对比 Rework 10 与 Rework 11 中文 390px 证据，确认顶部摘要、时间左槽、分类回复/动作、普通记录行和固定记录工具簇的空间利用率是否达到预期；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 10 | P1 | 删除移动 Agent 页面无意义留白并提升空间利用率 | 接受产品负责人“还有一些稀里糊涂的空隙，空间利用率太低”的反馈，不再继续缩字，而是清理结构占位：390px 激活态日期上下文到“记录”区由 24px 收为 12px；单行活动来源记录从通用 72px 收为 52px，并同步收紧时间/正文内边距；未回答问题的单个“保持原文”与回复输入共享同一行，不再另占 44px 内容行；三项解决动作由最多两排改为一排三列，英文可在各自目标内换行；普通记录到固定记录区移除叠加的 32px margin + 32px padding，改为单一 12px 过渡。所有动作仍不少于 44px，textarea 仍为 16px，右侧 Search/Calendar/Settings/Export icon、文字层级、记录正文轴、Plan Agent、行为、数据、账号、离线、同步、备份与快速记录均不变。完整门禁曾准确发现顶栏压缩会影响模拟刘海与 320/390px 月历避让，已恢复必要的 116px 顶栏保护，只保留内容流密度收益。视觉证据：`output/ln-074-agent-rework10/ln-074-agent-question-390.png`、`ln-074-agent-category-390.png`、`ln-074-agent-question-resolved-390.png`、`ln-074-agent-review-zh-390.png`。定向 Diary、Plan、首页右轨、日期选择器均通过；最终隔离 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未提交、未推送、未部署。 | 产品负责人直接对比 Rework 9 与 Rework 10 的中文 390px 证据，确认顶部、来源到批注、回复到动作、动作到下一记录、普通记录到固定记录是否已形成连续而紧凑的阅读流；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 9 | P1 | 按真实页面审查重构 Agent 批注层级，而非继续做无感微调 | 接受产品负责人“你自己检查一下 UI 对吗、去找优秀设计学习”的否定，重新以成熟行内评论、页边批注和安静笔记产品的通用原则审视 390px 页面：来源必须明确、辅助文字不得成为第二正文、动作不能形成第二个大控件、工具 icon 继续留在右轨。移动 Diary Agent 因此撤销 `14px Serif/宋体` 和全宽分段组：记录正文保持 `16px` 全墨色 Sans；问题回到同一 Sans 阅读系统但降为 `13px` 支撑墨色；新增 `10px` Mono `Agent` 角色标签与 `11px` 弱进度；分类/动作/placeholder 为 `12px`，真实 textarea 仍为 `16px`。左侧仅保留 `2px × 24px` 短来源标记，问题/分类/回复文字与真实 `.entry-content` 轴误差不超过 `1px`；关闭 icon 移到右上，进度在其左侧；分类改为紧凑弱底标签；动作取消外框、圆角和分隔线，变成靠右的 `44px` 文字目标，主动作只用蓝色文字。顶部“发现 N 处”降为 `12px` 弱色加 5px 蓝点。右侧 Search/Calendar/Settings/Export 图标位置、Plan Agent、行为、数据、账号、离线、同步、备份与快速记录均不变。视觉证据：`output/ln-074-agent-rework9/ln-074-agent-review-zh-390.png`、`ln-074-agent-question-390.png`、`ln-074-agent-category-390.png`、`ln-074-agent-question-resolved-390.png`。定向 Diary/Plan Agent 各 1/1；完整门禁在独立临时输出目录运行并退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未提交、未推送、未部署。 | 产品负责人直接对比 Rework 8 与 Rework 9 中文 390px 证据，确认正文是否稳定成为主视觉、Agent 是否明显附着而不抢占、右侧关闭与无框动作是否符合预期；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 8 | P1 | 将 Agent 从“缩小的正文”改成肉眼可辨的页边批注角色 | 接受产品负责人对 Rework 7“都看不出区别”的否定，不再依赖 1–2px 数值差：记录正文继续 `16px` 全墨色 Instrument Sans；Diary Agent 问题改为 `14px` muted Instrument Serif / 宋体回退，分类保持 `13px/600` Sans，动作与 placeholder 降为 `12px`，真实 textarea 仍为 `16px`，所有动作仍不少于 `44px`。批注真实正文轴和右缘不动，但左侧关闭/角标/回复短线沟槽整体向正文靠近 `10px`，顶部预留由 `24px` 减为 `12px`，形成可见的“正文主信息 / Agent 附属批注”角色差。新增 source/prompt 字体族差异断言；收紧沟槽后完整门禁曾准确发现关闭按钮被会话层拦截，补充明确 z-index 后失败场景复测通过。视觉证据：`output/playwright/ln-074-agent-review-zh-390.png`、`ln-074-agent-question-390.png`、`ln-074-agent-category-390.png`；Diary/Plan Agent 聚焦回归 2/2。最终 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改右侧图标位置、Plan Agent、Agent 行为、数据、账号、离线、同步、备份或快速记录；未提交、未推送、未部署。 | 产品负责人直接复核最新中文 390px 截图：Serif/Sans 角色差是否足够明确、灰色问题是否仍清晰可读、沟槽靠近后是否自然；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 7 | P1 | 按最新 390px 标注强化记录/批注层级并压缩留白 | 将对齐基准从带 padding 的 `.entry-body` 改为用户真实看到的 `.entry-content`：新增断言先准确拦截旧样式 `promptOffset=-22px`，修正后问题、分类、回复文字与操作组均与来源记录正文轴误差不超过 `1px`，批注右缘及右侧 Search/Calendar/Settings/Export 图标带不动。记录正文保持 `16px` 全墨色，Diary Agent 降为问题 `14px` 支撑墨色、分类/动作 `13px`、placeholder `12px`，真实 textarea 仍为 `16px`；分类到回复保持 `4–8px`，回复到操作为 `4px`，操作到下一条记录为 `12–16px`，按钮继续不少于 `44px`。视觉证据：`output/playwright/ln-074-agent-review-zh-390.png`、`ln-074-agent-question-390.png`、`ln-074-agent-category-390.png`；Diary/Plan Agent 聚焦回归 2/2。最终 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。仅修改 Diary 移动 CSS、视觉断言与规范；未改 Plan Agent、Agent 行为、数据、账号、离线、同步、备份、右轨或快速记录；未提交、未推送、未部署。 | 在真实 390px 设备和真实中文记录密度下独立确认记录正文是否稳定成为主视觉、14/13/12px 批注层级是否清晰、压缩后的 4px/12–16px 节奏是否自然；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 6 | P1 | 按最新 390px 标注统一 Agent 批注内部对齐轴 | 将截图中的“看似同组、实际多条左轴”改成可测量契约：问题、分类结果、回复文字与分段操作组左缘共享正文阅读轴；关闭符号、短角标与回复蓝色短笔触共享批注沟槽中心；操作组右缘与会话右缘一致，问题到分类固定为 `4px`。旧样式被新增断言准确拦截：问题/分类/操作左缘为 `62px`，回复文字为 `82px`（右偏 `20px`）；沟槽中心 `40px`，关闭中心 `54px`、回复笔触中心 `63px`；问题到分类为 `-1px`。修正后上述轴线误差均不超过 `1px`，所有按钮仍不少于 `44px`；右侧搜索/月历/设置/导出图标位置、记录正文轴和 Plan Agent 样式不变。视觉证据：`output/playwright/ln-074-agent-review-zh-390.png`；Diary/Plan Agent 聚焦回归 2/2。最终 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改 Agent 行为、数据、账号、离线、同步、备份或快速记录；未提交、未推送、未部署。 | 在真实 390px 设备和真实中文记录密度下独立确认两条竖轴是否形成稳定阅读节奏、4px 配对间距是否自然、宽按钮组是否仍足够克制；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 5 | P1 | 按最新 390px 标注修正 Agent 批注文字层级 | 保持 Rework 4 的右侧图标带不动：搜索、月历、设置与导出继续位于装订线右侧约 `28px`，目录节点、“日记 / 计划”和记录印章仍保持原轴。移动 Agent 批注改为明确的 `问题 15px > 分类结果 14px > 回复提示/操作 13px` 三级阅读顺序；回复 placeholder 使用 `--faint`，真实 textarea 继续 `16px` 以避免 iOS 聚焦缩放；主动作浅蓝底由 8% 降为 4%，文字饱和度降低，所有动作仍保持至少 `44px`。新增计算样式断言，旧样式以 `prompt 15 / placeholder 16` 被准确拦截；修正后 Diary/Plan Agent 聚焦回归 2/2。视觉证据：`output/playwright/ln-074-agent-review-zh-390.png`。最终 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改 Agent 行为、数据、账号、离线、同步、备份或快速记录；未提交、未推送、未部署。 | 在真实 390px 设备和真实中文记录密度下独立确认问题正文是否成为唯一主视觉、分类结果是否保持可识别、回复提示与操作是否足够克制；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 4 | P1 | 按最新 390px 标注收拢移动端右轨与 Agent 行内批注 | 搜索、月历、设置已替换为手绘 icon-only 控件，可见图标与 `44px` 触控中心位于装订线右侧约 `28px`，不再覆盖竖线；导出使用同一右侧操作带内裁切后的下载 glyph，不再显示文字或双圈印章。目录节点、“日记 / 计划”和记录印章保持原有装订线轴关系。Diary 活跃审查不再渲染移动小人，也不在源记录绘制蓝色虚线/长下划线；三边高括号收敛为 `16px` 局部角标。分类动作横向合并为一个 1px/6px 分段组；三项解决动作最多两行，组内弱分隔、单项无独立圆角或大面积蓝底，触控均不少于 `44px`。滚动揭示、`aria-current`、键盘焦点与 Plan Agent 样式保持隔离。截图：`output/playwright/ln-074-agent-review-zh-390.png`、`output/playwright/ln-074-agent-category-390.png`、`output/playwright/ln-074-agent-question-resolved-390.png`；定向 Diary Agent 1/1、首页右轨 1/1、日期头回归 1/1、Plan Agent 1/1。最终 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改快速记录步骤、数据、AI 边界、账号、离线、同步或备份；未提交、未推送、未部署。 | 在真实 390px 设备和真实中文记录密度下独立复核右侧图标带的辨识度与边缘余量、短角标与分段操作组是否足够克制；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 人工观察项。通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 3 | P1 | 按产品截图收口移动端日记页结构与 Agent 行内批注 | 关闭月历时标题上下文到“记录”区块由 73px 收敛为 24px；390px Agent prompt 与记录正文轴偏差 8px，批注右缘距轨道 20px，激活时闲置小人槽高度为 0；显式动作改为 6px 功能圆角、1px 边界、44px 高，主动作单一蓝色；搜索/月历/设置与导出改为共轴横读短标签，移动端隐藏双圈导出印章；紧凑首区块高于目录带时节点夹到上边缘，不再反向制造正文空白；小人槽建立独立层级但保留完成动作点击，Plan Agent 样式保持隔离。截图：`output/playwright/ln-074-agent-review-zh-390.png`、`output/playwright/ln-074-agent-category-390.png`；定向 Diary Agent 1/1、右轨 1/1、Plan Agent 1/1。最终 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改记录步骤、数据、AI 边界、账号、同步或备份，未提交、未推送、未部署。 | 在真实 390px 设备与真实中文记录密度下复核横排右轨是否仍足够克制、按钮主次是否符合预期；真实模型措辞、延迟和 14 天启用/信任仍沿用 LN-074 的人工观察项。 |
| LN-074 Rework 2 | P1 | 将首页行内日记 Agent 扩展到 Plan 审查 | Plan 仅在所选日存在可编辑本地计划时显示 Agent，空计划或仅 Google 事件时隐藏；唤醒后检查时间重叠和含糊标题，小人与紧凑页边批注锚定到单一本地计划块，不增加重复横线或遮挡右轨/新增按钮。支持追问与简单聊天；回复后可预览标题或时间 proposal，只有点击“更新计划”才复用现有 local-first/revision 保存路径写入，“保持原计划”和聊天零写入，标题/时间更新各自保留其他字段。Google 只传 `title/startMinute/endMinute` 作无身份冲突上下文并始终只读；无 Token/离线使用确定性本地降级。打开编辑器、切日期/Diary/工具/账号会取消会话。视觉证据：`output/ln-074-plan-agent-review/ln-074-plan-agent-{review-320,review-390,review-1280,proposal-390}.png`，`results.json` 专项 1/1；最终 `npm run check` 退出 0：设计规范 11/11、单测 157/157、浏览器 24/24、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。320/390/426/700/1280px 专项核对通过，触控目标不小于 44px，无横向溢出。未新增 schema/依赖，未提交、未推送、未部署。 | 真实账号下独立核对 DeepSeek 中文措辞、模型延迟与建议质量；在 14 天使用观察中评估启用率、复用率和用户是否信任显式更新，若遮挡 Plan 创建、锚定不稳定、产生不安全时间建议或未被复用，继续隔离或移除；通过前保持“验证中”，不标记 Accepted。 |
| LN-074 Rework 1 | P1 | 单日 AI 梳理改为首页行内日记 Agent | 已完成 hero 原地唤醒、selected-day 普通记录扫描、Time/Category 行锚定与移动角色、紧凑纸面批注式追问/闲聊、补到原记录/作为新记录/保持原文、现有分类应用与撤销；日期、月历、搜索、设置、计划会取消会话，远端边界只发送 allowlist 最小字段并支持离线/无 Token/非法输出本地降级；`/organize?date=...` 保持兼容。最新视觉方向先通过生图重新设计，再落为页边批注：`×` 独立放在左上外侧，进度独立在右上；三边蓝色短括号包住墨色问题、弱提示与回复，输入只保留短蓝色光标笔触，空输入不显示禁用发送，批注以留白结束且后续固定记录不重复画顶部线。移动端操作纵向排列，未回答时仅保留回复与保持原文，获得建议后才显示补充/新记录；移动角色停在来源记录上方，不覆盖输入。状态证据：`output/ln-074-agent-states-v7/ln-074-agent-{idle,scanning,question,question-resolved,category,complete}-390.png`；专项 Agent E2E 1/1、设计规范 11/11、单测 151/151 已通过，完整 `npm run check` 待本轮最终重跑。未新增 schema/持久聊天/分类/标签，未提交、未推送、未部署。 | 独立核对真实账号下首次远端 Agent 中文措辞、模型延迟与建议质量；确认 14 日观察期的启用率/信任度退出条件。 |
| LN-075 Rework 9 | P1 | 设置与搜索聚集到当前日记页的左侧工作区 | Search、Calendar、Settings 互斥并直接替换 `/` 左侧日记工作区；日记 DOM 保持挂载并 inert，右侧装订轨、目录、Diary/Plan、导出和记录按钮持续存在且不因工具打开改变几何位置；无全屏 backdrop/modal；移动端设置保留六项索引与详情返回，Escape/主动轨道按钮关闭并恢复日期、模式、滚动和焦点；直达 `/settings`、`#record-setup`、`#structure` 兼容。规格包：`specs/002-in-page-tools/`；最终 `npm run check` 退出 0：设计规范 11/11、144/144 单测、22/22 浏览器、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。 | 用户已确认本轮手绘横竖线视觉“效果不错”；真实中文记录页 390px/实际设备的工具工作区手感仍可继续观察。 |
| LN-075 Rework 8 | P1 | 右轨视觉层级与基础设计原则返工 | 搜索、月历、设置已统一为 44px 节点、4px 组内节奏和 14px Instrument Sans 下划线操作文字；内容目录改为同轴 44px 节点、16px Instrument Serif 无下划线文字，目录可读区与设置按钮之间保持至少 24px。闲置使用弱墨色，焦点/展开/当前位置只用蓝色强调；320/390/426/600/671/700 保持无重叠、裁切或横向溢出，701px 以上继续使用紧凑桌面头部。长目录溢出使用独立滚动并自动揭示当前节点；横向记录分隔与全高竖轨使用透明 PNG 铅笔笔触，保留可见粗细和压力变化。规格包：`specs/001-rail-hierarchy/`；视觉证据：`output/playwright/ln-075-unified-rail-{320,390,426,600,671,700,768,1280}.png`。最终 `npm run check` 退出 0：设计规范 11/11、144/144 单测、22/22 浏览器、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改记录步骤、数据、账号、同步或备份。 | 用户已确认手绘横线、竖线当前效果可替换进入页面；后续仅需在真实设备持续观察目录与正文主次。 |
| LN-068 | P1 | 智能整理收敛为单日日期流程 | `/organize` 默认今天，只预览/分析所选自然日的全部普通记录；切日会取消旧分析并清空旧建议。Rework 将原生 `type=date + 今天` 替换为与首页共用的“月日 + 星期 + 展开箭头”日记式触发器和同一开放式月格内核；首页独有横滑与日记/计划联动仍隔离，智能整理选中日期后自动收起并回焦触发器，Escape 也可收起。最新用户视觉复核后删除日期触发器上方重复的“选择日期 / Choose a day”；日期上下文与当天记录之间改用 24px 区块留白，只在“当天记录”与列表内容之间保留弱分隔线，不再用两条等权横线表达不同关系。今日/近 7 天/未分类、搜索、复选框、已选数量、全选/反选与整日批量手动打标签均已移除；建议原因/置信、逐条移除、忽略、按组/全部应用、重算和撤销保留。首页入口已由 LN-075 Rework 4 保持为当天存在普通记录时显示的整理小人；点击把当前日期带入 `/organize`，空记录与计划模式不显示，真实触控区不少于 44px。纯模型测试覆盖指定日期、跨日排除、周期排除、稳定排序与伪日期；浏览器覆盖整理小人的适用状态、日期传递、无重复日期标题、主区块无横线、列表标题保留细线、共享组件、默认今天、前一天切换、选日收起、Escape、六档无横向溢出、应用/撤销及原文/分类不变。视觉证据新增 `output/ln-068-organize-nav-final/ln-056-floating-switch-{320,390,600,671,768,1280}.png`；最新门禁通过设计规范 11/11、122/122 单测、22/22 浏览器、PWA production build/installability/已认证离线缓存/persistence/update、生产构建与 `git diff --check`。未新增网络 AI、后台分析、schema、数据库或首页主入口；未提交、未推送、未部署。 | 用户在实际记录数据上复核：默认日期、切换任意一天、当天记录是否完整、建议是否符合预期，以及移动端日期控件是否顺手。通过前保持“验证中”。 |
| LN-067 | P1 | Log Note 计划与 Google Calendar 主日历同步 | 设置账号页已接显式 Calendar events 授权；access token 仅在页面内存；本地计划按私有来源标记幂等创建/更新/删除；Google 原有分时与全天事件进入账号隔离缓存并只读展示；同步中再次修改会排队补同步，账号切换/断开不会把旧请求结果写入新账号；Google 缓存不进入主数据、Supabase、备份或 Service Worker。`output/ln-067-google-calendar-final` 通过 320/390/1280 视觉与 DOM 回归，设计规范 11/11、121/121 单测、22/22 浏览器、PWA production build/installability/已认证离线缓存/persistence/update、生产构建与 `git diff --check` 全绿。2026-08-31 按产品负责人截图在公开版 Plan 空态增加一行弱元信息“Google 日历同步 · 账号设置”，中英文均低于主空态层级；内部发行继续隐藏，不新增授权入口或同步行为。同日继续补齐设置页受限态：无 Calendar Client 与 OAuth `origin_mismatch` 归入可观测 issue，中文显示“当前域名不可用”及计划保留说明，按钮禁用且不再暴露环境变量或上游原始错误；拒绝授权、弹窗拦截、离线和 API 故障分别归因。最终 `npm run check` 退出 0：设计规范 11/11、单测 203/203、浏览器 31/31、PWA production build/installability/authenticated offline cache/persistence/controlled update、生产构建与 `git diff --check` 全绿；无 Calendar 配置聚焦场景另通过 1/1 并生成 390px 中文视觉证据。2026-09-01 返回 OAuth 公开政策支持：新增 `/about`、`/privacy`、`/terms` 三条免登录静态双语页面、登录门与账号设置入口、精确 public-provider allowlist，以及 `specs/010-oauth-public-policies/` 发布清单。隐私政策生效日为 2026-09-01，并按代码真实披露 `calendar.events`、主日历读写窗口、受管事件、浏览器内存 token、账号隔离本地缓存、Supabase 事件引用、删除/撤销和可选 AI 数据流；服务条款不虚构公司、地址或管辖法。 | 真实 Google 账号点击“连接并同步”后，人工核对首次创建、Log Note 改名/改时、Log Note 删除、Google 原有事件只读、全天事件、拒绝授权与 API 失败；确认 Google Cloud 已启用 Calendar API，OAuth Web Client origins 包含实际部署域名。正式发布前必须解决新增合规阻塞：计划回顾会把重叠 Google 事件标题/时间交给 DeepSeek，而其公开隐私材料允许输入用于模型改进/训练；需取得禁止通用模型训练且满足 Google Limited Use 的合同/设置证据并补足上下文同意，或从 AI 请求中移除 Google 日历派生数据。聚焦政策单测 5/5、公共页面浏览器场景连续通过、320/390/1280 无溢出与 44px/focus 检查通过、设计规范 11/11、全量单测 220/220、PWA production build/installability/authenticated offline cache/persistence/controlled update、三条静态构建路由和 `git diff --check` 通过；当前完整 `npm run check` 仍被本写集外正在变化的 Diary Agent 分类回归阻塞（浏览器 31/32，`e2e/run-mobile.mjs:4920` 空元素），未据此改动 Agent 文件。通过前保持“验证中”，未部署、未发布。 |
| LN-066 | P0 | Google 账号身份基线 | Google Provider、PKCE callback、安全公开密钥校验和不虚报退出已完成并通过先前门禁 | 用户已纠正“本地身份”方向，原本地 profile 验收不再适用；Google 真实同浏览器登录/退出并入 LN-036 Phase 1 最终验收，LN-066 不单独 Accepted |

## 下一步

按表格顺序进入执行。

| ID | 优先级 | 任务 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |

## 等待

| ID | 优先级 | 任务 | 等待原因 | 恢复条件 |
| --- | --- | --- | --- | --- |
| LN-021 | P0 | 实际运行 14 天核心使用验证 | 需要用户选择真实 Day 1，并在日常使用中产生不可伪造的时间与行为证据 | 用户在 `docs/2026-08-13-14天核心使用验证协议.md` 填写 Day 1、设备与环境后开始；Day 7/14 由总控按协议复核 |
| LN-020 | P0 | 完成两次真实展示/评审并记录影响证据 | 需要用户安排真实对象、时间和展示场景，自动化不能替代外部反馈 | 至少完成 2 次真实展示/评审，并在演示包模板记录可核对反馈和至少 1 项影响证据 |

## 待规划

| ID | 优先级 | 任务 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| LN-007 | P1 | 定义 AI 与本地优先架构边界 | 评审文档明确数据是否/何时离开设备、授权方式、密钥与服务端方案、离线降级、删除重算、敏感信息处理和费用边界 | LN-003 |
| LN-008 | P1 | 扩展反馈闭环数据模型 | 区分原始记录、派生观察、干预/实验和结果；AI 不改原文；派生数据有来源且可更新、撤销、删除、重算；完整备份恢复通过 | LN-007 |
| LN-009 | P1 | 扩展 AI 分类反馈闭环 | 当前 LN-071 已限定为现有分类单选；若要新增可学习纠正、派生观察或额外分类能力，必须先完成数据来源、隐私、撤销与评估设计，不得回退为自动标签或改写原文 | LN-007、LN-008、LN-071 |
| LN-077 | P1 | AI 模板生成、增量优化与 Agent 分类结构提案 | 首次进入可选模板助手时，无用户描述必须零远程请求并直接使用本地默认结构；有明确描述时一次只生成一个完整模板提案。已有模板只能返回面向所选模板的保守小改，默认不改归属、记录类型、输入模式、周期、既有字段 ID/顺序且最多新增一个字段。用户主动让 Diary Agent 整理且现有结构确实无法承载时，可提出一个“领域 + 分类 + 模板”完整结构包；所有结果必须经结构校验、差异预览和显式确认后才本地先写，不自动建结构、不自动搬记录、不改原文。方案必须明确成熟平台选型、外发字段、账号/密钥、离线降级、Token 上限、备份兼容、撤销/移除和真实数据启用闸门 | LN-007、LN-009、LN-022、LN-069、LN-071 |
| LN-010 Phase 2 | P1 | 建立持久化“记录—调整—验证”闭环 | 在 Phase 1 本地只读复盘获得真实使用证据后，用户可主动生成阶段回顾；系统只提出一个有证据支持的模式与一个微型调整；可创建 3–7 天实验；结束后完成前后对比并收集准确性、可执行性和有效性反馈 | LN-010 Phase 1、LN-003、LN-008、LN-009 |
| LN-014 | P1 | 运行 14 天“当前轨迹”产品验证 | 连续记录 14 天；生成当前轨迹；找出一个关键变量；完成 3–7 天实验；收集解释准确性、执行成本和实际效用反馈 | LN-010 |
| LN-034 | P2 | 明确同日同周期模板多记录兼容策略 | 独立决定并验证“拒绝重复 / 合并 / 展示额外历史项”之一；页面可见数量与 Markdown 导出数量语义一致；旧备份、原始正文与恢复安全不受损 | LN-033；当前页面只展示第一条、Markdown 导出全部，暂不扩 schema/restore |

## 暂缓

| ID | 优先级 | 方向 | 重新启动条件 |
| --- | --- | --- | --- |
| LN-015 | P2 | Agent 提醒、日程、待办与有限自动化 | LN-010 的最小反馈闭环已被用户验证有效；具备逐领域授权、暂停、撤销和执行原因记录 |
| LN-016 | P2 | 匿名群体轨迹、搭子匹配、朋友/职业关系 | 个人记录基线、模式识别和历史最佳轨迹已经可靠 |
| LN-017 | P2 | Dating | 在 LN-016 之后单独完成隐私、人身安全、骚扰、欺诈、身份真实性与可解释性评审 |

## 已完成

| ID | 完成项 | 验证证据 |
| --- | --- | --- |
| LN-076 Rework 3（已被 Rework 5 取代） | 普通记录与固定记录之间的常驻多状态 Agent 活动区 | 本轮建立了 `idle/scanning/reviewing/complete` 四态本地透明线条精灵、可替换形象注册表、减弱动效静态落点与内容/控件避让。产品负责人在真实 390px 页面继续指出：即使空白被定义为“活动区”，它仍然破坏亲密性与空间利用率；Agent 应从页面上方沿装订边与记录并行，不应拥有正文流中的独立大块空白。四态资产、行为隔离和注册表继续有效，专属 `152–168px` 流式舞台与相关视觉验收由 Rework 5 取代；历史证据保留于 `output/ln-076-agent-stage/`。 |
| TOOL-001 | Spec Kit 0.16.5 项目级引入与 Log Note 模板适配 | `specify version --features` 通过；临时目录脚手架审查确认只写 `.specify/` 与 `.agents/skills/`；当前仓库已生成 Codex Skills、Constitution、项目覆盖模板和使用规范。2026-09-01 在不改业务代码的前提下补充社区标准知识架构：`ARCHITECTURE.md` 按精简 arc42 9.0 十二章重组并加入两张 C4 视图，`docs/decisions/` 采用 MADR 4.0，Constitution 升至 1.1.0 并明确 Living Spec 与技术真源。结构回归 5/5、设计规范 11/11、单测 225/225、独立 PWA production build/installability/authenticated offline cache/persistence/controlled update 和 `git diff --check` 通过；完整 `npm run check` 的移动浏览器为 30/34，剩余 4 条来自同一脏工作树正在进行的 About、日期头/Plan 旧动作 dock 与 320px Diary Agent reviewing 层级改动，因此本次不虚报完整门禁通过，也未越界修复。未提交、未推送、未部署。 |
| LN-075 Rework 7 | 全局书页纸张背景 | 页面底层由纯色纸面改为 `--paper` 暖白底叠加本地 `paper-texture.svg`：只包含低对比度随机纸浆纤维与轻微暖色明暗，无文字、外链、边框、污渍、阴影或可识别重复横线。纹理以 `360×360px` 连续平铺覆盖长日记页；弹窗、输入、功能卡与窄屏月历遮罩继续使用稳定实色，正文、蓝色状态和手绘书脊可读性不变。纹理加入 PWA 应用壳并验证离线命中；浏览器回归锁定 body 背景资产、repeat 与尺度，PWA 回归校验 SVG 真实纹理、无文字/外部图像及缓存 MIME。最终 `npm run check` 退出 0：设计规范 11/11、144/144 单测、22/22 浏览器、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check` 全绿。未改记录步骤、正文、数据、账号、同步或备份；未提交、未推送、未部署。 |
| LN-075 Rework 6（已被 Rework 7 取代） | 右上三工具改为书脊孔位文字按钮 | 搜索、月历、设置不再显示独立 icon，统一复用细书脊孔位并配可见文字；移动端孔位与全高笔画同轴，文字竖排，桌面端沿用同一孔位 + 文字语言。操作文字使用较小 Instrument Sans、细下划线与蓝色 hover/focus/展开状态，右轨内容目录继续使用更大的 Instrument Serif，明确区分工具与标签。三项真实命中区保持 `44px`，月历首行继续避开设置目标，键盘名称、快捷键、展开状态与 reduced-motion 均保留。视觉证据见 `output/playwright/ln-075-unified-rail-{390,426,1280}.png` 与 `output/playwright/ln-057-expanded-context-390.png`；回归新增孔位资产、同轴布局和字体/下划线差异断言。最终门禁通过设计规范 11/11、144/144 单测、22/22 浏览器、PWA production build/installability/authenticated offline/persistence/update、生产构建与 `git diff --check`。未改记录步骤、正文、数据、账号、同步或备份；未提交、未推送、未部署。 |
| LN-075 Rework 5（已被 Rework 6 取代） | 手绘记录分隔、整行聚焦与标题锚定目录轨 | 时间视图仅在普通记录存在时显示“记录”；空日期完整省略该标题、说明文案、对应右轨节点与孤立长线，直接进入固定字段。周期领域名留在隐藏区块标题与右轨；时间/正文改为 `18×6px` 手绘短横，记录、指标、区块和全高右轨均使用透明手绘栅格笔画。右轨在时间视图索引实际存在的普通记录区块与周期领域，在分类视图索引真实领域；节点优先与对应标题同高，越界后按文档顺序收拢，标题回到可读区时约 `180ms` 平滑归位，空间不足才内部滚动。普通记录、固定行和目录按钮的真实焦点改为完整蓝色手绘圈线，约 `200ms` 从左到右显露；输入原生硬矩形移除，reduced-motion 直接显示最终状态。ImageGen 生成 `rail-brush-handdrawn.png`、`record-rule-handdrawn.png`、`record-time-dash-handdrawn.png`、`record-focus-loop.png` 四张透明 RGBA 资产并加入 `v13` 离线壳。426×923 对照、聚焦与空日期证据见 `output/ln-075-rework-5-handdrawn-anchor/`，`design-qa.md` 为 `final result: passed`。最终 `npm run check` 退出 0：设计规范 11/11、144/144 单测、22/22 浏览器、PWA production build/installability/authenticated offline/cold-route/persistence/透明资产/`v13` upgrade、生产构建和 `git diff --check` 全绿。未改 schema、正文、领域/分类/标签、同步或备份；未提交、未推送、未部署。 |
| LN-075 Rework 4（已被 Rework 5 取代） | 细右轨、无错误月历空层与开放纸面记录 | 本轮把移动笔画和节点收细，删除月历错误空层，并把普通/周期记录从卡片改为开放纸面。用户随后明确时间视图不应重复领域标题，纠正时间/正文分隔为手绘短横，要求正文与右轨全部采用手绘笔画、目录节点跟随左侧标题，并以整行手绘圈线取代硬输入框。历史证据保留于 `output/ln-075-rework-4-fine-open-paper/`，当前视觉真源由 Rework 5 接管。 |
| LN-075 Rework 3（已被 Rework 5 取代） | 无标题横线、右轨月历、统一手绘图标与紧凑月历 | 本轮建立了右轨日历、三月份选择器、工具/笔画分层和统一手绘控制；用户随后连续收细轨道、月历与记录布局，并在 Rework 5 明确手绘内容线、标题锚定节点和整行聚焦。历史证据保留于 `output/ln-075-rework-3-handdrawn-icons/`，当前视觉真源由 Rework 5 接管。 |
| LN-075 Rework 2（已被 Rework 5 取代） | 移动端贯穿全页的统一右轨 | 本轮建立了搜索、设置、真实领域节点、日记/计划、下载、记录与计划新增共轴的全高笔画，以及设置内记录结构、整理小人和兼容路由；后续 Rework 继续细化月历、开放纸面、手绘线与标题锚定。历史证据保留于 `output/ln-075-rework-2-unified-rail/`，当前视觉真源由 Rework 5 接管。 |
| LN-075 Rework 1（已被 Rework 5 取代） | 参考日记首页、移动领域目录轨道、整理小人与设置内记录结构 | 本轮曾将右轨实现为分类视图的只读领域目录；其数据边界、领域映射、整理小人、设置内记录结构与兼容路由仍有效，后续由统一全高轨、开放纸面和标题锚定目录逐步取代视觉定义。历史证据保留于 `output/ln-075-rework-visual/`，当前视觉真源由 Rework 5 承接。 |
| LN-074 | AI 单日时间梳理初版 | `/organize` 已在既有单日上下文中提供“时间梳理 / 分类归档”两个次级任务并默认时间梳理；远端请求只包含所选日普通记录的 `id/time/content`、日期和界面语言。服务端使用 AI SDK、DeepSeek OpenAI-compatible provider 与结构化 schema 生成一句总览和引用源记录 ID 的时间段，随后执行 ID 白名单、去重、按真实时间重排与遗漏记录补齐；界面按上午/下午/晚上/未记录时间展示摘要和可核对原文，不改原记录、不持久化派生结果，也不生成建议或行为洞察。离线、无 Key、超时、限流和非法输出均明确退化为本地时间顺序；切换日期或任务会取消旧请求并清空旧结果。单元测试覆盖时间排序、无时间置底、非法/重复引用、遗漏补齐、请求最小化、鉴权/限流/超时/错误映射和模型参数；E2E 覆盖默认任务、切日取消、只读结果、本地降级、分类归档回归与 320/390/600/671/768/1280 六档无溢出。项目总控人工复核 `output/playwright/ln-074-daily-timeline-{320,390,600,671,768,1280}.png`；最终 `npm run check` 退出 0：设计规范 11/11、138/138 单测、22/22 浏览器、PWA production build/installability/authenticated offline cache/persistence/update、生产构建与 `git diff --check` 全绿。为避免在无人值守测试中发送真实私人记录，自动化使用模型契约 mock 与本地降级，真实账号首次点击后的摘要措辞仍留给用户人工复核。未提交、未推送、未部署。 |
| LN-073（首页归属胶囊视觉已被 LN-075 Rework 5 取代） | 首页记录标签 UI 统一与组件收口 | 本轮历史上统一了归属与用户标签几何；最新参考图要求首页移除可见领域/分类归属胶囊，因此当前首页只保留隐藏无障碍归属和真实用户 `#标签`。数据、搜索与导出语义未被 Rework 5 修改；旧证据保留在 `output/ln-073-label-system/`。 |
| LN-072（首页周期卡片视觉已被 LN-075 Rework 5 取代） | 首页模块间距与功能卡片收口 | 本轮历史上将可填写周期模块放入低阴影功能卡；最新参考图要求首页固定与分类周期记录回到开放纸面，因此当前视觉改用两列行与手绘分隔线。填写/展开行为和数据语义未被 Rework 5 修改；旧证据保留在 `output/ln-072-card-layout/`。 |
| LN-071 | 智能整理按已有领域/分类归档 | 用户确认继续使用当前 `2742160682@qq.com` 账号，并明确纠正“按照领域分类而不是加标签”。候选改为当前账号已有领域与分类的稳定路径，DeepSeek、服务端白名单、本地降级和页面应用统一使用 `categoryId`；每条记录最多一个非当前分类，非法与低置信结果保持原位。页面显示“领域 / 分类”，不再渲染 `#标签`；显式应用只改 `entry.categoryId`，正文、原标签、模板、附件、日期和时间逐字保持，撤销恢复原分类。历史示例按分类均衡抽取，浏览器不发送邮箱、附件、标签或完整文档。真实当前账号 `/organize` 核对 2026-08-18 共 6 条记录均显示当前“日常 / 记录”；为避免未经再次确认发送私人日记，未点击真实 DeepSeek 分析、未应用用户记录。`output/ln-071-category-organize-final` 六档视觉与回归通过；最终 `env E2E_PORT=3157 E2E_OUTPUT_DIR=output/ln-071-category-organize-final npm run check` 退出 0：设计规范 11/11、132/132 单测、22/22 浏览器、PWA 与生产构建、`git diff --check` 全绿。未新增 schema、领域、分类或标签，未提交、未推送、未部署。 |
| LN-070 | 日记 Markdown 合并导入与当前账号历史记录迁入 | 设置恢复页已支持多选日期 Markdown、普通 quick record 合并和 `date + time + content` 精确去重；2026-08-12～18 共 98 条已导入并同步至当时及当前继续使用的 `2742160682@qq.com`，总记录数由 14 增至 112，云端版本 2。用户 2026-08-18 最终确认“就这个账号吧不切换了”，因此不再迁往 Gmail，也不删除这 98 条。专项、移动端与完整门禁均通过；未提交、未推送、未部署。 |
| LN-069 | DeepSeek 单日智能整理真实模型接入 | 初始验收建立了已登录同源 API、Supabase token 验证、Origin/Host、256 KiB 请求体、每用户 10 次/分钟限流、20 秒超时、服务端 Key、损坏输出保护与本地降级能力；当时结果语义为已有标签匹配。2026-08-18 用户已用 LN-071 明确覆盖结果语义为已有领域/分类归档，LN-069 继续作为安全传输与远端模型基础，不再作为当前标签行为真源。当前对话曾暴露初始 Key，部署前需轮换并更新运行环境。 |
| LN-065 | 下载页亲密性结构与推荐信息收口 | 根据用户截图与既定“字体对比表达重要性、亲密性表达归属”原则，下载页稳定为“记录 → 备份 → 记录结构”三个主区块；下载格式降为记录组内的内缩渐进设置，推荐标签紧贴完整备份标题，浏览器记录/图片状态使用 `12px` 等宽元信息，换设备提示独立为下一行 `14px` 说明。三组标题统一为 `18px` 小标题，跨组间距为 `32px`，删除列表首尾装饰线，只保留相邻操作间的 `1px` 分隔；三组使用语义化 `section` 与 `aria-labelledby`，操作与 disclosure 保持至少 `44px`。正式页面规范已同步。人工复核 `output/ln-065-download-structure-final/ln-065-download-structure-{320,390,600,671,768,1280}.png` 与 `ln-065-download-structure-zh-390.png`：中英文层级清晰、中文截图避开 `160ms` 入场动画、六档无横向溢出，移动与桌面均保持连续纸张而非卡片墙。最终 `env E2E_PORT=3150 E2E_OUTPUT_DIR=output/ln-065-download-structure-final npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿；日期选择器场景固定测试时钟到既有 2026-08-15 基准，消除跨午夜硬编码波动，不改产品日期逻辑。未改 entries/schema、备份格式、Markdown 生成语义、恢复逻辑、账号/网络或快速记录；未提交、未推送、未部署。 |
| LN-064 Rework 3 | 常规设置说明与分隔线继续去冗余 | 根据用户截图直接反馈，常规页删除“界面语言”的显而易见效果说明，也删除“添加到手机主屏幕”的重复效果描述；语言项只保留名称、当前状态和切换操作，安装项只保留真正可执行的“分享菜单 → 添加到主屏幕”提示或安装按钮。常规面板不再重复栏目说明。设置组取消首项上边线和末项下边线，仅在“界面语言”与“添加到手机主屏幕”两个同级设置之间保留一根细分隔线。E2E 反向断言旧说明不再出现，并读取真实 CSS 确认列表顶边 `0px`、首行顶边 `0px`、第二行顶边 `1px`、两行底边均 `0px`。项目总控人工复核 `output/ln-064-rework3-final/ln-064-settings-general-copy-zh-390.png`：移动端无首尾装饰线、无重复文案，单一中间边界与设置归属清晰。最终 `env E2E_PORT=3149 E2E_OUTPUT_DIR=output/ln-064-rework3-final npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿。未改设置行为、entries/schema、备份格式、Markdown 输出、账号/网络或快速记录；未提交、未推送、未部署。 |
| LN-064 Rework 2 | 设置按用户任务与亲密度重组 | 根据用户直接反馈，将按技术概念拆分的常规、Markdown、备份、结构、存储五项收敛为常规、下载、恢复、图片四个用户任务。移动首页只保留一个“设置”标题，四行副信息分别显示当前语言、记录数、恢复动作、图片数量/容量，删除“选择设置”“记录保存在哪里”“本地使用”“保存在此设备”等重复或抽象表达。所有产生文件的动作按亲密度合并进下载页，并分为记录、下载格式、备份、记录结构；完整备份旁只出现一次浏览器数据边界，恢复独立承载导入与替换风险，图片页只管理占用和清理。移动详情只显示顶栏当前任务名，不再与正文重复标题；桌面保持四项 sticky 索引。`#export/#backup/#structure/#storage` 及 `#download/#restore/#images` 均兼容，旧 `#structure` 可直接看到记录结构组。项目总控人工复核 `output/ln-064-ia-final-check/ln-064-settings-mobile-index-{320,390}.png`、六档 `ln-064-settings-general-*` 与恢复页证据；320/390/600/671/768/1280 无横向溢出，触控与移动两级导航未退化。最终 `env E2E_PORT=3148 E2E_OUTPUT_DIR=output/ln-064-ia-final-check npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未改 entries/schema、备份格式、Markdown 输出语义、账号/网络或快速记录流程；未提交、未推送、未部署。 |
| LN-064 | 完整设置页面与本地数据控制台收口 | `/settings` 已收口为连续纸张式设置工作区：桌面保留 sticky 左侧索引与单一活动面板；`≤760px` 改用成熟移动设置两级结构，设置首页一次完整展示常规、Markdown、备份、结构与存储五项，点入后由紧凑 `56px` 顶栏承载当前分区与“返回设置”，详情全宽替换首页而非叠加或横向滚动，并保留 `/settings#structure` 深链。常规页进一步去除“此设备上的 Log Note”“本地数据”“作为 App 使用”等抽象术语，直接回答“能设置什么、记录保存在哪里、怎样添加到手机”：明确记录只保存在当前浏览器且不会自动同步，清除数据或换设备前应先备份，并给出“分享 → 添加到主屏幕”的操作路径。Portable 作为含图片时的推荐完整备份，JSON 明确不含图片文件；恢复独立分组并在最终确认列出记录、计划、结构、模板、Markdown 设置与图片的完整替换范围。损坏/迁移失败进入保护模式，暂停派生导出、编辑和图片清理；只有浏览器确实返回原始 payload 时才允许逐字下载，读取前失败不会伪造 `null` 文件，仍可选择已知 JSON/便携备份恢复。快捷跨面板入口转移键盘焦点，移动进入/返回使用克制 `180ms` 水平转场，reduced-motion 近乎即时，语言、安装、索引行与返回等关键目标不少于 `44px`。项目总控人工复核 `output/ln-064-copy-final/ln-064-settings-mobile-index-{320,390}.png`、`ln-064-settings-general-{320,390,600,671,768,1280}.png`、`ln-064-settings-general-copy-zh-390.png` 与 `ln-064-settings-backup-390.png`：移动首页五项一次可见、中文任务文案无歧义、详情无桌面侧栏与横向 Tab，六档无横向溢出；桌面索引未退化。最终 `env E2E_PORT=3143 E2E_OUTPUT_DIR=output/ln-064-copy-final npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿。未新增账号、网络、同步、数据库、主题或清空数据；未改 entries/schema、备份格式、Markdown 输出语义或快速记录流程；未提交、未推送、未部署。 |
| LN-063 | 首页切换层级、分类密度与横滑完整日期铭牌收口 | 顶部统一为日记专属 `Time / Category`，右下统一为两种工作区都可达的 `Diary / Plan`；进入计划不重置记录视图，返回日记恢复先前时间/分类状态。分类视图、周期记录与 `/organize` 保持亲密性层级和 `44px` 控件。首页横滑继续全屏命中，只有松手最终位移严格超过 viewport `20%` 才切换，越界后回撤取消；方向阴影克制衰减，目标提示由本地化完整日期铭牌替代圆球：中文 `2026年9月12日`、英文 `Aug 16, 2026`，按方向左右偏置，纵向锚点稳定、不跟手、不带动页面，并支持 reduced-motion。项目总控在 LN-064 最终 revision 复核 `output/ln-064-final/ln-063-date-card-complete-zh-390.png`、阈值提交/回撤证据及完整浏览器结果；最终 `env E2E_PORT=3143 E2E_OUTPUT_DIR=output/ln-064-final npm run check` 通过设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录流程；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 16：收窄方向阴影与固定中心日期球 | 根据用户纠正，保留 Rework 15 的全屏方向阴影语义，但把深色视觉范围收窄：右滑仍为左边缘最深并向右透明，左滑镜像，渐变在画布约 64% 处完全透明，不再形成大面积浮层。恢复唯一的固定中心日期球：`44×44px`、低饱和石墨半透明表面，只显示目标日或月，始终固定在 viewport 几何中心，不跟手、不吸附、不显示箭头、不带动页面。短拖时阴影和球在 `140ms` 内同步回收，过阈值后两者快速消退并完成既有前后日/月更新。项目总控人工复核 `output/playwright/ln-057-rework16-final/ln-057-rework16-{left-swipe-right-shadow-orb,right-swipe-left-shadow-orb,month-shadow-orb,plan-shadow-orb}-390.png`：阴影范围明显小于 Rework 15，透明端完整，中心球不抢主视觉；日记、展开月历与日计划位置一致。E2E 验证球尺寸 `44–52px`、中心偏差不超过 `1px`、目标标签可见、旧跟手球与边缘组件均不存在。最终隔离 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 15：全屏方向阴影 | 根据用户直接纠正，完整删除日期球、箭头、手柄、胶囊和楔形幕布等独立组件动效。全页横向意图锁定后只渲染覆盖整个 viewport 的线性阴影：右滑时左边缘为黑色并从左向右连续衰减至右边缘完全透明，左滑时镜像；纸面、顶栏、日期、正文、月格、日计划网格与浮动操作均保持静止，不位移、不缩放、不交叉淡化。短拖只让阴影在 `140ms` 内回收，过阈值后阴影快速消退并完成既有前后日/月更新。项目总控人工复核 `output/playwright/ln-057-rework15-final/ln-057-rework15-{left-swipe-right-shadow,right-swipe-left-shadow,month-shadow,plan-shadow}-390.png`：左右渐变方向正确、透明端完整、黑端未遮断内容，日记、展开月历和日计划一致；DOM 反向断言无 `.home-swipe-orb` 与 `.home-swipe-edge-cue`。保留全页命中、`pan-y`、阈值回收、点击抑制、选区清除、输入/弹窗/月轨隔离、键盘和 reduced-motion。最终隔离 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 14：参考视频式边缘幕布与暖石墨日期球 | 根据用户提供的视频 App 截图，移除 Rework 13 的小胶囊手柄和 Rework 12 的高饱和蓝色日期球。全页横向意图锁定后，在拖动方向对侧 viewport 边缘显露 `68×276px` 的窄高深色半透明楔形幕布：向右拖时左侧显示上一项白色箭头，向左拖时右侧显示下一项白色箭头；幕布贴边、横向侵入受限，不再形成占据半屏的大黑三角。日期球改为低饱和暖石墨渐变，去除蓝色残影、强高光和重 halo，继续承载目标日/月并吸附到顶部日期。项目总控对照用户原图人工复核 `output/playwright/ln-057-rework14-final/ln-057-rework14-{left-swipe-right-curtain,right-swipe-left-curtain,month-curtain,plan-curtain,graphite-orb-absorb}-390.png`：左右镜像方向正确，箭头清晰且弱于内容，日记、月历和计划一致；保留全页命中、日期/月语义、`pan-y`、点击抑制、选区清除、输入/弹窗/月轨隔离和 reduced-motion。最终隔离 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 13：系统式横滑边缘提示 | 用户认可日期吸附球，但否定自创蓝色短条，要求复用成熟手机交互。最终采用 predictive-back 边缘手柄：全页横向意图锁定后，在拖动方向的对侧边缘部分滑入 `24×40px` 圆角手柄；向右拖时左侧显示上一项左箭头，向左拖时右侧显示下一项右箭头。手柄使用纸面 tonal surface、细边框和轻阴影，随进度只做轻微显露与缩放；达阈值后先收束，再由日期球完成吸附，短拖则两者同步回收。未改变左滑下一日/月、右滑上一日/月的既有语义，也未影响 `pan-y`、点击抑制、文本选区清除、输入/弹窗/月轨隔离和 reduced-motion。项目总控人工复核 `output/playwright/ln-057-rework13-final/ln-057-rework13-{left-swipe-right-edge,right-swipe-left-edge,month-edge-cue,plan-edge-cue}-390.png`：边缘手柄部分裁切、方向明确且弱于日期球，日记、月历、计划一致，无蓝色进度条或第二主动画。E2E 新增左右侧、标准 chevron、部分裁切、回收消失和跨表面断言；最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 12：高级日期吸附球 | 根据用户认可的“纸面稳定、球负责方向、日期负责结果”，保留整页横滑命中范围，但取消 Rework 11 的整页搬移。横向锁定后在手指上方生成唯一的蓝色日期球：小幅拖动以哑光蓝墨滴跟手并轻微拉伸，达阈值显示目标日或月，完成后以 `190ms` 有重量地吸附到顶部日期；未达阈值则 `180ms` 回收消失。吸附阶段只让日期相关内容克制交叉淡入，页面、顶栏、月格、日记/计划内容和浮动操作保持稳定；去除玻璃高光、硬外圈与夸张发光，只保留单层柔和 halo。纵向滚动继续使用 `pan-y`，点击抑制、文本选区清除、输入/弹窗/月份轨道隔离和 reduced-motion 均保留。项目总控人工复核 `output/playwright/ln-057-rework12-final/ln-057-rework12-{orb,month-orb,plan-orb}-{drag,absorb}-390.png`：日记、展开月历与日计划三种表面动效一致，目标标签可读，无玻璃弹珠感、页面漂移或操作遮挡。E2E 同时修复 reduced-motion 下瞬时回 idle 的方向取证，最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 11：全屏横滑与整页切换动效 | 根据用户直接纠正，将横滑命中面从日期标题扩大到整个首页可视工作区。收起状态全屏横滑切前后日，展开状态切前后月；日记与计划均可从当前内容区发起。应用顶栏、日期身份、月格或当前日记/计划内容及上下文浮动操作使用同一位移、透明度与轻微缩放，形成一张纸页同步跟手；未达阈值以 `230ms` 克制回弹，达阈值后当前页约 `160ms` 退出、新页从相反方向约 `230ms` 带入。横向锁定后清除文本选区，纵向滚动保持 `pan-y`；输入、弹窗与月份快捷轨道保留独立交互，滑动后的点击被抑制，reduced-motion 近乎即时。项目总控人工复核 `output/playwright/ln-057-rework11-final/ln-057-rework11-fullscreen-drag-390.png`、`ln-057-rework11-fullscreen-month-drag-390.png` 与 `ln-057-rework11-plan-fullscreen-drag-390.png`：整页元素同向等距移动，无独立日期漂移、文本误选或控件脱离。E2E 覆盖短拖回弹、日/月方向、计划内切日、动作同步、非手势选日、六档无溢出及日计划内部滚动/FAB。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 10：日记/计划命名与箭头去冗余 | 根据用户最新截图纠正，上层内容模式统一收敛为英文 `Diary / Plan`、中文“日记 / 计划”；完整移除日期标题下方可见的前后日/月箭头组件及占位。收起状态继续横滑切日，展开状态继续横滑切月；点击日期展开月格、日期单元格、月份轨道和键盘网格导航继续提供明确的非手势路径。移除箭头后 341–700px 顶栏回到单行，320px 使用日期行 + 工具行保护完整日期与 44px 触控，701px 以上保留品牌行与共享日期行。项目总控人工复核 `output/ln-057-rework10-playwright-final/ln-057-rework10-diary-plan-no-arrows-{320,390,600,671,768,1280}.png`、`ln-057-rework10-expanded-{320,390,600,671,768,1280}.png` 与 `ln-057-rework10-plan-{390,1280}.png`：无箭头空白、无横向溢出，日记/计划复用同一日期 DOM，390px 展开计划仍保留可用小时网格且新增计划按钮完整可见。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-062 | 固定记录标题工具簇降噪 | 项目总控在 LN-057 Rework 10 同一最终 revision 独立复核：完成数使用 `12px`、`500` 字重、等宽字体与弱色，“调整”使用 `14px`、`500` 字重并提供至少 `44px` 触控和明确键盘焦点；二者同一行、同基线、紧邻排列并显著弱于 `22px` 固定记录标题。六档视觉证据见 `output/ln-057-rework10-playwright-final/ln-062-fixed-header-tools-{320,390,600,671,768,1280}.png`；E2E 逐档验证层级、颜色、基线、顺序、触控和键盘可达，完整 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA、生产构建与 `git diff --check`。未改记录数据、填写流程、备份或日期导航，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 9：移动端日期替代品牌标题 | 根据用户截图，320–700px 的唯一共享日期标题直接占用原 `Log Note` 品牌位置，记录工作区不再重复日期；320px 将语言放在第二行左侧、前后导航放在右侧，390–700px 语言与搜索/结构/设置保持首行，日期、星期和展开箭头均完整可见。701px 以上继续显示品牌第一行与同一日期 DOM 的第二行。展开/收起、收起切日/展开切月、横滑、Escape 回焦、选日保持展开、记录/日计划共享状态和日计划 viewport 均保留。项目总控人工复核 `output/ln-057-rework9-playwright-final/ln-057-rework9-mobile-date-header-{320,390,600,671,768,1280}.png`、`ln-057-expanded-context-{320,390,600,671,768,1280}.png` 与 `ln-057-shared-day-plan-390.png`；六档无横向溢出，语言与工具继续不少于 44px。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 8 | 根据用户直接纠正，把共享日期导航收敛为日记式标题：月/日使用 Serif 主标题，星期使用更小 Sans、弱色和 `8–16px` 间隔；收起与展开保留同一标题 DOM，彻底移除可见月份重复标题、“记录日/记录”统计和活动摘要。月格仍直接展开在标题下，唯一箭头、收起切日/展开切月、横向滑动、键盘、Escape 回焦、月份轨道、记录/计划信号点、记录/日计划共享上层 DOM 与日计划 action dock 语义均保留。项目总控人工复核 `output/ln-057-rework8-playwright-final/ln-057-collapsed-context-390.png` 与 `ln-057-rework8-diary-date-{390,1280}.png`：日期为第一层级、星期明确从属，无重复月份/统计、无横向溢出或控件挤压。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿；未改数据/schema、备份、快速记录、账号或网络，未提交、未推送、未部署。 |
| LN-061 | 固定记录前置归属标签弱化 | 项目总控独立复核六档截图与改动边界：领域标签保留在记录名前方，收敛为 `12px`、`500` 字重、`--faint` 弱色、`2px 6px` 内边距和极浅无描边胶囊；记录名继续为 `16px` 主信息，原网格、48px 行高、填写和展开行为不变。截图见 `output/ln-061-fixed-scope-tags-final/ln-061-fixed-scope-tags-{320,390,600,671,768,1280}.png`；同 revision 完整 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。确认未修改日期组件、数据/schema、备份、快速记录或日计划 action dock；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 7 | 根据用户截图把所选日期、展开箭头、当前月份、月度记录日/记录数与唯一前后导航收进同一顶部日期组件：收起时只保留紧凑日期主行，展开时在同一个 disclosure 内显示月份与安静的摘要；唯一箭头组紧邻该 disclosure，按现有语义收起切日、展开切月。月格从组件下方直接开始，不恢复内部标题、统计或第二组箭头；滑动、阈值回弹、键盘、Escape 回焦、月份轨道、记录/计划信号点、记录/日计划共享 DOM 与日计划 action dock 语义均保留。项目总控复核 `output/ln-057-rework7-playwright-final/ln-057-rework7-fused-date-context-{390,1280}.png`，六档无横向溢出、日期与箭头间距不超过 24px、关键触控不少于 44px。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿；未改数据/schema、备份、快速记录流程、账号或网络，未提交、未推送、未部署。 |
| LN-058 | 全项目亲密性间距、字体层级与分类页节奏收口 | 项目总控独立核对语义字体×亲密性表、CSS 边界、六档截图与 `output/ln-058-playwright-final/ln-058-spacing-evidence.json`：390px 分类标题→首项 `4px`、相邻分类 `20px`、内容→领域分隔 `24px`、下一领域标题内距 `29px`；搜索计数→结果约 `8px`、正文→辅助信息约 `4px`；结构相邻分类 `8px`；附件文件名→元信息 `4px`；智能整理标题→说明 `8px`、说明→范围 `16px`；设置标题→说明 `8px`、说明→内容 `16px`、区块内距 `24px`。确认未改变数据/schema、备份、记录流程、融合日期导航或日计划 action dock。最终隔离 revision 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA、production build 与 `git diff --check`。剩余风险仅为不同系统中文字体回退的轻微字面宽度差异；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 6 | 在唯一共享日期导航上增加横向手势：收起时左滑下一日/右滑上一日，展开时同手势切前后月；拖动跟手并轻微淡化，达到 `56px` 或快速短划阈值后以 `180ms` 方向动效落位，小幅移动回弹。项目总控独立核对 `touch-action: pan-y`、滑动不误触展开、可见箭头与键盘路径保留、reduced-motion `1ms`，并复核 `output/ln-057-rework6-playwright-final/ln-057-rework6-swipe-drag-390.png` 与 `results.json`；动效克制、无新增遮挡或信息结构。最终隔离 revision 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`；未改数据/schema、备份、快速记录步骤、唯一箭头结构或日计划 action dock，未提交、未推送、未部署。 |
| LN-060 | 日计划隐藏记录视图 action dock | 根据用户直接反馈，日计划激活时不再渲染记录专用的“时间 / 分类”胶囊、导出和新增记录 action dock；日计划自己的新增计划 `+` 保留，上方共享“查看记录 / 日计划”仍是清晰返回出口。E2E 反向断言日计划 `.view-switch` 数量为 0，并真实通过“查看记录”返回时间记录；与 LN-057 Rework 4 同一最终 revision 完整 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器回归、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未改记录视图中的浮动切换、数据或快速记录流程；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 4 | 将原先分离的“月份入口、所选日期、前后日导航”融合为唯一共享日期导航组件：左侧所选日期本身承担月历展开/收起，箭头明确状态，右侧保留前后日与桌面回到今天；不再渲染独立月份行或第二日期行。层级收敛为“应用顶栏 → 融合日期导航 → 可选月格与月份轨道 → 记录/日计划切换 → 当前内容”；选日和切换记录/日计划均保持月历展开与同一 DOM，Escape 收起并回焦日期按钮。320/390/600/671/768/1280 无横向溢出且日期按钮不少于 44px，390×844 展开日计划仍显示约 3 个小时行，新增计划按钮完整可见。视觉证据见 `output/playwright/ln-057-collapsed-context-390.png`、`ln-057-expanded-context-{320,390,600,671,768,1280}.png`、`ln-057-shared-day-plan-{390,1280}.png`；最终 revision 完整 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器回归、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未改 entries、planBlocks、schema、备份、Markdown、账号、网络或快速记录流程；未提交、未推送、未部署。 |
| LN-059 | 固定记录标题区移除重复说明 | 根据用户截图删除标题下方“数值直接填写，表单在当前列表展开”及 `0/6` 下方重复的“剩余 6 项”；可见区域保留固定记录标题、完成/总数和调整入口，完整完成/剩余语义继续通过进度元素的中英文无障碍标签提供，不改内联填写、保存、数据、备份或快速记录步骤。移动 E2E 新增反向断言，验证两段冗余文案不可见且无障碍进度仍存在；视觉复核 `output/playwright/ln-052-compact-home-{600,671,768}.png`。隔离端口 `3157` 完整运行 `npm run check`：设计规范、97/97 单测、20/20 移动 E2E、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未提交、未推送、未部署。 |
| LN-056 | 将“时间 / 分类”视图切换移至右下快速操作区 | 顶栏移除常驻视图切换，轻量胶囊固定在右下新增/导出操作上方并与主操作右对齐；日计划仍可直接返回时间或分类，激活态、键盘聚焦、语言持久化、至少 44px 触控和 320/390/600/671/768/1280 无横向溢出均由浏览器回归覆盖。项目总控逐张复核 `output/playwright/ln-056-floating-switch-{320,390,600,671,768,1280}.png`，并在同一稳定 revision 独立运行 `npm run check`：设计规范、97/97 单测、20/20 移动 E2E、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未提交、未推送、未部署。 |
| LN-055 | 基础智能整理与日常记录分类工作台 | 建立了独立 `/organize`、桌面双栏、移动分步、原因/置信、逐条移除、忽略、重算、显式应用和撤销等交互基础；其最初“已有标签”结果语义已被 2026-08-18 的 LN-071 覆盖，历史截图和门禁仅作为工作台与交互基线，不再作为当前数据写入行为真源。 |
| LN-054 | Google Calendar 式月概览交互收口 | 月模式隐藏首页重复大日期区，以紧凑月份轨道、开放式 7 列月格、选中日圆形强调和记录/计划信号点完成浏览；点击日期留在月概览并更新当天上下文，“查看记录/计划这一天”分别进入既有记录和日计划链路。键盘方向键/PageUp/PageDown、月份轨道、前后月按钮和至少 44px 触控目标均有 DOM 回归；320/390 首屏同时显示完整月格与上下文起点，600/671/768/1280 保持单一阅读簇且无横向溢出。视觉证据见 `output/playwright/ln-054-google-month-{320,390,600,671,768,1280}.png`。项目总控在当前 revision 独立运行 `npm run check`：设计规范、90/90 单测、19/19 移动 E2E、PWA production build/installability/offline/persistence/update 和 `git diff --check` 全绿；沙箱内首次监听 3141 因 `EPERM` 未进入产品断言，获准本地重跑后退出码 0。未接 Google API，未改 schema、备份、Markdown、账号、网络或快速记录步骤；未提交、未推送、未部署。 |
| LN-052 | 600–800px 紧凑桌面/平板响应式收口 | 600/671/768 使用单行顶栏、36px 日期标题与受限的时间线/固定记录/值列阅读簇，320/390 手机和 1280 桌面保持原有层级；真实 DOM 断言覆盖无横向溢出、固定行与输入列宽度，视觉证据见 `output/playwright/ln-052-compact-home-{600,671,768}.png`。项目总控在唯一 Next 进程下独立重跑 `npm run check`：设计规范、90/90 单测、19/19 移动 E2E、PWA 安装/离线/持久化/升级、生产构建和 diff check 全绿；首次沙箱内 `3141` 绑定因 `EPERM` 未进入断言，获准本地重跑后退出码 0。未提交、未推送、未部署。 |
| LN-051 | 代码减量、数据写入与 Next/PWA 边界收口 | 首页派生模型、附件草稿生命周期和记录视图按完整职责拆出，后续复核把 `page.js` 继续降至 464 行；首页路由样式按月历、日计划、时间线和固定记录拆分且均低于 400 行。删除无消费者导出、旧结构 CSS、测试专用计划函数、重复日期规则和重复翻译键；统一下载、日期显示、持久化与质量门禁边界。设置和结构页只在 localStorage 写入成功后更新 UI，损坏 payload 仅在确认恢复真实写入成功后解除保护；带图片草稿不能切到不支持图片的模板，失败的 Blob 清理保留重试 ID 并给出设置页清理提示，容量错误使用稳定 code；便携恢复以单个 IndexedDB 事务写入最终附件集合，旧库已占满 50 MiB 时仍可恢复最终合法的小型备份，localStorage 失败继续回滚新 Blob。根布局保留一次性安装提示，Service Worker v6 隔离 API/RSC，查询和尾斜杠路由离线命中对应 pathname，真实未缓存 script 失败不返回 HTML；移动 dev 与 PWA build 使用隔离的 Next `distDir`，PWA runner 成为质量门禁唯一 production build owner，避免重复构建和产物互相破坏。完整门禁通过 90/90 单测、19/19 移动 E2E、PWA、单次生产构建、设计检查和 diff check；静态路由体积为首页 147 kB、设置 134 kB、结构页 153 kB。未提交、未推送、未部署。 |
| LN-050 | 顶栏语言入口与轻量胶囊视图切换 | 顶栏在 320/390/1280 下保持语言入口先于胶囊式时间/分类切换，同一行内无横向溢出；激活与未激活状态、薄边框、至少 44px 触控和相邻搜索/结构/设置入口均由浏览器断言覆盖。视觉证据见 `output/playwright/ln-050-topbar-{320,390,1280}.png`；同 revision 通过完整质量门禁。未提交、未推送、未部署。 |
| LN-049 | 日计划新增入口去歧义 | 日计划保留“点击空白时段直接创建并按 15 分钟吸附时间”的画布交互，同时仅保留一个固定在当前 viewport 右下的显式新增计划 `+`；日计划隐藏全局新增记录和导出操作，返回时间视图后快速记录与导出恢复，并真实保存一条普通记录。17/17 移动 E2E 覆盖空白时段创建、显式入口与返回主链路。未提交、未推送、未部署。 |
| LN-047 | 月历内本地日计划 viewport Rework | 日计划成为顶栏以下的剩余 viewport 工作区，页面本身不滚动，小时网格独立滚动，紧凑日期、模式切换和前后日导航保持可见；计划块支持重叠分列，新增 FAB 在 320×844、390×844、1280×720 均完整位于 viewport 内。非 fullPage 证据见 `output/playwright/ln-047-google-day-plan-{320,390,1280}.png`；完整门禁通过。未提交、未推送、未部署。 |
| LN-048 | 固定单值记录移除冗余保存勾号 | 根据用户对无意义按钮的直接反馈，删除固定单值输入右侧仅重复触发失焦的勾号和对应样式；输入框继续在真实失焦时自动保存，Enter 仍通过表单触发失焦，保存继续使用既有 Toast，不增加替代图标或操作步骤。失焦处理改为读取输入框当前值，避免快速输入后立即 Enter 时 React 草稿尚未提交而读取旧值。移动 E2E 断言 320/390 输入字号至少 16px、三档均无按钮且输入占满值列，并真实覆盖失焦保存、Enter 更新和清空删除。当前 revision 通过 90/90 单测、15/15 移动 E2E、PWA、生产构建、设计校验和 diff check；视觉证据见 `output/playwright/ln-041-category-hierarchy-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-046 | 日期标题月历入口调整 | 根据用户截图把日期导航中间的“今天/日期”从浏览器原生日期输入改为应用内月历唯一入口，并移除下方视图切换中的重复“月历”按钮；下方只保留“时间/分类”，左右箭头继续逐日切换，月历日期继续返回既有时间视图。不新增记录步骤、数据字段、网络、账号或备份变化。日期按钮提供中英文无障碍名称、激活状态、键盘焦点和至少 44px 触控目标；移动 E2E 覆盖“点日期入口 → 月历打开 → 无重复入口 → 选日回时间视图”，PWA 离线月历也使用同一入口。85/85 单测、14/14 移动 E2E、PWA 离线月历/持久化/升级、生产构建、设计校验和 diff check 全绿；视觉证据见 `output/playwright/ln-044-calendar-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-045 | 分类视图排版四原则收口 | 按亲密性、对齐、重复、对比完成日期头与分类记录簇收口：移动日期保持单行并与视图切换分行；领域→分类→首项间距分别稳定在既定范围；字段标签和值共享阅读轴，同类字段行统一分隔节奏，可展开行不再例外；320/390/1280 无横向溢出。后续按用户计数反馈移除领域与普通记录分类的冗余数量，含可见周期模板的分类改为显示完成/总数，未完成状态保留 `0/1`，并提供中英文无障碍标签；不改记录数据、正文、备份或快速新增步骤。当前 revision 通过 90/90 单测、15/15 移动 E2E、PWA、生产构建、设计校验和 diff check；普通分类与周期比例证据见 `output/playwright/ln-033-category-390.png`、`output/playwright/ln-045-layout-principles-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-044 | 月历记录浏览视图 | 用户直接授权后新增纯本地“月历”第三浏览视图：按月展示每天的全部记录数量、当月活跃记录日与总记录数，支持上/下月、相邻月日期、方向键、Home/End、PageUp/PageDown，并从日期直接进入既有时间视图；英文周日起始、中文周一起始。实现由独立纯函数 `src/lib/calendar-model.mjs` 和可移除视图组件组成，不改 entry、schema、备份、Markdown 或快速新增步骤，不引入日程、提醒与网络。真实浏览器发现 320px 日期格初版仅约 39.6px，已改为移动端贴边网格并验证日期格及翻月按钮均至少 44px；桌面阅读簇限制为 700px，浮动新增/导出不遮挡日期。专项 6/6、全量 85/85 单测、14/14 移动 E2E、完全离线打开月历的 PWA 安装/持久化/升级回归、生产构建、设计校验和 diff check 全绿；证据见 `output/playwright/ln-044-calendar-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-043 | 首页分类视图无意义留白压缩与响应式密度收口 | 根据用户对扫描路径和空隙的反馈，在保持“领域 → 分类 → 指标 → 值”语义字号与逐级缩进的前提下压缩首页日期头、领域、分类、记录行、固定记录和空状态的垂直节奏；修复移动端通用 `.fixed-records` 规则覆盖嵌入式列表、为每个分类重复增加上下内边距的根因。桌面端指标/值列限制在 620px 阅读簇内，避免两端无意义拉扯；320×844、390×844、1280×720 均无横向溢出，并新增领域到分类 `8–16px`、分类到首项 `0–8px`、分类间 `12–20px` 和指标到值 `24–220px` 的真实布局断言。当前工作区通过 79/79 单测、13/13 移动 E2E、PWA 安装/离线壳/持久化/升级、隔离生产构建、设计校验和 diff check；视觉证据见 `output/playwright/ln-043-compact-density-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-042 | 本地离线图片附件主工作区纵向闭环 | P01-T14 的隔离实现经 P01-T00 三方差异核对后精确移植，保留 LN-040 时间线对齐和 LN-041 语义层级资产：普通自由文本记录可在“更多”中添加一张 JPEG/PNG/WebP，Blob 仅存 IndexedDB，v3 状态只保存引用；单张 5 MiB、总量 50 MiB，不使用 data URL、远程图片、数据库或上传。时间/分类视图只从本地 `blob:` URL 展示，缺图保留正文并安全占位；设置页提供占用/孤儿清理和带 SHA-256 的 `.lnbackup` 便携导出恢复，损坏、截断和 localStorage 写入失败均不覆盖当前数据并回滚新 Blob。主工作区同 revision 通过 79/79 单测、13/13 移动 E2E、PWA 完全离线上传/刷新/展示、生产构建、设计校验与 diff check；证据见 `output/playwright/ln-042-attachment-{320,390,1280}.png`、`output/playwright/pwa/ln-042-offline-image.png`、两份 results JSON 与 `docs/2026-08-14-LN-042本地离线图片附件实施说明.md`。未提交、未推送、未部署。 |
| LN-041 | 全局字体层级与分类视图阅读顺序重构 | 按用户视觉反馈建立 display / 页面标题 / 区块标题 / 分类标题 / 正文 / 标签 / 元数据语义字号，并统一正文用 Sans、展示与章节标题用 Serif、时间与真实数值用 Mono；移除分类阅读页无语义竖线，改为领域→分类→具体指标的字号级差和逐级缩进，数量紧邻所属标题并带“类/项”单位，数值置于指标之后且不抢主层级。同步整理模板、设置、搜索、编辑器、附件区和首页时间线字重与字体角色；全项目 CSS 无 8–11px 可见文字或未加载字重，模板管理中表达拖拽树关系的竖线保留。新增中文分类层级、模板层级、设置层级、附件标题/操作/文件名/元数据层级、三档无横向溢出和首页 44px 触控回归；当前工作区 79/79 单测、13/13 E2E、PWA 安装/离线/文字与图片持久化/升级、生产构建、设计校验和 diff check 通过。视觉证据见 `output/playwright/ln-041-category-hierarchy-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-040 | 首页时间线记录行对齐修复 | 按用户截图修正时间线记录行的列与纵向基线：时间文字与“领域 · 分类”元信息顶端对齐，正文列在移动端和桌面端与固定记录标签列共用同一间距节奏；不改记录数据、正文、交互步骤或固定记录行为。新增移动端真实布局断言，验证文字起点和跨模块列间距；73/73 单测、11/11 移动 E2E、PWA 安装/离线/持久化/升级、生产构建、设计校验和 diff check 通过。视觉证据见 `output/playwright/ln-039-timeline-390.png`。未提交、未推送、未部署。 |
| LN-039 | 普通记录 Markdown 基础富文本与安全展示 | P01-T13 在用户直接重新放行后续用原任务完成：普通自由文本选区支持可逆粗体、斜体及正文/标题/副标题整行样式；时间视图、分类视图和搜索结果以 React 文本节点安全渲染标题、强调、列表、任务列表和换行。`entry.content` 继续逐字保存 Markdown 纯字符串，不使用 `dangerouslySetInnerHTML`，HTML、URL、图片与破损语法保持字面文本且不触发网络请求；搜索、JSON backup/restore、Markdown export、离线链路不变。项目总控独立复核专项 12/12、全量 72/72 单测、11/11 移动 E2E、PWA 安装/离线/持久化/升级、生产构建、设计校验和 diff check；320×844、390×844、1280×720 编辑器及 390×844 渲染证据见 `output/playwright/ln-039-*.png`。本项未把 Markdown 图片语法伪实现为联网图片；用户选择的本地离线附件已由 LN-042 独立闭环。未提交、未推送、未部署。 |
| LN-038 | 普通记录 Markdown 列表连续输入 | P01-T12 returned 后经项目总控独立验收：普通自由文本 `textarea` 中，`- ` 与 `- [ ] ` 列表支持 Enter 同类续写和空项退出；Shift+Enter、修饰键与 IME 不触发，支持光标中段、选区、中文/emoji 和 Chromium 原生撤销。实现以独立纯文本模块 `src/lib/markdown-list-input.mjs` 接入现有 `entry.content` 字符串链路，无新增 CSS、工具栏、schema/restore 或旧正文转换。项目总控重新通过专项 8/8、全量 60/60 单测、移动 10/10 E2E、PWA 安装/离线/持久化/升级、设计校验与 diff check；320×844、390×844、1280×720 证据见 `output/playwright/ln-038-composer-*.png`。未提交、未推送、未部署。 |
| LN-035 | 本地可运行的报告下载 API 纵向闭环 | P01-T11 returned 后经项目总控精确移植并独立验收：同源 `POST /api/reports/download` 支持 Markdown 单日/范围/全部、完整备份 JSON 与不含 entries 的结构 JSON；调用方显式提交 state，服务端不读取 localStorage、不保存或上传数据。请求体按流累计，超过 1 MiB 立即取消并 413；生成结果限制为 2 MiB UTF-8，Markdown 在既有生成器前做保守预算，所有格式生成后精确校验；成功/错误均提供受控类型、文件名、private no-store、nosniff 与 UTF-8 Content-Length。主工作区独立通过 15/15 专项、52/52 单测、9/9 移动 E2E、PWA 安装/离线/持久化/升级与 API 不入缓存、生产构建、设计校验和 diff check；真实 3111 HTTP 以合成 fixture 验证 200 Markdown、中文字节长度、405、403、输入 413 与输出放大 413。说明见 `docs/2026-08-14-LN-035本地报告下载API验证说明.md`。未连接数据库、未部署、未使用真实数据。 |
| LN-001 | 建立项目任务看板与统一任务编号 | 本文件已建立状态、优先级、验收标准、依赖和维护规则 |
| LN-004 | 结构拖拽排序与体验收口 | `O1-KR4｜Log Note影响力成果｜01 MVP收口` 完成 27/27 单测、设计规范校验、生产构建及桌面/移动端浏览器验收；尚未提交 |
| LN-013 | 本地数据保护 | `T01` 完成：解析/迁移失败保留原 payload 且禁止自动覆盖；30/30 测试、设计校验、生产构建与差异检查通过；恢复面板 UI 尚未纳入范围 |
| LN-002 | 文档与实现口径校准 | `T01` 完成：README、product 与结构示例已改为领域→分类→模板、14 条种子、周期模板、导出/恢复和本地优先边界；30/30 测试、生产构建与差异检查通过 |
| LN-005 | 核心浏览器 E2E 回归 | `T01` 完成：`npm run test:e2e` 在移动端缓存 Chromium 上 4/4 通过，覆盖线性记录 CRUD/搜索、结构化必填、周期记录、模板排序与结构导出、JSON 恢复和 Markdown 导出；失败保留截图与 trace |
| LN-006 | 统一质量门禁与 CI | `T01` 完成：`npm run check` 顺序通过 30/30 单测、4/4 移动端 E2E、生产构建和差异检查；PR/push 工作流在 Playwright 1.61.1 Chromium 容器中执行 `npm ci && npm run check` |
| LN-011 | 本地备份与安全恢复防线 | `T01` 完成：设置页显示备份状态、浏览器/设备/域名/端口风险及恢复校验说明；恢复先验证再确认替换，损坏文件不改写当前数据；31/31 单测和 4/4 移动端 E2E 覆盖空备份、旧版本、重复 ID、损坏文件与恢复后内容保留，`npm run check` 通过 |
| LN-012 | 真实 PWA 安装、离线与升级验证 | `e2e/run-pwa.mjs` 在生产构建与移动端 Chromium 中验证：在线后 `/`、`/templates`、`/templates?focus=periodic`、`/settings` 与 `/settings/` 均可离线打开，离线新增后刷新保留；manifest 192/512 PNG maskable 图标均为 200；Chrome installability errors 为 0；worker `v6 → e2e-previous → e2e-current → v6` 激活时每轮只保留目标缓存。`npm run check` 通过 |
| LN-003 | 首要用户、阶段目标和成功指标 | `product.md` 明确作者本人为当前首要用户、核心记录循环与 MVP 阶段成功条件；北极星指标为“连续有用记录周”，并定义 14 天记录持续性、60 秒有用检索、可靠演示、备份恢复、离线及未来观察/建议/实验指标；功能数量、记录总数和使用时长不作为成功指标；准入评分、红线与 14/30 天退出机制已建立；总控实施过程记录见 `docs/2026-08-13-持续迭代与子Agent调度实施记录.md` |
| LN-018 | 14 天核心使用验证协议 | `docs/2026-08-13-14天核心使用验证协议.md` 提供可直接填写的 Day 1–14 日表、有效记录日/种子排除、两次 60 秒有用检索、Day 1/7/14 备份恢复与离线检查、异常日志、北极星和 10/14 公式、退出/校准条件、隐私边界及 LN-019 证据交接；不新增遥测、账号、同步、提醒或记录必填 |
| LN-019 | 结构化案例与可靠演示包 | `docs/2026-08-13-Log-Note结构化案例与演示包.md` 记录背景、约束、决策、任务分解、已实现能力、失败恢复和证据边界；提供约 6 分 30 秒演示脚本、演示前检查、证据索引、隐私脱敏规则及两次真实展示/评审空模板；明确 14 天真实使用、外部展示与影响证据尚未发生 |
| LN-022 | 输入与模板工作流体验收口 | 模板往返按模板保留草稿，非空/已改记录关闭有放弃保护；已有兼容结构化记录可按字段重开编辑；结构化与单值 prompt 可见，支持快捷保存和必填聚焦；模板新建提供自由文本/结构化/每日单值起点，可复制并即时预览；导出按记录/备份/结构分组，通用示例覆盖 3 种输入模式、5 种字段和 3 种周期。`npm run design:check` 通过；`npm run check` 通过 31/31 单测、4/4 移动端 E2E、PWA 安装/离线/升级、生产构建和差异检查；实施说明见 `docs/2026-08-13-输入与模板工作流优化实施说明.md` |
| LN-023 | 固定记录模块空状态与直达填写 | 修正首页仅在已有周期记录时渲染模块的问题：只要存在周期模板，首页始终显示“固定记录”，未填写项显示“去记录”并可一键打开对应模板，已填写项继续直达编辑；标题显示完成数/总数。31/31 单测、4/4 移动端 E2E、PWA 验证、生产构建、设计校验和差异检查通过 |
| LN-024 | 首屏语言切换与轻量交互去弹层 | 首页页头新增紧凑的 `EN / 中` 原地切换，选择即时生效并在刷新后保留；语言设置从设置弹窗移除；设置弹窗取消“通用/数据”二级分页，打开即进入数据管理。记录编辑、搜索和备份恢复继续保留专注或防误操作弹层。31/31 单测、4/4 移动端 E2E（含双语切换与持久化）、PWA 验证、生产构建、设计校验和差异检查通过 |
| LN-025 | 固定记录首页内联填写 | 固定记录不再打开记录弹窗：晨重、步数、腰围等单值在首页列表直接输入，回车或失焦保存，清空已有值删除；睡眠等结构化周期项在当前行原地展开字段、校验、保存并收起；已有值可原地修改，普通快速记录流程不变。31/31 单测、4/4 移动端 E2E（覆盖单值内联与结构化展开）、PWA 验证、生产构建、设计校验和差异检查通过；页面规范已明确固定记录优先内联交互 |
| LN-026 | 模板与设置管理页面 UI 统一 | 设置从首页弹窗迁移为独立 `/settings` 管理页；模板与设置共用返回、Log Note 眉题、页头尺寸、940px 页面外壳和 760px 主工作区，移动端使用自然文档滚动；首页齿轮改为链接，导出、恢复、Markdown、备份和安装功能保留；`/settings` 纳入离线应用壳。31/31 单测、4/4 移动端 E2E、PWA 在线/离线/升级、生产构建、设计校验和差异检查通过；实施说明见 `docs/2026-08-13-模板与设置管理页面统一实施说明.md` |
| LN-027 | 固定记录首屏布局与信息层级 | P01-T05 完成并经总控独立验收：桌面时间线与固定记录并列进入同一首屏工作区，移动端固定记录先于时间线；移除空时间线视口最小高度与大块占位，进度显示完成/总数及剩余项，新增记录主 CTA 不变。31/31 单测、5/5 移动端 E2E、PWA 在线/离线/升级、生产构建、设计校验和差异检查通过；截图证据见 `output/playwright/ln-027-desktop-empty.png` 与 `output/playwright/ln-027-mobile-empty.png`。 |
| LN-028 | 固定记录填写模型与旧模板兼容 | P01-T06 returned 后由总控精确移植并补充反例修正：value、structured、自定义 legacy free 与不可逆 structured 在首页分流；仅正文符合已知 `标签=值` 形态的旧内置单值项适配为 value，自由原文不因模板 ID 被误判；旧 structured 未知字段保留；固定记录在 localStorage 真正写入成功后才折叠或显示成功，失败与 recovery-blocked 均保留草稿并提示未保存；不迁移模板、不提升 schema、不静默改写正文。34/34 单测、6/6 移动端 E2E、PWA 在线/离线/升级、生产构建、设计校验和差异检查通过。 |
| LN-029 | 固定记录完成反馈与回归验收 | P01-T07 对真实 `localhost:3100` 完成最终只读验收：320×844 与 390×844 的 5 个单值输入均为 16px，保存按钮均为 45×45px，页面无横向滚动；1280×720 时间线与固定记录首屏双栏对齐。触控保存后反馈明确、刷新持久化，旧“去记录”入口为 0；value、structured、legacy free、失败重试、清空、离线、JSON 往返、toast live region 与 reduced-motion 回归通过。34/34 单测、6/6 移动端 E2E、PWA、生产构建、设计校验和差异检查通过；运行时证据见 `output/playwright/ln-029-final-recheck.json` 与三视口 `ln-029-recheck-*.png`。 |
| LN-030 | 固定记录底部布局、统一结构管理与可逆显示 | P01-T08 returned 后由项目总控精确移植并独立验收：首页顺序改为“当天内容 → 固定记录”，快速新增路径不变；移除多数行级横线；领域、分类、记录方式合并为连续层级树但数据概念独立；固定记录提供调整入口和 `homeVisible` 可逆暂停/恢复，旧数据缺失字段默认显示，完整/结构 JSON 往返保留状态，不改历史正文。中英文改为动作与结果语言，桌面编辑使用宽侧边检查器，移动端保留 autosave、44px 触控与软键盘滚动余量。35/35 单测、7/7 核心 E2E、PWA、生产构建、设计校验和差异检查通过；实施说明见 `docs/2026-08-13-LN-030固定记录与结构管理体验实施说明.md`。 |
| LN-031 | 固定记录管理与展开的移动端体验 | P01-T09 对主工作区完成独立运行验收：320×844 与 390×844 首页均无横向滚动，固定记录位于当天内容之后，5 个输入均为 16px、保存触控均为 45×45px；`/templates?focus=periodic` 在 320px 只展示固定记录，拖拽、重排菜单和新增入口均为 44×44px，结构层级无过度缩进；320×500 编辑 sheet 全屏可达，自动保存状态可见；1280×720 无移动端样式回退。旧 T09 worktree 的 `home-timeline.css`、`templates.css`、`run-mobile.mjs`、`template-page.js` 改动保持隔离且未移植。完整主工作区门禁沿用 LN-030 的 35/35 单测、7/7 E2E、PWA、生产构建、设计校验与差异检查通过证据。 |
| LN-032 | Markdown 输出、格式编辑与导出入口去重 | P01-T10 returned 后经项目总控独立验收：`/settings#export` 成为唯一完整导出工作区，将今日/全部 Markdown、即时格式编辑和当前日预览共置；完整 JSON 备份恢复、记录结构与起步示例分别独立分组。模板页重复“导出记录”抽屉及五项下载已删除，页头“导出选项”直接进入 `/settings#structure`；首页当前日快捷导出不变。动作列表移除尾部箭头、蓝底图标块和连续等权横线；320/390/1280 无横向滚动，格式输入 16px，关键动作高 64–74.875px。36/36 单测、7/7 移动端 E2E、PWA 安装/离线/持久化/升级、生产构建、设计校验和差异检查通过；证据见 `output/playwright/ln-032-visual-evidence.json`、四张 `ln-032-*.png` 与 `docs/2026-08-13-Markdown输出工作区实施说明.md`。 |
| LN-033 | 分类视图与 Markdown 统一固定记录归属 | P01-T08 主实现和返工、P01-T10 导出契约核验、P01-T09 跨端验收，经项目总控独立移植与复核完成：分类视图按领域→分类共置普通记录、已填固定记录与未填固定模板；已有固定项按记录实际 `categoryId` 展示和更新，未填模板按模板分类展示，不再因编辑静默搬家；今日与全部 Markdown 同样按记录分类导出。320×500、320×844、390×844、1280×720 无横向滚动，关键移动触控目标至少 44px。37/37 单测、9/9 E2E、PWA、生产构建、设计校验和差异检查通过；证据见 `output/playwright/ln-033-category-390.png`、`output/playwright/results.json`，实施说明见 `docs/2026-08-13-LN-033固定记录分类归属实施说明.md`。 |
| BASE-001 | MVP 数据层基线通过 | 2026-08-12：`npm test` 通过 |
| BASE-002 | MVP 生产构建通过 | 2026-08-12：`npm run build` 成功，静态生成 `/` 与 `/templates` |

## 已知风险与决策记录

1. **工作区风险**：当前分支领先远端且工作区非干净。任何提交、重构或依赖清理都需要先核对已有改动，不能把不相关变更混入同一任务。
2. **真实使用证据缺口**：自动化已覆盖数据保护、移动端核心交互、导出恢复、PWA 离线与升级，但 14 天自然使用尚未开始；测试记录和种子数据不能代替 LN-021。
3. **产品范围决策**：先验证个人记录主线，再决定 AI、自动化、同步或社交；不能以新功能数量替代北极星指标。
4. **AI 分支范围风险**：T02 已从调研扩张为 DeepSeek + SQLite 原型，继续隔离在 worktree；在产品、架构、隐私、数据出设备与最小合入拆分评审前不得并入主线。
5. **影响力验收缺口**：可靠演示基线和结构化案例已具备；仍需 2 次真实展示/评审及至少 1 项有效影响证据，不能把自我评价或无反馈展示计为 KR 完成。
6. **固定记录链路已收口**：LN-027～029 已完成首屏布局、旧模板兼容、保存反馈和跨端验收；期间发现并修复旧 `.next` 长驻进程导致的运行版本漂移，以及移动端 13px 输入和 30×30px 触控目标。后续若再次出现“源码正确、实际页面旧”的现象，必须先核对真实端口进程与构建版本。
7. **附件恢复容量边界**：便携包自身最多 60 MiB、附件静态总量最多 50 MiB；为保护当前图片，恢复不会先删除旧 Blob 腾空间，因此当“现有图片 + 待恢复图片”临时超过 50 MiB 时会明确失败，需后续独立设计跨 localStorage/IndexedDB 的可恢复替换事务后才能放宽。

## 看板维护规则

1. 新任务必须包含：唯一 ID、优先级、用户问题/目标结果、状态、验收标准、依赖、验证方式和产品防退化项；进入“进行中”时补负责人、执行会话和写范围。
2. 一个任务只有在验收标准全部满足并留下测试、构建、截图或文档证据后，才能进入“已完成”。
3. 需求变化先更新本看板，再开始实现；灵感文档不能直接替代正式任务。
4. 每次调度先核对看板、执行任务、工作区和验证证据；原任务未验收时续跑原任务，验收通过后在同一轮从“下一步”顶部拉取第一项 Ready 任务。
5. 发现新风险时优先新增风险卡，不在无关任务中顺手扩大范围。

## 持续迭代协议

项目使用以下闭环，执行者返回不等于任务完成：

```text
Ready → 已分派 → 进行中 → 已返回 → 验证中 → 已验收 → 拉取下一项
                         ↘ 等待 / 返工 ↗
```

1. **Ready 判定**：依赖已完成，目标、范围、验收、验证命令、产品防退化项和权限状态均明确；需要稳定 Codex 任务号时还必须已有合法映射。
2. **不得空转**：Ready 队列非空、没有合法等待、主工作区写槽空闲时，总控必须立即拉取下一项，不能等待下一次心跳或用户催促。
3. **单写多读**：主工作区同时最多 1 个写任务；代码探索、产品研究、日志分析和独立验证可并行使用只读子 Agent。写范围重叠时必须串行，独立写任务需隔离 worktree。
4. **验收交接**：执行者只提交 `returned` 证据；总控依据验收标准独立核对 diff、测试、构建、截图或文档后才能 `accepted`，然后更新看板并立即拉取下一项。
5. **恢复而非复制**：测试失败、限流、`systemError`、宿主截断或意外结束均在原任务恢复；保留现场，不创建重复任务。
6. **合法停止**：仅限等待用户选择/审批/登录/API Key、外部系统或观察期、真实权限/并发冲突、没有 Ready 工作，或项目全部验收完成。
7. **最小纵向切片**：每轮只验证一个用户行为改进，不把新功能、大重构和视觉改版混为一项。

### 产品准入与零回归预算

新能力进入主线前必须回答：改善哪个“快速记→浏览→搜索→编辑/删除→备份恢复→离线”行为；有什么真实痛点证据；默认界面与记录步骤成本；离线/隐私/可逆性；如何验收和移除。无法回答则留在探索区。

一票否决项：静默改写原始记录、核心流程依赖网络/账号/API Key、未授权数据外发、日常记录新增必填步骤、破坏旧备份恢复或现有 E2E。AI、同步账号、社交/匹配、通用待办日历和插件平台默认隔离，未经产品、架构与隐私评审不并入主线。

每轮验收至少保证：`npm run check` 全绿；新增能力有对应回归；移动端快速记录不增加步骤；首页首要 CTA 不增长；常用动作不超过两层；无新增默认弹窗或必填；备份、恢复、离线和旧数据迁移不退化。交互变更另运行 `npm run design:check`。

## Codex 会话总控机制

总控会话负责 O1-KR4 Log Note 影响力成果相关 Codex 会话的命名、状态巡检、异常续跑与并发协调：

- 会话命名：`O1-KR4｜Log Note影响力成果｜00 总控`；`01–49` 为当前执行会话；`90+` 为长期研究或历史会话。
- 自动巡检：Codex 心跳 `Log Note Codex 会话总控巡检`，每 30 分钟运行一次；正常时静默，仅在失败、实际介入或需要决策时报告。

1. 每次巡检以 Codex 项目会话为对象，核对会话状态、最新回合、工作区变化、最近测试/构建结果和验收证据；临时 sub-agent 不作为项目会话统计口径。
2. 以下情况视为异常信号：`systemError`、模型容量或限流、会话意外结束但任务未验收、长时间无进展且没有正常等待原因、失败后无人处理、改动与看板状态不一致、并发写会话冲突或任务偏离验收标准。
3. 出现异常时先读取原会话上下文并复核现场、保留已有改动，再向原会话发送续跑消息；模型容量异常可切换可用模型，不创建重复会话。
4. 等待用户输入、审批、登录、密钥、外部系统变化或明确约定的观察期属于正常等待，不擅自绕过。
5. 任务只有在验收通过后才能结束；执行者结束、代码写完或一次命令成功都不等于任务完成。
6. 新建或发现 Log Note 会话时由总控根据职责统一命名；标题只用于展示，不能替代实际上下文核验。
7. 巡检正常时不制造噪音；只有发生重命名、异常续跑、冲突干预、发现新风险或确需用户决策时才主动报告。
