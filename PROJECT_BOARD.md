# O1-KR4｜Log Note 影响力成果项目任务看板

> 本文件是项目任务、优先级和验收状态的唯一正式来源。`思维风暴/` 只保存探索想法，未进入本看板的内容不代表开发承诺。

- 最后更新：2026-08-17
- 项目归属：O1-KR4｜通过 Log Note 形成可验证的个人影响力成果
- 项目意图：通过把模糊产品目标拆解、实现并形成可展示证据，提升“能把模糊目标拆成可执行系统”的个人影响力
- 对齐状态：已写入 `/Users/kual/Desktop/memory/OKR.canvas`；Log Note 项目是 O1-KR4 的行动载体，O3 保留个人生活记录与评价实践
- 当前阶段：MVP 收口、成果证据与发布基线建设
- 当前判断：产品已达到“可演示 MVP”，尚未达到“可放心长期使用/正式发布”
- 当前分支：`feature/req-20260811-01-log-note-mvp`
- GitHub 状态：2026-08-14 已推送至 `origin/feature/req-20260811-01-log-note-mvp`（截至 `28f1fd4`）；已完成表中的“未提交、未推送”描述保留为各任务验收当时状态

## 会话与 OKR 绑定审计（2026-08-12）

项目内 Codex 会话按稳定编号命名；编号不随 OKR 归属变化而变动。

| 编号 | 会话任务 | OKR 绑定 | 绑定依据 | 当前状态 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| T00 | 项目总控 | O1·KR4 | 统一调度可靠演示、案例、展示与影响证据，直接服务 KR4 交付 | 进行中 | 维护任务边界、验收与异常续跑 |
| T01 | 可靠演示基线、质量门禁与本地数据安全收口 | O1·KR4 | 核心路径可靠性是 KR4“可靠演示基线”的前置条件 | 已完成至 LN-012 | 等待总控按 Ready 队列分派下一项可靠性任务 |
| T02 | 智能分组 AI 原型 | O1·KR4 | 作为可展示的记录分析能力候选，服务可靠演示与案例，但尚未批准合入主线 | 等待凭证；隔离 worktree | 先完成架构、隐私与最小合入拆分评审；未批准前不合并 |
| T03 | 代码质量与提交收口 | O1·KR4 | 测试、构建与可回滚提交保障可靠演示基线 | 历史任务，已完成先前收口 | 仅在主线变更准备提交时按需唤醒 |
| T04 | 产品远景与反馈回路 | 非 OKR | 远期 Agent 自动化、匹配与产品思考不直接满足当前 KR4 验收 | 归档参考 | 不启动实现；后续另行立项 |

治理结论：项目登记与 OKR 真源当前均为 `O1·KR4`，历史 `O3·KR1–2` 显示冲突已解除；项目内任务继续按真实工作使用 `[O1·KR4]` 或符合规范时使用 `[=]`。

## 状态说明

| 状态 | 含义 |
| --- | --- |
| 进行中 | 已有明确负责人和写范围正在推进；主工作区同时最多 1 个写任务 |
| 下一步（Ready） | 依赖、范围、验收、验证和决策状态均明确，可立即拉取 |
| 验证中 | 执行者已返回，等待总控独立核对证据；不等于完成 |
| 等待 | 等待用户决策、审批、凭证、外部状态或观察期，并记录原因 |
| 待规划 | 方向成立，但需要先完成依赖或产品决策 |
| 暂缓 | 当前阶段明确不投入 |
| 已完成 | 已满足验收标准并留下验证证据 |

优先级：`P0` 阻塞可靠使用或当前阶段收口；`P1` 下一阶段核心能力；`P2` 长期探索。

## 项目基线

- Next.js 15 + React 19，账号归属、离线可用、移动端优先的 PWA。
- 已有记录新增/编辑/删除、日期时间线、月历密度浏览、搜索、领域/分类/模板管理、周期模板、中英双语、Markdown/JSON 导出恢复、本地图片附件、便携附件备份和离线壳。
- 未登录时所有业务路由统一停留在账号门禁；登录后文字与结构写入 `log-note:data:user:<auth.uid()>:v1`，图片 Blob 在 IndexedDB 内按同一账号命名空间隔离。文字记录、日计划、结构和设置本地先写、自动 debounce 保存到 Supabase；revision 不一致立即停写并要求选择版本，图片仍只在本地和便携备份中。
- 2026-08-16 Phase 2 当前门禁覆盖设计规范 11/11、116/116 单测、21/21 移动浏览器、PWA production build/installability/已认证离线缓存/persistence/update、生产构建和 `git diff --check`；独立审计发现的 NULL CAS、异步 reconcile 覆盖和云端文字拉取丢图片引用均已修正。初始数据库 migration 与匿名反向权限已验证，CAS 安全纠正 migration 仍待在目标项目执行；真实邮箱/Google 登录后的自动创建、跨设备 revision 冲突和双账号 RLS 仍待真实会话验收。
- 当前分支已提交基线与远端一致，但主工作区保留 LN-043～062、CI/视觉证据、用户文档以及 `research/`、`output/promo/` 未提交资产；尚未创建 PR、合并主分支或部署。

## 进行中

| ID | 优先级 | 任务 | 负责人 | 验收标准 | 备注/依赖 |
| --- | --- | --- | --- | --- | --- |
| LN-036 Phase 2 | P0 | 必须登录、账号隔离缓存与自动版本化云端保存 | P01-T00 主工作区 | 未登录无法进入首页/设置/结构/整理；邮箱密码与 Google 产生真实 `auth.uid()`；每个账号使用独立文字缓存和图片命名空间；新设备登录后优先加载云端，已有认证设备离线可继续；所有修改本地先写并自动 debounce 上传；云端每账号一份完整文字 JSON，省略图片；CAS revision 冲突停写并提供显式选择；旧匿名数据不删除、不静默上传；备份/恢复和 PWA 不退化 | 实现已 Returned，等待目标数据库纠正与真实会话独立验收。独立审计的 3 个 P0 已修：NULL expected revision 不再绕过 CAS；云读取失败先重读、不直接写；reconcile 使用最新本机状态且云端文字合并保留本机图片引用。慢保存续写、operation ID 重试、账号切换异步 generation、图片 owner 捕获和旧图片认领回滚也已收紧；冲突页并列展示双方记录/计划/版本/更新时间，两个替换选择同权。隔离 `output/ln-036-phase2-final` 完整 `npm run check` 已通过：设计规范 11/11、116/116 单测、21/21 浏览器、PWA production build/installability/已认证离线缓存/persistence/update、生产构建与 `git diff --check` 全绿。初始 migration 已执行且匿名读两表/RPC 均 401；仍需执行 `20260816170000_require_expected_revision.sql`，再人工验证真实邮箱与 Google 登录、首次 revision 1、第二设备读回、stale revision 停写和双用户 RLS。未部署。 |

## 验证中

| ID | 优先级 | 任务 | 已有证据 | 待独立核对 |
| --- | --- | --- | --- | --- |
| LN-068 | P1 | 智能整理收敛为单日日期流程 | `/organize` 默认今天，只预览/分析所选自然日的全部普通记录；切日会取消旧分析并清空旧建议。Rework 将原生 `type=date + 今天` 替换为与首页共用的“月日 + 星期 + 展开箭头”日记式触发器和同一开放式月格内核；首页独有横滑与日记/计划联动仍隔离，智能整理选中日期后自动收起并回焦触发器，Escape 也可收起。最新用户视觉复核后删除日期触发器上方重复的“选择日期 / Choose a day”；日期上下文与当天记录之间改用 24px 区块留白，只在“当天记录”与列表内容之间保留弱分隔线，不再用两条等权横线表达不同关系。今日/近 7 天/未分类、搜索、复选框、已选数量、全选/反选与整日批量手动打标签均已移除；建议原因/置信、逐条移除、忽略、按组/全部应用、重算和撤销保留。首页整理入口已上移到“时间 / 分类”共享切换栏右侧，在两个记录视图中保持同一位置；中文显示“整理记录”、英文显示“Organize”，可见按钮高 34px、真实触控区不少于 44px，并按文字宽度收口，不再占用分类内容区或新增第二条分隔线。320px 同栏无换行或横向溢出，共享月历日期格也不少于 44px。纯模型测试覆盖指定日期、跨日排除、周期排除、稳定排序与伪日期；浏览器覆盖固定整理入口、时间/分类切换后入口不消失、无重复日期标题、主区块无横线、列表标题保留细线、共享组件、默认今天、前一天切换、选日收起、Escape、六档无横向溢出、应用/撤销及原文/分类不变。视觉证据新增 `output/ln-068-organize-nav-final/ln-056-floating-switch-{320,390,600,671,768,1280}.png`；最新门禁通过设计规范 11/11、122/122 单测、22/22 浏览器、PWA production build/installability/已认证离线缓存/persistence/update、生产构建与 `git diff --check`。未新增网络 AI、后台分析、schema、数据库或首页主入口；未提交、未推送、未部署。 | 用户在实际记录数据上复核：默认日期、切换任意一天、当天记录是否完整、建议是否符合预期，以及移动端日期控件是否顺手。通过前保持“验证中”。 |
| LN-067 | P1 | Log Note 计划与 Google Calendar 主日历同步 | 设置账号页已接显式 Calendar events 授权；access token 仅在页面内存；本地计划按私有来源标记幂等创建/更新/删除；Google 原有分时与全天事件进入账号隔离缓存并只读展示；同步中再次修改会排队补同步，账号切换/断开不会把旧请求结果写入新账号；Google 缓存不进入主数据、Supabase、备份或 Service Worker。`output/ln-067-google-calendar-final` 通过 320/390/1280 视觉与 DOM 回归，设计规范 11/11、121/121 单测、22/22 浏览器、PWA production build/installability/已认证离线缓存/persistence/update、生产构建与 `git diff --check` 全绿。 | 真实 Google 账号点击“连接并同步”后，人工核对首次创建、Log Note 改名/改时、Log Note 删除、Google 原有事件只读、全天事件、拒绝授权与 API 失败；确认 Google Cloud 已启用 Calendar API，OAuth Web Client origins 包含实际部署域名。通过前保持“验证中”，未部署。 |
| LN-066 | P0 | Google 账号身份基线 | Google Provider、PKCE callback、安全公开密钥校验和不虚报退出已完成并通过先前门禁 | 用户已纠正“本地身份”方向，原本地 profile 验收不再适用；Google 真实同浏览器登录/退出并入 LN-036 Phase 1 最终验收，LN-066 不单独 Accepted |

## 下一步

按表格顺序进入执行。

| ID | 优先级 | 任务 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |

## 等待

| ID | 优先级 | 任务 | 等待原因 | 恢复条件 |
| --- | --- | --- | --- | --- |
| LN-021 | P0 | 实际运行 14 天核心使用验证 | 需要用户选择真实 Day 1，并在日常使用中产生不可伪造的时间与行为证据 | 用户在 `docs/2026-08-13-14天核心使用验证协议.md` 填写 Day 1、设备与环境后开始；Day 7/14 由总控按协议复核 |
| LN-020 | P0 | 完成两次真实展示/评审并记录影响证据 | 需要用户安排真实对象、时间和展示场景，自动化不能替代外部反馈 | 至少完成 2 次真实展示/评审，并在演示包模板记录可核对反馈和至少 1 项影响证据 |

## 待规划

| ID | 优先级 | 任务 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| LN-007 | P1 | 定义 AI 与本地优先架构边界 | 评审文档明确数据是否/何时离开设备、授权方式、密钥与服务端方案、离线降级、删除重算、敏感信息处理和费用边界 | LN-003 |
| LN-008 | P1 | 扩展反馈闭环数据模型 | 区分原始记录、派生观察、干预/实验和结果；AI 不改原文；派生数据有来源且可更新、撤销、删除、重算；完整备份恢复通过 | LN-007 |
| LN-009 | P1 | 实现“已有标签匹配”AI MVP | 只从用户已有标签中选择最多 3 个；低置信度不打标签；用户可一键纠正；结果可撤销重算；纠正可用于后续匹配 | LN-007、LN-008 |
| LN-010 | P1 | 建立最小“记录—调整—验证”闭环 | 用户主动生成阶段回顾；系统只提出一个有证据支持的模式与一个微型调整；可创建 3–7 天实验；结束后完成前后对比并收集准确性、可执行性和有效性反馈 | LN-003、LN-008、LN-009 |
| LN-014 | P1 | 运行 14 天“当前轨迹”产品验证 | 连续记录 14 天；生成当前轨迹；找出一个关键变量；完成 3–7 天实验；收集解释准确性、执行成本和实际效用反馈 | LN-010 |
| LN-034 | P2 | 明确同日同周期模板多记录兼容策略 | 独立决定并验证“拒绝重复 / 合并 / 展示额外历史项”之一；页面可见数量与 Markdown 导出数量语义一致；旧备份、原始正文与恢复安全不受损 | LN-033；当前页面只展示第一条、Markdown 导出全部，暂不扩 schema/restore |
| LN-037 | P1 | 部署 Web 与报告 API 到服务器 | 明确托管平台、域名、Node 版本、环境变量、数据库迁移、健康检查、日志脱敏、HTTPS、回滚和发布证据；生产发布前完整门禁通过，且获得外部资源与凭据授权 | LN-035；若启用数据库则依赖 LN-036；需用户提供/选择部署平台、域名和生产权限 |

## 暂缓

| ID | 优先级 | 方向 | 重新启动条件 |
| --- | --- | --- | --- |
| LN-015 | P2 | Agent 提醒、日程、待办与有限自动化 | LN-010 的最小反馈闭环已被用户验证有效；具备逐领域授权、暂停、撤销和执行原因记录 |
| LN-016 | P2 | 匿名群体轨迹、搭子匹配、朋友/职业关系 | 个人记录基线、模式识别和历史最佳轨迹已经可靠 |
| LN-017 | P2 | Dating | 在 LN-016 之后单独完成隐私、人身安全、骚扰、欺诈、身份真实性与可解释性评审 |

## 已完成

| ID | 完成项 | 验证证据 |
| --- | --- | --- |
| LN-065 | 下载页亲密性结构与推荐信息收口 | 根据用户截图与既定“字体对比表达重要性、亲密性表达归属”原则，下载页稳定为“记录 → 备份 → 记录结构”三个主区块；下载格式降为记录组内的内缩渐进设置，推荐标签紧贴完整备份标题，浏览器记录/图片状态使用 `12px` 等宽元信息，换设备提示独立为下一行 `14px` 说明。三组标题统一为 `18px` 小标题，跨组间距为 `32px`，删除列表首尾装饰线，只保留相邻操作间的 `1px` 分隔；三组使用语义化 `section` 与 `aria-labelledby`，操作与 disclosure 保持至少 `44px`。正式页面规范已同步。人工复核 `output/ln-065-download-structure-final/ln-065-download-structure-{320,390,600,671,768,1280}.png` 与 `ln-065-download-structure-zh-390.png`：中英文层级清晰、中文截图避开 `160ms` 入场动画、六档无横向溢出，移动与桌面均保持连续纸张而非卡片墙。最终 `env E2E_PORT=3150 E2E_OUTPUT_DIR=output/ln-065-download-structure-final npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿；日期选择器场景固定测试时钟到既有 2026-08-15 基准，消除跨午夜硬编码波动，不改产品日期逻辑。未改 entries/schema、备份格式、Markdown 生成语义、恢复逻辑、账号/网络或快速记录；未提交、未推送、未部署。 |
| LN-064 Rework 3 | 常规设置说明与分隔线继续去冗余 | 根据用户截图直接反馈，常规页删除“界面语言”的显而易见效果说明，也删除“添加到手机主屏幕”的重复效果描述；语言项只保留名称、当前状态和切换操作，安装项只保留真正可执行的“分享菜单 → 添加到主屏幕”提示或安装按钮。常规面板不再重复栏目说明。设置组取消首项上边线和末项下边线，仅在“界面语言”与“添加到手机主屏幕”两个同级设置之间保留一根细分隔线。E2E 反向断言旧说明不再出现，并读取真实 CSS 确认列表顶边 `0px`、首行顶边 `0px`、第二行顶边 `1px`、两行底边均 `0px`。项目总控人工复核 `output/ln-064-rework3-final/ln-064-settings-general-copy-zh-390.png`：移动端无首尾装饰线、无重复文案，单一中间边界与设置归属清晰。最终 `env E2E_PORT=3149 E2E_OUTPUT_DIR=output/ln-064-rework3-final npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿。未改设置行为、entries/schema、备份格式、Markdown 输出、账号/网络或快速记录；未提交、未推送、未部署。 |
| LN-064 Rework 2 | 设置按用户任务与亲密度重组 | 根据用户直接反馈，将按技术概念拆分的常规、Markdown、备份、结构、存储五项收敛为常规、下载、恢复、图片四个用户任务。移动首页只保留一个“设置”标题，四行副信息分别显示当前语言、记录数、恢复动作、图片数量/容量，删除“选择设置”“记录保存在哪里”“本地使用”“保存在此设备”等重复或抽象表达。所有产生文件的动作按亲密度合并进下载页，并分为记录、下载格式、备份、记录结构；完整备份旁只出现一次浏览器数据边界，恢复独立承载导入与替换风险，图片页只管理占用和清理。移动详情只显示顶栏当前任务名，不再与正文重复标题；桌面保持四项 sticky 索引。`#export/#backup/#structure/#storage` 及 `#download/#restore/#images` 均兼容，旧 `#structure` 可直接看到记录结构组。项目总控人工复核 `output/ln-064-ia-final-check/ln-064-settings-mobile-index-{320,390}.png`、六档 `ln-064-settings-general-*` 与恢复页证据；320/390/600/671/768/1280 无横向溢出，触控与移动两级导航未退化。最终 `env E2E_PORT=3148 E2E_OUTPUT_DIR=output/ln-064-ia-final-check npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未改 entries/schema、备份格式、Markdown 输出语义、账号/网络或快速记录流程；未提交、未推送、未部署。 |
| LN-064 | 完整设置页面与本地数据控制台收口 | `/settings` 已收口为连续纸张式设置工作区：桌面保留 sticky 左侧索引与单一活动面板；`≤760px` 改用成熟移动设置两级结构，设置首页一次完整展示常规、Markdown、备份、结构与存储五项，点入后由紧凑 `56px` 顶栏承载当前分区与“返回设置”，详情全宽替换首页而非叠加或横向滚动，并保留 `/settings#structure` 深链。常规页进一步去除“此设备上的 Log Note”“本地数据”“作为 App 使用”等抽象术语，直接回答“能设置什么、记录保存在哪里、怎样添加到手机”：明确记录只保存在当前浏览器且不会自动同步，清除数据或换设备前应先备份，并给出“分享 → 添加到主屏幕”的操作路径。Portable 作为含图片时的推荐完整备份，JSON 明确不含图片文件；恢复独立分组并在最终确认列出记录、计划、结构、模板、Markdown 设置与图片的完整替换范围。损坏/迁移失败进入保护模式，暂停派生导出、编辑和图片清理；只有浏览器确实返回原始 payload 时才允许逐字下载，读取前失败不会伪造 `null` 文件，仍可选择已知 JSON/便携备份恢复。快捷跨面板入口转移键盘焦点，移动进入/返回使用克制 `180ms` 水平转场，reduced-motion 近乎即时，语言、安装、索引行与返回等关键目标不少于 `44px`。项目总控人工复核 `output/ln-064-copy-final/ln-064-settings-mobile-index-{320,390}.png`、`ln-064-settings-general-{320,390,600,671,768,1280}.png`、`ln-064-settings-general-copy-zh-390.png` 与 `ln-064-settings-backup-390.png`：移动首页五项一次可见、中文任务文案无歧义、详情无桌面侧栏与横向 Tab，六档无横向溢出；桌面索引未退化。最终 `env E2E_PORT=3143 E2E_OUTPUT_DIR=output/ln-064-copy-final npm run check` 退出 0：设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿。未新增账号、网络、同步、数据库、主题或清空数据；未改 entries/schema、备份格式、Markdown 输出语义或快速记录流程；未提交、未推送、未部署。 |
| LN-063 | 首页切换层级、分类密度与横滑完整日期铭牌收口 | 顶部统一为日记专属 `Time / Category`，右下统一为两种工作区都可达的 `Diary / Plan`；进入计划不重置记录视图，返回日记恢复先前时间/分类状态。分类视图、周期记录与 `/organize` 保持亲密性层级和 `44px` 控件。首页横滑继续全屏命中，只有松手最终位移严格超过 viewport `20%` 才切换，越界后回撤取消；方向阴影克制衰减，目标提示由本地化完整日期铭牌替代圆球：中文 `2026年9月12日`、英文 `Aug 16, 2026`，按方向左右偏置，纵向锚点稳定、不跟手、不带动页面，并支持 reduced-motion。项目总控在 LN-064 最终 revision 复核 `output/ln-064-final/ln-063-date-card-complete-zh-390.png`、阈值提交/回撤证据及完整浏览器结果；最终 `env E2E_PORT=3143 E2E_OUTPUT_DIR=output/ln-064-final npm run check` 通过设计规范 11/11、99/99 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录流程；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 16：收窄方向阴影与固定中心日期球 | 根据用户纠正，保留 Rework 15 的全屏方向阴影语义，但把深色视觉范围收窄：右滑仍为左边缘最深并向右透明，左滑镜像，渐变在画布约 64% 处完全透明，不再形成大面积浮层。恢复唯一的固定中心日期球：`44×44px`、低饱和石墨半透明表面，只显示目标日或月，始终固定在 viewport 几何中心，不跟手、不吸附、不显示箭头、不带动页面。短拖时阴影和球在 `140ms` 内同步回收，过阈值后两者快速消退并完成既有前后日/月更新。项目总控人工复核 `output/playwright/ln-057-rework16-final/ln-057-rework16-{left-swipe-right-shadow-orb,right-swipe-left-shadow-orb,month-shadow-orb,plan-shadow-orb}-390.png`：阴影范围明显小于 Rework 15，透明端完整，中心球不抢主视觉；日记、展开月历与日计划位置一致。E2E 验证球尺寸 `44–52px`、中心偏差不超过 `1px`、目标标签可见、旧跟手球与边缘组件均不存在。最终隔离 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 15：全屏方向阴影 | 根据用户直接纠正，完整删除日期球、箭头、手柄、胶囊和楔形幕布等独立组件动效。全页横向意图锁定后只渲染覆盖整个 viewport 的线性阴影：右滑时左边缘为黑色并从左向右连续衰减至右边缘完全透明，左滑时镜像；纸面、顶栏、日期、正文、月格、日计划网格与浮动操作均保持静止，不位移、不缩放、不交叉淡化。短拖只让阴影在 `140ms` 内回收，过阈值后阴影快速消退并完成既有前后日/月更新。项目总控人工复核 `output/playwright/ln-057-rework15-final/ln-057-rework15-{left-swipe-right-shadow,right-swipe-left-shadow,month-shadow,plan-shadow}-390.png`：左右渐变方向正确、透明端完整、黑端未遮断内容，日记、展开月历和日计划一致；DOM 反向断言无 `.home-swipe-orb` 与 `.home-swipe-edge-cue`。保留全页命中、`pan-y`、阈值回收、点击抑制、选区清除、输入/弹窗/月轨隔离、键盘和 reduced-motion。最终隔离 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 14：参考视频式边缘幕布与暖石墨日期球 | 根据用户提供的视频 App 截图，移除 Rework 13 的小胶囊手柄和 Rework 12 的高饱和蓝色日期球。全页横向意图锁定后，在拖动方向对侧 viewport 边缘显露 `68×276px` 的窄高深色半透明楔形幕布：向右拖时左侧显示上一项白色箭头，向左拖时右侧显示下一项白色箭头；幕布贴边、横向侵入受限，不再形成占据半屏的大黑三角。日期球改为低饱和暖石墨渐变，去除蓝色残影、强高光和重 halo，继续承载目标日/月并吸附到顶部日期。项目总控对照用户原图人工复核 `output/playwright/ln-057-rework14-final/ln-057-rework14-{left-swipe-right-curtain,right-swipe-left-curtain,month-curtain,plan-curtain,graphite-orb-absorb}-390.png`：左右镜像方向正确，箭头清晰且弱于内容，日记、月历和计划一致；保留全页命中、日期/月语义、`pan-y`、点击抑制、选区清除、输入/弹窗/月轨隔离和 reduced-motion。最终隔离 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 13：系统式横滑边缘提示 | 用户认可日期吸附球，但否定自创蓝色短条，要求复用成熟手机交互。最终采用 predictive-back 边缘手柄：全页横向意图锁定后，在拖动方向的对侧边缘部分滑入 `24×40px` 圆角手柄；向右拖时左侧显示上一项左箭头，向左拖时右侧显示下一项右箭头。手柄使用纸面 tonal surface、细边框和轻阴影，随进度只做轻微显露与缩放；达阈值后先收束，再由日期球完成吸附，短拖则两者同步回收。未改变左滑下一日/月、右滑上一日/月的既有语义，也未影响 `pan-y`、点击抑制、文本选区清除、输入/弹窗/月轨隔离和 reduced-motion。项目总控人工复核 `output/playwright/ln-057-rework13-final/ln-057-rework13-{left-swipe-right-edge,right-swipe-left-edge,month-edge-cue,plan-edge-cue}-390.png`：边缘手柄部分裁切、方向明确且弱于日期球，日记、月历、计划一致，无蓝色进度条或第二主动画。E2E 新增左右侧、标准 chevron、部分裁切、回收消失和跨表面断言；最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 12：高级日期吸附球 | 根据用户认可的“纸面稳定、球负责方向、日期负责结果”，保留整页横滑命中范围，但取消 Rework 11 的整页搬移。横向锁定后在手指上方生成唯一的蓝色日期球：小幅拖动以哑光蓝墨滴跟手并轻微拉伸，达阈值显示目标日或月，完成后以 `190ms` 有重量地吸附到顶部日期；未达阈值则 `180ms` 回收消失。吸附阶段只让日期相关内容克制交叉淡入，页面、顶栏、月格、日记/计划内容和浮动操作保持稳定；去除玻璃高光、硬外圈与夸张发光，只保留单层柔和 halo。纵向滚动继续使用 `pan-y`，点击抑制、文本选区清除、输入/弹窗/月份轨道隔离和 reduced-motion 均保留。项目总控人工复核 `output/playwright/ln-057-rework12-final/ln-057-rework12-{orb,month-orb,plan-orb}-{drag,absorb}-390.png`：日记、展开月历与日计划三种表面动效一致，目标标签可读，无玻璃弹珠感、页面漂移或操作遮挡。E2E 同时修复 reduced-motion 下瞬时回 idle 的方向取证，最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 11：全屏横滑与整页切换动效 | 根据用户直接纠正，将横滑命中面从日期标题扩大到整个首页可视工作区。收起状态全屏横滑切前后日，展开状态切前后月；日记与计划均可从当前内容区发起。应用顶栏、日期身份、月格或当前日记/计划内容及上下文浮动操作使用同一位移、透明度与轻微缩放，形成一张纸页同步跟手；未达阈值以 `230ms` 克制回弹，达阈值后当前页约 `160ms` 退出、新页从相反方向约 `230ms` 带入。横向锁定后清除文本选区，纵向滚动保持 `pan-y`；输入、弹窗与月份快捷轨道保留独立交互，滑动后的点击被抑制，reduced-motion 近乎即时。项目总控人工复核 `output/playwright/ln-057-rework11-final/ln-057-rework11-fullscreen-drag-390.png`、`ln-057-rework11-fullscreen-month-drag-390.png` 与 `ln-057-rework11-plan-fullscreen-drag-390.png`：整页元素同向等距移动，无独立日期漂移、文本误选或控件脱离。E2E 覆盖短拖回弹、日/月方向、计划内切日、动作同步、非手势选日、六档无溢出及日计划内部滚动/FAB。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 10：日记/计划命名与箭头去冗余 | 根据用户最新截图纠正，上层内容模式统一收敛为英文 `Diary / Plan`、中文“日记 / 计划”；完整移除日期标题下方可见的前后日/月箭头组件及占位。收起状态继续横滑切日，展开状态继续横滑切月；点击日期展开月格、日期单元格、月份轨道和键盘网格导航继续提供明确的非手势路径。移除箭头后 341–700px 顶栏回到单行，320px 使用日期行 + 工具行保护完整日期与 44px 触控，701px 以上保留品牌行与共享日期行。项目总控人工复核 `output/ln-057-rework10-playwright-final/ln-057-rework10-diary-plan-no-arrows-{320,390,600,671,768,1280}.png`、`ln-057-rework10-expanded-{320,390,600,671,768,1280}.png` 与 `ln-057-rework10-plan-{390,1280}.png`：无箭头空白、无横向溢出，日记/计划复用同一日期 DOM，390px 展开计划仍保留可用小时网格且新增计划按钮完整可见。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-062 | 固定记录标题工具簇降噪 | 项目总控在 LN-057 Rework 10 同一最终 revision 独立复核：完成数使用 `12px`、`500` 字重、等宽字体与弱色，“调整”使用 `14px`、`500` 字重并提供至少 `44px` 触控和明确键盘焦点；二者同一行、同基线、紧邻排列并显著弱于 `22px` 固定记录标题。六档视觉证据见 `output/ln-057-rework10-playwright-final/ln-062-fixed-header-tools-{320,390,600,671,768,1280}.png`；E2E 逐档验证层级、颜色、基线、顺序、触控和键盘可达，完整 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA、生产构建与 `git diff --check`。未改记录数据、填写流程、备份或日期导航，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 9：移动端日期替代品牌标题 | 根据用户截图，320–700px 的唯一共享日期标题直接占用原 `Log Note` 品牌位置，记录工作区不再重复日期；320px 将语言放在第二行左侧、前后导航放在右侧，390–700px 语言与搜索/结构/设置保持首行，日期、星期和展开箭头均完整可见。701px 以上继续显示品牌第一行与同一日期 DOM 的第二行。展开/收起、收起切日/展开切月、横滑、Escape 回焦、选日保持展开、记录/日计划共享状态和日计划 viewport 均保留。项目总控人工复核 `output/ln-057-rework9-playwright-final/ln-057-rework9-mobile-date-header-{320,390,600,671,768,1280}.png`、`ln-057-expanded-context-{320,390,600,671,768,1280}.png` 与 `ln-057-shared-day-plan-390.png`；六档无横向溢出，语言与工具继续不少于 44px。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建和 `git diff --check` 全绿。未改数据/schema、备份、Markdown、账号、网络或快速记录，未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 8 | 根据用户直接纠正，把共享日期导航收敛为日记式标题：月/日使用 Serif 主标题，星期使用更小 Sans、弱色和 `8–16px` 间隔；收起与展开保留同一标题 DOM，彻底移除可见月份重复标题、“记录日/记录”统计和活动摘要。月格仍直接展开在标题下，唯一箭头、收起切日/展开切月、横向滑动、键盘、Escape 回焦、月份轨道、记录/计划信号点、记录/日计划共享上层 DOM 与日计划 action dock 语义均保留。项目总控人工复核 `output/ln-057-rework8-playwright-final/ln-057-collapsed-context-390.png` 与 `ln-057-rework8-diary-date-{390,1280}.png`：日期为第一层级、星期明确从属，无重复月份/统计、无横向溢出或控件挤压。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿；未改数据/schema、备份、快速记录、账号或网络，未提交、未推送、未部署。 |
| LN-061 | 固定记录前置归属标签弱化 | 项目总控独立复核六档截图与改动边界：领域标签保留在记录名前方，收敛为 `12px`、`500` 字重、`--faint` 弱色、`2px 6px` 内边距和极浅无描边胶囊；记录名继续为 `16px` 主信息，原网格、48px 行高、填写和展开行为不变。截图见 `output/ln-061-fixed-scope-tags-final/ln-061-fixed-scope-tags-{320,390,600,671,768,1280}.png`；同 revision 完整 `npm run check` 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`。确认未修改日期组件、数据/schema、备份、快速记录或日计划 action dock；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 7 | 根据用户截图把所选日期、展开箭头、当前月份、月度记录日/记录数与唯一前后导航收进同一顶部日期组件：收起时只保留紧凑日期主行，展开时在同一个 disclosure 内显示月份与安静的摘要；唯一箭头组紧邻该 disclosure，按现有语义收起切日、展开切月。月格从组件下方直接开始，不恢复内部标题、统计或第二组箭头；滑动、阈值回弹、键盘、Escape 回焦、月份轨道、记录/计划信号点、记录/日计划共享 DOM 与日计划 action dock 语义均保留。项目总控复核 `output/ln-057-rework7-playwright-final/ln-057-rework7-fused-date-context-{390,1280}.png`，六档无横向溢出、日期与箭头间距不超过 24px、关键触控不少于 44px。最终隔离 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check` 全绿；未改数据/schema、备份、快速记录流程、账号或网络，未提交、未推送、未部署。 |
| LN-058 | 全项目亲密性间距、字体层级与分类页节奏收口 | 项目总控独立核对语义字体×亲密性表、CSS 边界、六档截图与 `output/ln-058-playwright-final/ln-058-spacing-evidence.json`：390px 分类标题→首项 `4px`、相邻分类 `20px`、内容→领域分隔 `24px`、下一领域标题内距 `29px`；搜索计数→结果约 `8px`、正文→辅助信息约 `4px`；结构相邻分类 `8px`；附件文件名→元信息 `4px`；智能整理标题→说明 `8px`、说明→范围 `16px`；设置标题→说明 `8px`、说明→内容 `16px`、区块内距 `24px`。确认未改变数据/schema、备份、记录流程、融合日期导航或日计划 action dock。最终隔离 revision 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA、production build 与 `git diff --check`。剩余风险仅为不同系统中文字体回退的轻微字面宽度差异；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 6 | 在唯一共享日期导航上增加横向手势：收起时左滑下一日/右滑上一日，展开时同手势切前后月；拖动跟手并轻微淡化，达到 `56px` 或快速短划阈值后以 `180ms` 方向动效落位，小幅移动回弹。项目总控独立核对 `touch-action: pan-y`、滑动不误触展开、可见箭头与键盘路径保留、reduced-motion `1ms`，并复核 `output/ln-057-rework6-playwright-final/ln-057-rework6-swipe-drag-390.png` 与 `results.json`；动效克制、无新增遮挡或信息结构。最终隔离 revision 通过设计规范 11/11、97/97 单测、20/20 浏览器、PWA production build/installability/offline/persistence/update、生产构建与 `git diff --check`；未改数据/schema、备份、快速记录步骤、唯一箭头结构或日计划 action dock，未提交、未推送、未部署。 |
| LN-060 | 日计划隐藏记录视图 action dock | 根据用户直接反馈，日计划激活时不再渲染记录专用的“时间 / 分类”胶囊、导出和新增记录 action dock；日计划自己的新增计划 `+` 保留，上方共享“查看记录 / 日计划”仍是清晰返回出口。E2E 反向断言日计划 `.view-switch` 数量为 0，并真实通过“查看记录”返回时间记录；与 LN-057 Rework 4 同一最终 revision 完整 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器回归、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未改记录视图中的浮动切换、数据或快速记录流程；未提交、未推送、未部署。 |
| LN-057 | Google Calendar 式折叠日期上下文 Rework 4 | 将原先分离的“月份入口、所选日期、前后日导航”融合为唯一共享日期导航组件：左侧所选日期本身承担月历展开/收起，箭头明确状态，右侧保留前后日与桌面回到今天；不再渲染独立月份行或第二日期行。层级收敛为“应用顶栏 → 融合日期导航 → 可选月格与月份轨道 → 记录/日计划切换 → 当前内容”；选日和切换记录/日计划均保持月历展开与同一 DOM，Escape 收起并回焦日期按钮。320/390/600/671/768/1280 无横向溢出且日期按钮不少于 44px，390×844 展开日计划仍显示约 3 个小时行，新增计划按钮完整可见。视觉证据见 `output/playwright/ln-057-collapsed-context-390.png`、`ln-057-expanded-context-{320,390,600,671,768,1280}.png`、`ln-057-shared-day-plan-{390,1280}.png`；最终 revision 完整 `npm run check` 退出 0：设计规范 11/11、97/97 单测、20/20 浏览器回归、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未改 entries、planBlocks、schema、备份、Markdown、账号、网络或快速记录流程；未提交、未推送、未部署。 |
| LN-059 | 固定记录标题区移除重复说明 | 根据用户截图删除标题下方“数值直接填写，表单在当前列表展开”及 `0/6` 下方重复的“剩余 6 项”；可见区域保留固定记录标题、完成/总数和调整入口，完整完成/剩余语义继续通过进度元素的中英文无障碍标签提供，不改内联填写、保存、数据、备份或快速记录步骤。移动 E2E 新增反向断言，验证两段冗余文案不可见且无障碍进度仍存在；视觉复核 `output/playwright/ln-052-compact-home-{600,671,768}.png`。隔离端口 `3157` 完整运行 `npm run check`：设计规范、97/97 单测、20/20 移动 E2E、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未提交、未推送、未部署。 |
| LN-056 | 将“时间 / 分类”视图切换移至右下快速操作区 | 顶栏移除常驻视图切换，轻量胶囊固定在右下新增/导出操作上方并与主操作右对齐；日计划仍可直接返回时间或分类，激活态、键盘聚焦、语言持久化、至少 44px 触控和 320/390/600/671/768/1280 无横向溢出均由浏览器回归覆盖。项目总控逐张复核 `output/playwright/ln-056-floating-switch-{320,390,600,671,768,1280}.png`，并在同一稳定 revision 独立运行 `npm run check`：设计规范、97/97 单测、20/20 移动 E2E、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未提交、未推送、未部署。 |
| LN-055 | 基础智能整理与日常记录分类工作台 | 分类视图提供唯一的次级入口，独立 `/organize` 在桌面使用双栏、移动使用选择→分析→确认分步流程；支持今日/近 7 天/未分类、搜索、多选、已有标签本地规则建议、原因/置信、低置信保持不分类、显式应用、忽略、重算、撤销与用户显式新建标签。建议/结果只在页面会话中，只有用户确认后通过既有数据写入更新 tags；最终标签稳定去重且最多 3 个、已有标签优先，`entry.content` 与 `categoryId` 保持不变；不接账号、网络、API Key、真实模型或后台扫描，`/organize` 纳入离线壳。项目总控核对专项 7/7、六档视觉 `output/playwright/ln-055-organize-{320,390,600,671,768,1280}.png`、移动建议操作至少 44px，并在同一稳定 revision 独立运行 `npm run check`：设计规范、97/97 单测、20/20 移动 E2E、PWA production build/installability/offline/persistence/update 与 `git diff --check` 全绿。未提交、未推送、未部署。 |
| LN-054 | Google Calendar 式月概览交互收口 | 月模式隐藏首页重复大日期区，以紧凑月份轨道、开放式 7 列月格、选中日圆形强调和记录/计划信号点完成浏览；点击日期留在月概览并更新当天上下文，“查看记录/计划这一天”分别进入既有记录和日计划链路。键盘方向键/PageUp/PageDown、月份轨道、前后月按钮和至少 44px 触控目标均有 DOM 回归；320/390 首屏同时显示完整月格与上下文起点，600/671/768/1280 保持单一阅读簇且无横向溢出。视觉证据见 `output/playwright/ln-054-google-month-{320,390,600,671,768,1280}.png`。项目总控在当前 revision 独立运行 `npm run check`：设计规范、90/90 单测、19/19 移动 E2E、PWA production build/installability/offline/persistence/update 和 `git diff --check` 全绿；沙箱内首次监听 3141 因 `EPERM` 未进入产品断言，获准本地重跑后退出码 0。未接 Google API，未改 schema、备份、Markdown、账号、网络或快速记录步骤；未提交、未推送、未部署。 |
| LN-052 | 600–800px 紧凑桌面/平板响应式收口 | 600/671/768 使用单行顶栏、36px 日期标题与受限的时间线/固定记录/值列阅读簇，320/390 手机和 1280 桌面保持原有层级；真实 DOM 断言覆盖无横向溢出、固定行与输入列宽度，视觉证据见 `output/playwright/ln-052-compact-home-{600,671,768}.png`。项目总控在唯一 Next 进程下独立重跑 `npm run check`：设计规范、90/90 单测、19/19 移动 E2E、PWA 安装/离线/持久化/升级、生产构建和 diff check 全绿；首次沙箱内 `3141` 绑定因 `EPERM` 未进入断言，获准本地重跑后退出码 0。未提交、未推送、未部署。 |
| LN-051 | 代码减量、数据写入与 Next/PWA 边界收口 | 首页派生模型、附件草稿生命周期和记录视图按完整职责拆出，后续复核把 `page.js` 继续降至 464 行；首页路由样式按月历、日计划、时间线和固定记录拆分且均低于 400 行。删除无消费者导出、旧结构 CSS、测试专用计划函数、重复日期规则和重复翻译键；统一下载、日期显示、持久化与质量门禁边界。设置和结构页只在 localStorage 写入成功后更新 UI，损坏 payload 仅在确认恢复真实写入成功后解除保护；带图片草稿不能切到不支持图片的模板，失败的 Blob 清理保留重试 ID 并给出设置页清理提示，容量错误使用稳定 code；便携恢复以单个 IndexedDB 事务写入最终附件集合，旧库已占满 50 MiB 时仍可恢复最终合法的小型备份，localStorage 失败继续回滚新 Blob。根布局保留一次性安装提示，Service Worker v6 隔离 API/RSC，查询和尾斜杠路由离线命中对应 pathname，真实未缓存 script 失败不返回 HTML；移动 dev 与 PWA build 使用隔离的 Next `distDir`，PWA runner 成为质量门禁唯一 production build owner，避免重复构建和产物互相破坏。完整门禁通过 90/90 单测、19/19 移动 E2E、PWA、单次生产构建、设计检查和 diff check；静态路由体积为首页 147 kB、设置 134 kB、结构页 153 kB。未提交、未推送、未部署。 |
| LN-050 | 顶栏语言入口与轻量胶囊视图切换 | 顶栏在 320/390/1280 下保持语言入口先于胶囊式时间/分类切换，同一行内无横向溢出；激活与未激活状态、薄边框、至少 44px 触控和相邻搜索/结构/设置入口均由浏览器断言覆盖。视觉证据见 `output/playwright/ln-050-topbar-{320,390,1280}.png`；同 revision 通过完整质量门禁。未提交、未推送、未部署。 |
| LN-049 | 日计划新增入口去歧义 | 日计划保留“点击空白时段直接创建并按 15 分钟吸附时间”的画布交互，同时仅保留一个固定在当前 viewport 右下的显式新增计划 `+`；日计划隐藏全局新增记录和导出操作，返回时间视图后快速记录与导出恢复，并真实保存一条普通记录。17/17 移动 E2E 覆盖空白时段创建、显式入口与返回主链路。未提交、未推送、未部署。 |
| LN-047 | 月历内本地日计划 viewport Rework | 日计划成为顶栏以下的剩余 viewport 工作区，页面本身不滚动，小时网格独立滚动，紧凑日期、模式切换和前后日导航保持可见；计划块支持重叠分列，新增 FAB 在 320×844、390×844、1280×720 均完整位于 viewport 内。非 fullPage 证据见 `output/playwright/ln-047-google-day-plan-{320,390,1280}.png`；完整门禁通过。未提交、未推送、未部署。 |
| LN-048 | 固定单值记录移除冗余保存勾号 | 根据用户对无意义按钮的直接反馈，删除固定单值输入右侧仅重复触发失焦的勾号和对应样式；输入框继续在真实失焦时自动保存，Enter 仍通过表单触发失焦，保存继续使用既有 Toast，不增加替代图标或操作步骤。失焦处理改为读取输入框当前值，避免快速输入后立即 Enter 时 React 草稿尚未提交而读取旧值。移动 E2E 断言 320/390 输入字号至少 16px、三档均无按钮且输入占满值列，并真实覆盖失焦保存、Enter 更新和清空删除。当前 revision 通过 90/90 单测、15/15 移动 E2E、PWA、生产构建、设计校验和 diff check；视觉证据见 `output/playwright/ln-041-category-hierarchy-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-046 | 日期标题月历入口调整 | 根据用户截图把日期导航中间的“今天/日期”从浏览器原生日期输入改为应用内月历唯一入口，并移除下方视图切换中的重复“月历”按钮；下方只保留“时间/分类”，左右箭头继续逐日切换，月历日期继续返回既有时间视图。不新增记录步骤、数据字段、网络、账号或备份变化。日期按钮提供中英文无障碍名称、激活状态、键盘焦点和至少 44px 触控目标；移动 E2E 覆盖“点日期入口 → 月历打开 → 无重复入口 → 选日回时间视图”，PWA 离线月历也使用同一入口。85/85 单测、14/14 移动 E2E、PWA 离线月历/持久化/升级、生产构建、设计校验和 diff check 全绿；视觉证据见 `output/playwright/ln-044-calendar-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-045 | 分类视图排版四原则收口 | 按亲密性、对齐、重复、对比完成日期头与分类记录簇收口：移动日期保持单行并与视图切换分行；领域→分类→首项间距分别稳定在既定范围；字段标签和值共享阅读轴，同类字段行统一分隔节奏，可展开行不再例外；320/390/1280 无横向溢出。后续按用户计数反馈移除领域与普通记录分类的冗余数量，含可见周期模板的分类改为显示完成/总数，未完成状态保留 `0/1`，并提供中英文无障碍标签；不改记录数据、正文、备份或快速新增步骤。当前 revision 通过 90/90 单测、15/15 移动 E2E、PWA、生产构建、设计校验和 diff check；普通分类与周期比例证据见 `output/playwright/ln-033-category-390.png`、`output/playwright/ln-045-layout-principles-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-044 | 月历记录浏览视图 | 用户直接授权后新增纯本地“月历”第三浏览视图：按月展示每天的全部记录数量、当月活跃记录日与总记录数，支持上/下月、相邻月日期、方向键、Home/End、PageUp/PageDown，并从日期直接进入既有时间视图；英文周日起始、中文周一起始。实现由独立纯函数 `src/lib/calendar-model.mjs` 和可移除视图组件组成，不改 entry、schema、备份、Markdown 或快速新增步骤，不引入日程、提醒与网络。真实浏览器发现 320px 日期格初版仅约 39.6px，已改为移动端贴边网格并验证日期格及翻月按钮均至少 44px；桌面阅读簇限制为 700px，浮动新增/导出不遮挡日期。专项 6/6、全量 85/85 单测、14/14 移动 E2E、完全离线打开月历的 PWA 安装/持久化/升级回归、生产构建、设计校验和 diff check 全绿；证据见 `output/playwright/ln-044-calendar-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-043 | 首页分类视图无意义留白压缩与响应式密度收口 | 根据用户对扫描路径和空隙的反馈，在保持“领域 → 分类 → 指标 → 值”语义字号与逐级缩进的前提下压缩首页日期头、领域、分类、记录行、固定记录和空状态的垂直节奏；修复移动端通用 `.fixed-records` 规则覆盖嵌入式列表、为每个分类重复增加上下内边距的根因。桌面端指标/值列限制在 620px 阅读簇内，避免两端无意义拉扯；320×844、390×844、1280×720 均无横向溢出，并新增领域到分类 `8–16px`、分类到首项 `0–8px`、分类间 `12–20px` 和指标到值 `24–220px` 的真实布局断言。当前工作区通过 79/79 单测、13/13 移动 E2E、PWA 安装/离线壳/持久化/升级、隔离生产构建、设计校验和 diff check；视觉证据见 `output/playwright/ln-043-compact-density-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-042 | 本地离线图片附件主工作区纵向闭环 | P01-T14 的隔离实现经 P01-T00 三方差异核对后精确移植，保留 LN-040 时间线对齐和 LN-041 语义层级资产：普通自由文本记录可在“更多”中添加一张 JPEG/PNG/WebP，Blob 仅存 IndexedDB，v3 状态只保存引用；单张 5 MiB、总量 50 MiB，不使用 data URL、远程图片、数据库或上传。时间/分类视图只从本地 `blob:` URL 展示，缺图保留正文并安全占位；设置页提供占用/孤儿清理和带 SHA-256 的 `.lnbackup` 便携导出恢复，损坏、截断和 localStorage 写入失败均不覆盖当前数据并回滚新 Blob。主工作区同 revision 通过 79/79 单测、13/13 移动 E2E、PWA 完全离线上传/刷新/展示、生产构建、设计校验与 diff check；证据见 `output/playwright/ln-042-attachment-{320,390,1280}.png`、`output/playwright/pwa/ln-042-offline-image.png`、两份 results JSON 与 `docs/2026-08-14-LN-042本地离线图片附件实施说明.md`。未提交、未推送、未部署。 |
| LN-041 | 全局字体层级与分类视图阅读顺序重构 | 按用户视觉反馈建立 display / 页面标题 / 区块标题 / 分类标题 / 正文 / 标签 / 元数据语义字号，并统一正文用 Sans、展示与章节标题用 Serif、时间与真实数值用 Mono；移除分类阅读页无语义竖线，改为领域→分类→具体指标的字号级差和逐级缩进，数量紧邻所属标题并带“类/项”单位，数值置于指标之后且不抢主层级。同步整理模板、设置、搜索、编辑器、附件区和首页时间线字重与字体角色；全项目 CSS 无 8–11px 可见文字或未加载字重，模板管理中表达拖拽树关系的竖线保留。新增中文分类层级、模板层级、设置层级、附件标题/操作/文件名/元数据层级、三档无横向溢出和首页 44px 触控回归；当前工作区 79/79 单测、13/13 E2E、PWA 安装/离线/文字与图片持久化/升级、生产构建、设计校验和 diff check 通过。视觉证据见 `output/playwright/ln-041-category-hierarchy-{320,390,1280}.png`。未提交、未推送、未部署。 |
| LN-040 | 首页时间线记录行对齐修复 | 按用户截图修正时间线记录行的列与纵向基线：时间文字与“领域 · 分类”元信息顶端对齐，正文列在移动端和桌面端与固定记录标签列共用同一间距节奏；不改记录数据、正文、交互步骤或固定记录行为。新增移动端真实布局断言，验证文字起点和跨模块列间距；73/73 单测、11/11 移动 E2E、PWA 安装/离线/持久化/升级、生产构建、设计校验和 diff check 通过。视觉证据见 `output/playwright/ln-039-timeline-390.png`。未提交、未推送、未部署。 |
| LN-039 | 普通记录 Markdown 基础富文本与安全展示 | P01-T13 在用户直接重新放行后续用原任务完成：普通自由文本选区支持可逆粗体、斜体及正文/标题/副标题整行样式；时间视图、分类视图和搜索结果以 React 文本节点安全渲染标题、强调、列表、任务列表和换行。`entry.content` 继续逐字保存 Markdown 纯字符串，不使用 `dangerouslySetInnerHTML`，HTML、URL、图片与破损语法保持字面文本且不触发网络请求；搜索、JSON backup/restore、Markdown export、离线链路不变。项目总控独立复核专项 12/12、全量 72/72 单测、11/11 移动 E2E、PWA 安装/离线/持久化/升级、生产构建、设计校验和 diff check；320×844、390×844、1280×720 编辑器及 390×844 渲染证据见 `output/playwright/ln-039-*.png`。本项未把 Markdown 图片语法伪实现为联网图片；用户选择的本地离线附件已由 LN-042 独立闭环。未提交、未推送、未部署。 |
| LN-038 | 普通记录 Markdown 列表连续输入 | P01-T12 returned 后经项目总控独立验收：普通自由文本 `textarea` 中，`- ` 与 `- [ ] ` 列表支持 Enter 同类续写和空项退出；Shift+Enter、修饰键与 IME 不触发，支持光标中段、选区、中文/emoji 和 Chromium 原生撤销。实现以独立纯文本模块 `src/lib/markdown-list-input.mjs` 接入现有 `entry.content` 字符串链路，无新增 CSS、工具栏、schema/restore 或旧正文转换。项目总控重新通过专项 8/8、全量 60/60 单测、移动 10/10 E2E、PWA 安装/离线/持久化/升级、设计校验与 diff check；320×844、390×844、1280×720 证据见 `output/playwright/ln-038-composer-*.png`。未提交、未推送、未部署。 |
| LN-035 | 本地可运行的报告下载 API 纵向闭环 | P01-T11 returned 后经项目总控精确移植并独立验收：同源 `POST /api/reports/download` 支持 Markdown 单日/范围/全部、完整备份 JSON 与不含 entries 的结构 JSON；调用方显式提交 state，服务端不读取 localStorage、不保存或上传数据。请求体按流累计，超过 1 MiB 立即取消并 413；生成结果限制为 2 MiB UTF-8，Markdown 在既有生成器前做保守预算，所有格式生成后精确校验；成功/错误均提供受控类型、文件名、private no-store、nosniff 与 UTF-8 Content-Length。主工作区独立通过 15/15 专项、52/52 单测、9/9 移动 E2E、PWA 安装/离线/持久化/升级与 API 不入缓存、生产构建、设计校验和 diff check；真实 3111 HTTP 以合成 fixture 验证 200 Markdown、中文字节长度、405、403、输入 413 与输出放大 413。说明见 `docs/2026-08-14-LN-035本地报告下载API验证说明.md`。未连接数据库、未部署、未使用真实数据。 |
| LN-001 | 建立项目任务看板与统一任务编号 | 本文件已建立状态、优先级、验收标准、依赖和维护规则 |
| LN-004 | 结构拖拽排序与体验收口 | `O1-KR4｜Log Note影响力成果｜01 MVP收口` 完成 27/27 单测、设计规范校验、生产构建及桌面/移动端浏览器验收；尚未提交 |
| LN-013 | 本地数据保护 | `T01` 完成：解析/迁移失败保留原 payload 且禁止自动覆盖；30/30 测试、设计校验、生产构建与差异检查通过；恢复面板 UI 尚未纳入范围 |
| LN-002 | 文档与实现口径校准 | `T01` 完成：README、product 与结构示例已改为领域→分类→模板、14 条种子、周期模板、导出/恢复和本地优先边界；30/30 测试、生产构建与差异检查通过 |
| LN-005 | 核心浏览器 E2E 回归 | `T01` 完成：`npm run test:e2e` 在移动端缓存 Chromium 上 4/4 通过，覆盖线性记录 CRUD/搜索、结构化必填、周期记录、模板排序与结构导出、JSON 恢复和 Markdown 导出；失败保留截图与 trace |
| LN-006 | 统一质量门禁与 CI | `T01` 完成：`npm run check` 顺序通过 30/30 单测、4/4 移动端 E2E、生产构建和差异检查；PR/push 工作流在 Playwright 1.61.1 Chromium 容器中执行 `npm ci && npm run check` |
| LN-011 | 本地备份与安全恢复防线 | `T01` 完成：设置页显示备份状态、浏览器/设备/域名/端口风险及恢复校验说明；恢复先验证再确认替换，损坏文件不改写当前数据；31/31 单测和 4/4 移动端 E2E 覆盖空备份、旧版本、重复 ID、损坏文件与恢复后内容保留，`npm run check` 通过 |
| LN-012 | 真实 PWA 安装、离线与升级验证 | `e2e/run-pwa.mjs` 在生产构建与移动端 Chromium 中验证：在线后 `/`、`/templates`、`/templates?focus=periodic`、`/settings` 与 `/settings/` 均可离线打开，离线新增后刷新保留；manifest 192/512 PNG maskable 图标均为 200；Chrome installability errors 为 0；worker `v6 → e2e-previous → e2e-current → v6` 激活时每轮只保留目标缓存。`npm run check` 通过 |
| LN-003 | 首要用户、阶段目标和成功指标 | `product.md` 明确作者本人为当前首要用户、核心记录循环与 MVP 阶段成功条件；北极星指标为“连续有用记录周”，并定义 14 天记录持续性、60 秒有用检索、可靠演示、备份恢复、离线及未来观察/建议/实验指标；功能数量、记录总数和使用时长不作为成功指标；准入评分、红线与 14/30 天退出机制已建立；总控实施过程记录见 `docs/2026-08-13-持续迭代与子Agent调度实施记录.md` |
| LN-018 | 14 天核心使用验证协议 | `docs/2026-08-13-14天核心使用验证协议.md` 提供可直接填写的 Day 1–14 日表、有效记录日/种子排除、两次 60 秒有用检索、Day 1/7/14 备份恢复与离线检查、异常日志、北极星和 10/14 公式、退出/校准条件、隐私边界及 LN-019 证据交接；不新增遥测、账号、同步、提醒或记录必填 |
| LN-019 | 结构化案例与可靠演示包 | `docs/2026-08-13-Log-Note结构化案例与演示包.md` 记录背景、约束、决策、任务分解、已实现能力、失败恢复和证据边界；提供约 6 分 30 秒演示脚本、演示前检查、证据索引、隐私脱敏规则及两次真实展示/评审空模板；明确 14 天真实使用、外部展示与影响证据尚未发生 |
| LN-022 | 输入与模板工作流体验收口 | 模板往返按模板保留草稿，非空/已改记录关闭有放弃保护；已有兼容结构化记录可按字段重开编辑；结构化与单值 prompt 可见，支持快捷保存和必填聚焦；模板新建提供自由文本/结构化/每日单值起点，可复制并即时预览；导出按记录/备份/结构分组，通用示例覆盖 3 种输入模式、5 种字段和 3 种周期。`npm run design:check` 通过；`npm run check` 通过 31/31 单测、4/4 移动端 E2E、PWA 安装/离线/升级、生产构建和差异检查；实施说明见 `docs/2026-08-13-输入与模板工作流优化实施说明.md` |
| LN-023 | 固定记录模块空状态与直达填写 | 修正首页仅在已有周期记录时渲染模块的问题：只要存在周期模板，首页始终显示“固定记录”，未填写项显示“去记录”并可一键打开对应模板，已填写项继续直达编辑；标题显示完成数/总数。31/31 单测、4/4 移动端 E2E、PWA 验证、生产构建、设计校验和差异检查通过 |
| LN-024 | 首屏语言切换与轻量交互去弹层 | 首页页头新增紧凑的 `EN / 中` 原地切换，选择即时生效并在刷新后保留；语言设置从设置弹窗移除；设置弹窗取消“通用/数据”二级分页，打开即进入数据管理。记录编辑、搜索和备份恢复继续保留专注或防误操作弹层。31/31 单测、4/4 移动端 E2E（含双语切换与持久化）、PWA 验证、生产构建、设计校验和差异检查通过 |
| LN-025 | 固定记录首页内联填写 | 固定记录不再打开记录弹窗：晨重、步数、腰围等单值在首页列表直接输入，回车或失焦保存，清空已有值删除；睡眠等结构化周期项在当前行原地展开字段、校验、保存并收起；已有值可原地修改，普通快速记录流程不变。31/31 单测、4/4 移动端 E2E（覆盖单值内联与结构化展开）、PWA 验证、生产构建、设计校验和差异检查通过；页面规范已明确固定记录优先内联交互 |
| LN-026 | 模板与设置管理页面 UI 统一 | 设置从首页弹窗迁移为独立 `/settings` 管理页；模板与设置共用返回、Log Note 眉题、页头尺寸、940px 页面外壳和 760px 主工作区，移动端使用自然文档滚动；首页齿轮改为链接，导出、恢复、Markdown、备份和安装功能保留；`/settings` 纳入离线应用壳。31/31 单测、4/4 移动端 E2E、PWA 在线/离线/升级、生产构建、设计校验和差异检查通过；实施说明见 `docs/2026-08-13-模板与设置管理页面统一实施说明.md` |
| LN-027 | 固定记录首屏布局与信息层级 | P01-T05 完成并经总控独立验收：桌面时间线与固定记录并列进入同一首屏工作区，移动端固定记录先于时间线；移除空时间线视口最小高度与大块占位，进度显示完成/总数及剩余项，新增记录主 CTA 不变。31/31 单测、5/5 移动端 E2E、PWA 在线/离线/升级、生产构建、设计校验和差异检查通过；截图证据见 `output/playwright/ln-027-desktop-empty.png` 与 `output/playwright/ln-027-mobile-empty.png`。 |
| LN-028 | 固定记录填写模型与旧模板兼容 | P01-T06 returned 后由总控精确移植并补充反例修正：value、structured、自定义 legacy free 与不可逆 structured 在首页分流；仅正文符合已知 `标签=值` 形态的旧内置单值项适配为 value，自由原文不因模板 ID 被误判；旧 structured 未知字段保留；固定记录在 localStorage 真正写入成功后才折叠或显示成功，失败与 recovery-blocked 均保留草稿并提示未保存；不迁移模板、不提升 schema、不静默改写正文。34/34 单测、6/6 移动端 E2E、PWA 在线/离线/升级、生产构建、设计校验和差异检查通过。 |
| LN-029 | 固定记录完成反馈与回归验收 | P01-T07 对真实 `localhost:3100` 完成最终只读验收：320×844 与 390×844 的 5 个单值输入均为 16px，保存按钮均为 45×45px，页面无横向滚动；1280×720 时间线与固定记录首屏双栏对齐。触控保存后反馈明确、刷新持久化，旧“去记录”入口为 0；value、structured、legacy free、失败重试、清空、离线、JSON 往返、toast live region 与 reduced-motion 回归通过。34/34 单测、6/6 移动端 E2E、PWA、生产构建、设计校验和差异检查通过；运行时证据见 `output/playwright/ln-029-final-recheck.json` 与三视口 `ln-029-recheck-*.png`。 |
| LN-030 | 固定记录底部布局、统一结构管理与可逆显示 | P01-T08 returned 后由项目总控精确移植并独立验收：首页顺序改为“当天内容 → 固定记录”，快速新增路径不变；移除多数行级横线；领域、分类、记录方式合并为连续层级树但数据概念独立；固定记录提供调整入口和 `homeVisible` 可逆暂停/恢复，旧数据缺失字段默认显示，完整/结构 JSON 往返保留状态，不改历史正文。中英文改为动作与结果语言，桌面编辑使用宽侧边检查器，移动端保留 autosave、44px 触控与软键盘滚动余量。35/35 单测、7/7 核心 E2E、PWA、生产构建、设计校验和差异检查通过；实施说明见 `docs/2026-08-13-LN-030固定记录与结构管理体验实施说明.md`。 |
| LN-031 | 固定记录管理与展开的移动端体验 | P01-T09 对主工作区完成独立运行验收：320×844 与 390×844 首页均无横向滚动，固定记录位于当天内容之后，5 个输入均为 16px、保存触控均为 45×45px；`/templates?focus=periodic` 在 320px 只展示固定记录，拖拽、重排菜单和新增入口均为 44×44px，结构层级无过度缩进；320×500 编辑 sheet 全屏可达，自动保存状态可见；1280×720 无移动端样式回退。旧 T09 worktree 的 `home-timeline.css`、`templates.css`、`run-mobile.mjs`、`template-page.js` 改动保持隔离且未移植。完整主工作区门禁沿用 LN-030 的 35/35 单测、7/7 E2E、PWA、生产构建、设计校验与差异检查通过证据。 |
| LN-032 | Markdown 输出、格式编辑与导出入口去重 | P01-T10 returned 后经项目总控独立验收：`/settings#export` 成为唯一完整导出工作区，将今日/全部 Markdown、即时格式编辑和当前日预览共置；完整 JSON 备份恢复、记录结构与起步示例分别独立分组。模板页重复“导出记录”抽屉及五项下载已删除，页头“导出选项”直接进入 `/settings#structure`；首页当前日快捷导出不变。动作列表移除尾部箭头、蓝底图标块和连续等权横线；320/390/1280 无横向滚动，格式输入 16px，关键动作高 64–74.875px。36/36 单测、7/7 移动端 E2E、PWA 安装/离线/持久化/升级、生产构建、设计校验和差异检查通过；证据见 `output/playwright/ln-032-visual-evidence.json`、四张 `ln-032-*.png` 与 `docs/2026-08-13-Markdown输出工作区实施说明.md`。 |
| LN-033 | 分类视图与 Markdown 统一固定记录归属 | P01-T08 主实现和返工、P01-T10 导出契约核验、P01-T09 跨端验收，经项目总控独立移植与复核完成：分类视图按领域→分类共置普通记录、已填固定记录与未填固定模板；已有固定项按记录实际 `categoryId` 展示和更新，未填模板按模板分类展示，不再因编辑静默搬家；今日与全部 Markdown 同样按记录分类导出。320×500、320×844、390×844、1280×720 无横向滚动，关键移动触控目标至少 44px。37/37 单测、9/9 E2E、PWA、生产构建、设计校验和差异检查通过；证据见 `output/playwright/ln-033-category-390.png`、`output/playwright/results.json`，实施说明见 `docs/2026-08-13-LN-033固定记录分类归属实施说明.md`。 |
| BASE-001 | MVP 数据层基线通过 | 2026-08-12：`npm test` 通过 |
| BASE-002 | MVP 生产构建通过 | 2026-08-12：`npm run build` 成功，静态生成 `/` 与 `/templates` |

## 已知风险与决策记录

1. **工作区风险**：当前分支领先远端且工作区非干净。任何提交、重构或依赖清理都需要先核对已有改动，不能把不相关变更混入同一任务。
2. **真实使用证据缺口**：自动化已覆盖数据保护、移动端核心交互、导出恢复、PWA 离线与升级，但 14 天自然使用尚未开始；测试记录和种子数据不能代替 LN-021。
3. **产品范围决策**：先验证个人记录主线，再决定 AI、自动化、同步或社交；不能以新功能数量替代北极星指标。
4. **AI 分支范围风险**：T02 已从调研扩张为 DeepSeek + SQLite 原型，继续隔离在 worktree；在产品、架构、隐私、数据出设备与最小合入拆分评审前不得并入主线。
5. **影响力验收缺口**：可靠演示基线和结构化案例已具备；仍需 2 次真实展示/评审及至少 1 项有效影响证据，不能把自我评价或无反馈展示计为 KR 完成。
6. **固定记录链路已收口**：LN-027～029 已完成首屏布局、旧模板兼容、保存反馈和跨端验收；期间发现并修复旧 `.next` 长驻进程导致的运行版本漂移，以及移动端 13px 输入和 30×30px 触控目标。后续若再次出现“源码正确、实际页面旧”的现象，必须先核对真实端口进程与构建版本。
7. **附件恢复容量边界**：便携包自身最多 60 MiB、附件静态总量最多 50 MiB；为保护当前图片，恢复不会先删除旧 Blob 腾空间，因此当“现有图片 + 待恢复图片”临时超过 50 MiB 时会明确失败，需后续独立设计跨 localStorage/IndexedDB 的可恢复替换事务后才能放宽。

## 看板维护规则

1. 新任务必须包含：唯一 ID、优先级、用户问题/目标结果、状态、验收标准、依赖、验证方式和产品防退化项；进入“进行中”时补负责人、执行会话和写范围。
2. 一个任务只有在验收标准全部满足并留下测试、构建、截图或文档证据后，才能进入“已完成”。
3. 需求变化先更新本看板，再开始实现；灵感文档不能直接替代正式任务。
4. 每次调度先核对看板、执行任务、工作区和验证证据；原任务未验收时续跑原任务，验收通过后在同一轮从“下一步”顶部拉取第一项 Ready 任务。
5. 发现新风险时优先新增风险卡，不在无关任务中顺手扩大范围。

## 持续迭代协议

项目使用以下闭环，执行者返回不等于任务完成：

```text
Ready → 已分派 → 进行中 → 已返回 → 验证中 → 已验收 → 拉取下一项
                         ↘ 等待 / 返工 ↗
```

1. **Ready 判定**：依赖已完成，目标、范围、验收、验证命令、产品防退化项和权限状态均明确；需要稳定 Codex 任务号时还必须已有合法映射。
2. **不得空转**：Ready 队列非空、没有合法等待、主工作区写槽空闲时，总控必须立即拉取下一项，不能等待下一次心跳或用户催促。
3. **单写多读**：主工作区同时最多 1 个写任务；代码探索、产品研究、日志分析和独立验证可并行使用只读子 Agent。写范围重叠时必须串行，独立写任务需隔离 worktree。
4. **验收交接**：执行者只提交 `returned` 证据；总控依据验收标准独立核对 diff、测试、构建、截图或文档后才能 `accepted`，然后更新看板并立即拉取下一项。
5. **恢复而非复制**：测试失败、限流、`systemError`、宿主截断或意外结束均在原任务恢复；保留现场，不创建重复任务。
6. **合法停止**：仅限等待用户选择/审批/登录/API Key、外部系统或观察期、真实权限/并发冲突、没有 Ready 工作，或项目全部验收完成。
7. **最小纵向切片**：每轮只验证一个用户行为改进，不把新功能、大重构和视觉改版混为一项。

### 产品准入与零回归预算

新能力进入主线前必须回答：改善哪个“快速记→浏览→搜索→编辑/删除→备份恢复→离线”行为；有什么真实痛点证据；默认界面与记录步骤成本；离线/隐私/可逆性；如何验收和移除。无法回答则留在探索区。

一票否决项：静默改写原始记录、核心流程依赖网络/账号/API Key、未授权数据外发、日常记录新增必填步骤、破坏旧备份恢复或现有 E2E。AI、同步账号、社交/匹配、通用待办日历和插件平台默认隔离，未经产品、架构与隐私评审不并入主线。

每轮验收至少保证：`npm run check` 全绿；新增能力有对应回归；移动端快速记录不增加步骤；首页首要 CTA 不增长；常用动作不超过两层；无新增默认弹窗或必填；备份、恢复、离线和旧数据迁移不退化。交互变更另运行 `npm run design:check`。

## Codex 会话总控机制

总控会话负责 O1-KR4 Log Note 影响力成果相关 Codex 会话的命名、状态巡检、异常续跑与并发协调：

- 会话命名：`O1-KR4｜Log Note影响力成果｜00 总控`；`01–49` 为当前执行会话；`90+` 为长期研究或历史会话。
- 自动巡检：Codex 心跳 `Log Note Codex 会话总控巡检`，每 30 分钟运行一次；正常时静默，仅在失败、实际介入或需要决策时报告。

1. 每次巡检以 Codex 项目会话为对象，核对会话状态、最新回合、工作区变化、最近测试/构建结果和验收证据；临时 sub-agent 不作为项目会话统计口径。
2. 以下情况视为异常信号：`systemError`、模型容量或限流、会话意外结束但任务未验收、长时间无进展且没有正常等待原因、失败后无人处理、改动与看板状态不一致、并发写会话冲突或任务偏离验收标准。
3. 出现异常时先读取原会话上下文并复核现场、保留已有改动，再向原会话发送续跑消息；模型容量异常可切换可用模型，不创建重复会话。
4. 等待用户输入、审批、登录、密钥、外部系统变化或明确约定的观察期属于正常等待，不擅自绕过。
5. 任务只有在验收通过后才能结束；执行者结束、代码写完或一次命令成功都不等于任务完成。
6. 新建或发现 Log Note 会话时由总控根据职责统一命名；标题只用于展示，不能替代实际上下文核验。
7. 巡检正常时不制造噪音；只有发生重命名、异常续跑、冲突干预、发现新风险或确需用户决策时才主动报告。
