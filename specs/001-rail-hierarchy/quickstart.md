# Quickstart: Right Rail Visual Hierarchy

1. From `/Users/kual/Desktop/log-note`, run `npm run dev`.
2. Open `http://localhost:3100/` in a mobile viewport.
3. Inspect 390px first, then 320px and 700px.
4. Confirm Search, Calendar, Settings form one compact utility group; the content directory is visually stronger and uses serif text; all controls remain reachable.
5. Run `npm run check` and review the generated `output/playwright/ln-075-unified-rail-390.png` evidence.
